# 정보처리기사 필기 3과목 — 데이터베이스 구축 핵심 정리

> 데이터 모델링 → 관계형 데이터베이스 → 키와 무결성 → 관계대수 → 정규화 → 트랜잭션 → 데이터베이스 구조 → SQL

이 문서는 원본 PDF에서 `3과목 데이터베이스 구축` 제목 다음에 나오는 논리적 설계부터 `4과목 프로그래밍 언어 활용` 직전의 트리거까지를 바탕으로 정리했습니다.

# 1. 데이터베이스 설계

## 1-1. 논리적 설계(Logical Design)

논리적 설계는 자료를 특정 DBMS가 지원하는 논리적 자료 구조로 변환하는 과정입니다.

- 개념 스키마를 평가하고 정제합니다.
- 데이터 모델로 매핑(Mapping)합니다.
- 트랜잭션 인터페이스를 설계합니다.

**핵심:** 테이블, 속성, 관계처럼 데이터를 논리적으로 어떻게 표현할지 결정합니다.

## 1-2. 물리적 설계(Physical Design)

물리적 설계는 논리적 구조로 표현한 데이터를 실제 저장장치에 저장할 물리적 구조로 바꾸는 과정입니다.

- 파일의 저장 구조와 접근 경로(Access Path)를 결정합니다.
- 레코드의 형식, 저장 순서, 접근 경로를 정합니다.

| 구분 | 의미 |
|---|---|
| 논리적 설계(Logical Design) | 테이블·속성·관계 등 논리적 데이터 구조 설계 |
| 물리적 설계(Physical Design) | 파일·레코드·인덱스·접근 경로 등 실제 저장 방식 설계 |

# 2. 데이터 모델(Data Model)과 관계

데이터 모델은 현실 세계의 데이터를 데이터베이스에 표현하는 방법입니다.

| 구성 요소 | 의미 |
|---|---|
| 구조(Structure) | 개체와 관계로 표현되는 데이터의 정적 구조 |
| 연산(Operation) | 데이터를 검색·삽입·삭제·수정하는 방법 |
| 제약 조건(Constraint) | 데이터가 지켜야 하는 논리적 조건 |

> 구조 + 연산 + 제약 조건

## 2-1. 관계의 형태

| 관계 | 의미 | 예시 |
|---|---|---|
| 일 대 일(1:1) | 한 개체가 다른 한 개체와 대응 | 사람과 주민등록번호 |
| 일 대 다(1:N) | 한 개체가 여러 개체와 대응 | 하나의 학과와 여러 학생 |
| 다 대 다(N:M) | 양쪽이 서로 여러 개체와 대응 | 학생과 수강 과목 |

다 대 다 관계는 보통 중간 테이블을 만들어 두 개의 일 대 다 관계로 나눕니다.

# 3. E-R 다이어그램(Entity-Relationship Diagram)

E-R 다이어그램은 개체와 관계를 그림으로 표현하는 모델링 도구입니다.

| 기호 | 의미 |
|---|---|
| 사각형 | 개체(Entity) 타입 |
| 마름모 | 관계(Relationship) 타입 |
| 타원 | 속성(Attribute) |
| 이중 타원 | 다중값 속성(Multivalued Attribute) |
| 선 | 개체·관계·속성을 연결 |

```text
[학생] - <수강> - [과목]
  |                 |
(학번)           (과목번호)
```

# 4. 관계형 데이터베이스의 기본 용어

## 4-1. 릴레이션(Relation), 튜플(Tuple), 속성(Attribute)

릴레이션은 관계형 데이터베이스의 2차원 테이블입니다.

| 용어 | 의미 |
|---|---|
| 릴레이션(Relation) | 데이터를 저장하는 테이블 |
| 튜플(Tuple) | 테이블의 행(Row) |
| 속성(Attribute) | 테이블의 열(Column) |
| 카디널리티(Cardinality) | 튜플, 즉 행의 수 |
| 디그리(Degree) | 속성, 즉 열의 수 |
| 도메인(Domain) | 한 속성이 가질 수 있는 원자값의 집합 |

## 4-2. 릴레이션의 특징

- 같은 릴레이션에 중복 튜플은 존재할 수 없습니다.
- 튜플 사이와 속성 사이에는 본질적인 순서가 없습니다.
- 속성 이름은 릴레이션 안에서 유일해야 합니다.
- 속성값은 더 이상 나눌 수 없는 원자값(Atomic Value)이어야 합니다.

# 5. 키(Key)와 데이터 무결성(Data Integrity)

키는 튜플을 식별하거나 릴레이션 사이를 연결하는 속성 또는 속성 집합입니다.

| 키 | 유일성 | 최소성 | 의미 |
|---|---:|---:|---|
| 슈퍼키(Super Key) | O | 필수 아님 | 튜플을 유일하게 식별 |
| 후보키(Candidate Key) | O | O | 기본키가 될 수 있는 최소 키 |
| 기본키(Primary Key) | O | O | 후보키 중 대표로 선택한 키 |
| 대체키(Alternate Key) | O | O | 기본키로 선택되지 않은 후보키 |
| 외래키(Foreign Key) | 필수 아님 | 필수 아님 | 다른 릴레이션의 기본키를 참조 |

**핵심:** 후보키는 유일성(Uniqueness)과 최소성(Minimality)을 모두 만족합니다.

## 5-1. 무결성 제약 조건

| 무결성 | 의미 |
|---|---|
| 개체 무결성(Entity Integrity) | 기본키는 Null이나 중복값을 가질 수 없음 |
| 참조 무결성(Referential Integrity) | 외래키는 Null이거나 참조 대상 기본키 값이어야 함 |

예를 들어 학생 테이블의 `학과코드`가 학과 테이블의 기본키를 참조한다면, 존재하지 않는 학과코드를 학생에게 입력할 수 없습니다.

# 6. 관계대수(Relational Algebra)와 관계해석(Relational Calculus)

| 구분 | 의미 |
|---|---|
| 관계대수(Relational Algebra) | 어떤 연산을 어떤 순서로 할지 표현하는 절차적 언어 |
| 관계해석(Relational Calculus) | 원하는 결과가 무엇인지 표현하는 비절차적 언어 |

> 관계대수: 어떻게(How)  
> 관계해석: 무엇을(What)

## 6-1. 순수 관계 연산자

| 연산 | 기호 | 의미 |
|---|---|---|
| 셀렉트(Select) | `σ` | 조건에 맞는 행(튜플)을 선택 |
| 프로젝트(Project) | `π` | 지정한 열(속성)만 추출 |
| 조인(Join) | `⋈` | 공통 속성을 기준으로 두 릴레이션 결합 |
| 디비전(Division) | `÷` | 특정 조건을 모두 만족하는 튜플을 찾음 |

```text
σ 학과='컴퓨터' (학생)    -> 컴퓨터학과 학생 행 선택
π 이름, 학과 (학생)      -> 이름과 학과 열만 추출
```

**핵심:** 셀렉트는 행, 프로젝트는 열을 고릅니다.

## 6-2. 일반 집합 연산자

| 연산 | 의미 |
|---|---|
| 합집합(Union) | 두 릴레이션의 튜플을 합침 |
| 교집합(Intersection) | 공통 튜플만 선택 |
| 차집합(Difference) | 첫 릴레이션에만 있는 튜플 선택 |
| 교차곱(Cartesian Product) | 가능한 모든 튜플 조합 생성 |

교차곱에서 R과 S의 결과는 다음과 같습니다.

```text
결과 디그리 = R의 디그리 + S의 디그리
결과 카디널리티 = R의 카디널리티 × S의 카디널리티
```

# 7. 정규화(Normalization)와 이상(Anomaly)

정규화는 잘못 설계된 릴레이션을 분해하여 중복을 줄이고 이상 현상을 막는 과정입니다.

| 이상 현상 | 의미 |
|---|---|
| 삽입 이상(Insertion Anomaly) | 불필요한 데이터 없이는 필요한 데이터를 삽입할 수 없음 |
| 삭제 이상(Deletion Anomaly) | 한 데이터를 삭제하며 필요한 다른 정보도 함께 사라짐 |
| 갱신 이상(Update Anomaly) | 중복 데이터 일부만 수정해 값이 불일치 |

## 7-1. 함수적 종속(Functional Dependency)

함수적 종속은 어떤 속성값이 다른 속성값을 결정하는 관계입니다.

```text
학번 -> 이름
```

학번을 알면 이름이 결정되므로 학번은 결정자(Determinant), 이름은 종속자(Dependent)입니다.

이행적 함수 종속(Transitive Functional Dependency)은 다음과 같습니다.

```text
A -> B, B -> C 이면 A -> C
학번 -> 학과코드, 학과코드 -> 학과명 이면 학번 -> 학과명
```

## 7-2. 정규화 과정

| 정규형 | 제거하거나 만족해야 하는 조건 |
|---|---|
| 제1정규형(1NF) | 도메인을 원자값으로 구성 |
| 제2정규형(2NF) | 부분 함수 종속 제거 |
| 제3정규형(3NF) | 이행 함수 종속 제거 |
| 보이스-코드 정규형(BCNF) | 모든 결정자가 후보키가 되도록 구성 |
| 제4정규형(4NF) | 다치 종속 제거 |
| 제5정규형(5NF) | 조인 종속 제거 |

> **원부이결다조**  
> 원자값 → 부분 함수 종속 → 이행 함수 종속 → 결정자 → 다치 종속 → 조인 종속

## 7-3. 반정규화(Denormalization)

반정규화는 성능과 운영 편의를 위해 정규화한 데이터를 의도적으로 통합·중복·분리하는 작업입니다.

| 방법 | 핵심 | 장점 | 주의점 |
|---|---|---|---|
| 통합 | 관련 테이블을 하나로 합침 | 조인 횟수 감소 | 행 크기와 갱신 범위가 커질 수 있음 |
| 중복 | 자주 조회하는 속성이나 데이터를 중복 저장 | 조회 절차 단순화 | 데이터 일관성 관리와 갱신 비용 증가 |
| 분리 | 테이블을 수직 또는 수평으로 나눔 | 자주 사용하는 데이터의 조회 범위 축소 | 추가 조인이나 관리 작업이 필요할 수 있음 |

| 구분 | 목적 |
|---|---|
| 정규화(Normalization) | 중복 제거와 무결성 향상 |
| 반정규화(Denormalization) | 조회 성능 향상과 조인 횟수 감소 |

반정규화는 성능에는 도움이 될 수 있지만 데이터 중복과 갱신 비용을 늘릴 수 있습니다.

# 8. 시스템 카탈로그, 트랜잭션, 인덱스, 뷰

## 8-1. 시스템 카탈로그(System Catalog)

시스템 카탈로그는 테이블, 속성, 인덱스, 사용자 권한 같은 데이터베이스의 메타데이터(Metadata)를 저장합니다.

- 사용자는 내용을 검색할 수 있습니다.
- 사용자가 직접 갱신하지 않고 DBMS가 관리합니다.

## 8-2. 트랜잭션(Transaction)과 ACID

트랜잭션은 데이터베이스에서 하나의 논리적인 작업 단위입니다. 계좌이체의 출금과 입금처럼 관련 작업은 모두 성공하거나 모두 실패해야 합니다.

| 특성 | 의미 |
|---|---|
| 원자성(Atomicity) | 모든 연산이 전부 실행되거나 전부 취소되어야 함 |
| 일관성(Consistency) | 실행 전후 데이터베이스의 제약조건과 일관성이 유지됨 |
| 독립성(Isolation) | 동시에 실행되는 트랜잭션이 서로 중간 결과에 간섭하지 않음 |
| 영속성(Durability) | 완료된 결과는 장애가 나도 영구적으로 보존됨 |

> **원일독영**

- 커밋(Commit): 변경 내용을 확정합니다.
- 롤백(Rollback): 변경 내용을 취소하고 이전 상태로 되돌립니다.

## 8-3. 인덱스(Index)와 뷰(View)

인덱스는 `<키 값, 포인터>` 형태로 데이터를 빠르게 찾기 위한 구조입니다. 검색은 빨라지지만 저장 공간이 필요하고 삽입·삭제·수정 때도 관리 비용이 듭니다.

```sql
CREATE INDEX idx_학생_이름 ON 학생(이름);
DROP INDEX idx_학생_이름;
```

인덱스는 `CREATE INDEX`로 생성하고 `DROP INDEX`로 삭제합니다. 인덱스 속성을 변경하는 세부 문법은 DBMS마다 달라서 필요한 경우 삭제 후 다시 생성할 수 있습니다.

뷰는 기본 테이블에서 필요한 데이터만 추출해 만든 가상 테이블입니다.

```sql
CREATE VIEW 우수학생 AS
SELECT 학번, 이름, 점수
FROM 학생
WHERE 점수 >= 90;

DROP VIEW 우수학생;
```

뷰는 물리적으로 독립된 데이터를 저장하지 않으며, 보안성과 논리적 독립성을 높이고 복잡한 질의를 단순하게 보이게 합니다.

| 뷰 형태 | 삽입·갱신·삭제 가능성 |
|---|---|
| 하나의 기본 테이블에서 키를 포함해 만든 단순 뷰 | DBMS가 허용하면 가능할 수 있음 |
| 조인·집계 함수·`DISTINCT`·`GROUP BY`를 포함한 뷰 | 기본 테이블의 한 행을 바로 특정하기 어려워 일반적으로 제한됨 |

# 9. 파티션, 분산 데이터베이스, 접근통제

## 9-1. 파티션(Partition)

| 종류 | 의미 |
|---|---|
| 범위 분할(Range Partitioning) | 값의 범위를 기준으로 분할 |
| 해시 분할(Hash Partitioning) | 해시 함수 결과로 분할 |
| 조합 분할(Composite Partitioning) | 둘 이상의 분할 방식을 결합 |
| 목록 분할(List Partitioning) | 지정한 값 목록을 기준으로 분할 |
| 라운드 로빈 분할(Round Robin Partitioning) | 레코드를 순서대로 균등 분배 |

## 9-2. 분산 데이터베이스의 투명성

| 투명성 | 의미 |
|---|---|
| 위치 투명성(Location Transparency) | 실제 데이터 저장 위치를 몰라도 접근 가능 |
| 중복 투명성(Replication Transparency) | 복제 데이터가 여러 곳에 있어도 하나처럼 사용 |
| 병행 투명성(Concurrency Transparency) | 동시 트랜잭션이 결과에 영향을 주지 않음 |
| 장애 투명성(Failure Transparency) | 일부 장애가 있어도 정확한 처리를 지원 |

> **위중병장**

## 9-3. 분할(Fragmentation)

| 구분 | 기준 |
|---|---|
| 수평 분할(Horizontal Fragmentation) | 행(Row)을 기준으로 분할 |
| 수직 분할(Vertical Fragmentation) | 열(Column)을 기준으로 분할 |

## 9-4. 접근통제(Access Control)

| 방식 | 권한 부여 기준 |
|---|---|
| 임의 접근통제(DAC) | 사용자 신원이나 소유자 판단 |
| 강제 접근통제(MAC) | 주체와 객체의 보안 등급 |
| 역할기반 접근통제(RBAC) | 조직 내 사용자의 역할 |

# 10. 저장장치 구성

| 구분 | 특징 | 대표 연결 방식 |
|---|---|---|
| 직접 연결 저장장치(DAS, Direct Attached Storage) | 서버와 저장장치를 케이블로 직접 연결 | SATA, eSATA, SCSI, SAS |
| 저장 영역 네트워크(SAN, Storage Area Network) | 고속 네트워크로 서버와 저장장치를 연결, 블록 단위 처리 | iSCSI, FC, FCoE |

# 11. SQL 언어의 분류

| 분류 | 역할 | 대표 명령어 |
|---|---|---|
| 데이터 정의어(DDL, Data Definition Language) | 구조를 정의·변경·삭제 | `CREATE`, `ALTER`, `DROP` |
| 데이터 조작어(DML, Data Manipulation Language) | 실제 데이터를 검색·삽입·수정·삭제 | `SELECT`, `INSERT`, `UPDATE`, `DELETE` |
| 데이터 제어어(DCL, Data Control Language) | 보안, 권한, 회복, 트랜잭션 제어 | `COMMIT`, `ROLLBACK`, `GRANT`, `REVOKE` |

## 11-1. 권한 및 트랜잭션 제어 예

```sql
GRANT SELECT ON 학생 TO trainee;
REVOKE SELECT ON 학생 FROM trainee;
```

`GRANT`는 `trainee`에게 학생 테이블의 조회 권한을 부여하고, `REVOKE`는 그 권한을 회수합니다.

```sql
UPDATE 학생 SET 학과코드 = 20 WHERE 학번 = 1001;
ROLLBACK;

UPDATE 학생 SET 학과코드 = 20 WHERE 학번 = 1001;
COMMIT;
```

첫 번째 변경은 `ROLLBACK`으로 취소되고, 두 번째 변경은 `COMMIT`으로 확정됩니다.

# 12. 데이터 정의어(DDL)

## 12-1. CREATE TABLE

```sql
CREATE TABLE 학생 (
    학번 INT NOT NULL,
    이름 VARCHAR(20) NOT NULL,
    학과코드 INT,
    PRIMARY KEY (학번),
    FOREIGN KEY (학과코드)
        REFERENCES 학과(학과코드)
);
```

| 제약조건 | 의미 |
|---|---|
| `PRIMARY KEY` | 기본키 지정 |
| `FOREIGN KEY` | 외래키 지정 |
| `REFERENCES` | 참조할 테이블과 속성 지정 |
| `NOT NULL` | Null 값 금지 |
| `UNIQUE` | 중복값 금지 |
| `DEFAULT` | 기본값 지정 |
| `CHECK` | 입력값 조건 지정 |

## 12-2. ALTER TABLE과 DROP TABLE

```sql
ALTER TABLE 학생 ADD 전화번호 VARCHAR(20);
ALTER TABLE 학생 DROP COLUMN 전화번호;
ALTER TABLE 학생 ALTER COLUMN 학과코드 SET DEFAULT 10;

DROP TABLE 학생;
DROP TABLE 학생 CASCADE;
DROP TABLE 학생 RESTRICT;
```

> **주의:** `ALTER TABLE`의 열 변경 및 `SET DEFAULT` 세부 문법은 DBMS마다 다를 수 있습니다.

| 옵션 | 의미 |
|---|---|
| `CASCADE` | 참조하는 관련 개체도 함께 삭제 |
| `RESTRICT` | 참조 중인 개체가 있으면 삭제 거부 |

# 13. 데이터 조작어(DML)

## 13-1. INSERT, UPDATE, DELETE

```sql
INSERT INTO 학생(학번, 이름, 학과코드)
VALUES (1001, '홍길동', 10);

UPDATE 학생
SET 학과코드 = 20
WHERE 학번 = 1001;

DELETE FROM 학생
WHERE 학번 = 1001;
```

**주의:** `UPDATE`나 `DELETE`에서 `WHERE`절을 생략하면 모든 튜플이 수정되거나 삭제될 수 있습니다.

## 13-2. SELECT문

```sql
SELECT 속성명
FROM 테이블명
WHERE 조건
GROUP BY 속성명
HAVING 그룹조건
ORDER BY 속성명 ASC;
```

| 절 | 의미 |
|---|---|
| `SELECT` | 출력할 열 지정 |
| `FROM` | 검색할 테이블 지정 |
| `WHERE` | 개별 튜플의 조건 |
| `GROUP BY` | 속성을 기준으로 그룹화 |
| `HAVING` | 그룹에 대한 조건 |
| `ORDER BY` | 결과 정렬 |

작성 순서와 논리적 처리 순서는 다릅니다.

```text
작성 순서: SELECT -> FROM -> WHERE -> GROUP BY -> HAVING -> ORDER BY
처리 순서: FROM -> WHERE -> GROUP BY -> HAVING -> SELECT -> ORDER BY
```

`DISTINCT`는 중복 결과를 제거합니다.

```sql
SELECT DISTINCT 학과코드
FROM 학생;
```

> **주의:** SQL 표준에서 `ASC`는 오름차순, `DESC`는 내림차순입니다. 정렬 방향을 생략하면 기본값인 `ASC`가 적용됩니다.

# 14. 조건, 그룹, 하위 질의

## 14-1. LIKE와 BETWEEN

| 기호 | 의미 |
|---|---|
| `%` | 0개 이상의 모든 문자 |
| `_` | 문자 한 개 |

```sql
SELECT * FROM 학생 WHERE 이름 LIKE '김%';
SELECT * FROM 학생 WHERE 이름 LIKE '_지%';
SELECT * FROM 학생 WHERE 점수 BETWEEN 80 AND 90;
```

`BETWEEN 80 AND 90`은 일반적으로 양 끝값을 포함합니다.

## 14-2. 그룹 함수와 GROUP BY

| 함수 | 의미 |
|---|---|
| `COUNT(*)` | NULL 여부와 관계없이 전체 행 수 |
| `COUNT(표현식)` | 표현식의 값이 NULL이 아닌 행 수 |
| `SUM()` | 합계 |
| `AVG()` | 평균 |
| `MAX()` | 최댓값 |
| `MIN()` | 최솟값 |

```sql
SELECT 학과코드, AVG(점수)
FROM 학생
GROUP BY 학과코드
HAVING AVG(점수) >= 80;
```

예를 들어 `점수예시` 테이블에 `90`, `NULL`, `80`의 세 행이 있으면 다음 결과가 나옵니다.

```sql
SELECT COUNT(*), COUNT(점수)
FROM 점수예시;
```

| `COUNT(*)` | `COUNT(점수)` |
|---:|---:|
| 3 | 2 |

**핵심:** `WHERE`는 그룹화 전의 개별 튜플에, `HAVING`은 그룹화 후의 그룹에 적용합니다.

## 14-3. 하위 질의(Subquery)

하위 질의는 SQL문 안에 포함된 또 다른 `SELECT`문입니다. 안쪽 질의를 먼저 실행하고 결과를 바깥 질의의 조건에 사용합니다.

```sql
SELECT 이름
FROM 학생
WHERE 학과코드 = (
    SELECT 학과코드
    FROM 학과
    WHERE 학과명 = '컴퓨터공학과'
);
```

# 15. 집합 연산자와 트리거

| 연산자 | 의미 |
|---|---|
| `UNION` | 두 결과를 합치고 중복 행 제거 |
| `UNION ALL` | 두 결과를 합치고 중복 행도 유지 |
| `INTERSECT` | 공통 행만 출력 |
| `EXCEPT` | 첫 번째 결과에서 두 번째 결과를 제외 |

다음 두 테이블의 학번을 입력으로 사용합니다.

| 테이블 | 학번 |
|---|---|
| `수강생_A` | 1001, 1002 |
| `수강생_B` | 1002, 1003 |

```sql
SELECT 학번 FROM 수강생_A
UNION
SELECT 학번 FROM 수강생_B;

SELECT 학번 FROM 수강생_A
UNION ALL
SELECT 학번 FROM 수강생_B;

SELECT 학번 FROM 수강생_A
INTERSECT
SELECT 학번 FROM 수강생_B;

SELECT 학번 FROM 수강생_A
EXCEPT
SELECT 학번 FROM 수강생_B;
```

`ORDER BY`를 사용하지 않은 출력 순서는 제외하고 결과 집합을 비교하면 다음과 같습니다.

| 연산자 | 결과 학번 |
|---|---|
| `UNION` | 1001, 1002, 1003 |
| `UNION ALL` | 1001, 1002, 1002, 1003 |
| `INTERSECT` | 1002 |
| `EXCEPT` | 1001 |

트리거(Trigger)는 `INSERT`, `UPDATE`, `DELETE` 같은 이벤트가 발생할 때 관련 작업을 자동으로 수행하는 절차형 SQL입니다.

```text
주문 등록 -> 재고 수량 자동 감소
회원 삭제 -> 관련 로그 자동 기록
```

# 시험 직전 필수 암기표

| 주제 | 핵심 내용 |
|---|---|
| 데이터 모델 요소 | 구조·연산·제약 조건 |
| 논리적 설계 | 테이블·속성·관계 같은 논리 구조 설계 |
| 물리적 설계 | 파일·레코드·인덱스·접근 경로 설계 |
| 튜플(Tuple) | 행(Row) |
| 속성(Attribute) | 열(Column) |
| 카디널리티(Cardinality) | 튜플의 수 |
| 디그리(Degree) | 속성의 수 |
| 후보키(Candidate Key) | 유일성 + 최소성 |
| 슈퍼키(Super Key) | 유일성은 만족, 최소성은 필수 아님 |
| 개체 무결성 | 기본키는 Null·중복 불가 |
| 참조 무결성 | 외래키는 Null 또는 존재하는 참조 기본키 값 |
| 셀렉트(Select) | 행 선택 |
| 프로젝트(Project) | 열 선택 |
| 관계대수 | 절차적 |
| 관계해석 | 비절차적 |
| 정규화 순서 | 원부이결다조 |
| 반정규화 | 성능을 위해 의도적으로 통합·중복·분리 |
| ACID | 원자성·일관성·독립성·영속성 |
| 인덱스(Index) | 검색 속도 향상, 갱신 비용 발생 |
| 뷰(View) | 기본 테이블에서 유도된 가상 테이블 |
| 수평 분할 | 행 분할 |
| 수직 분할 | 열 분할 |
| 분산 투명성 | 위중병장 |
| DAC | 사용자 신원·소유자 판단 |
| MAC | 보안 등급 |
| RBAC | 역할 |
| DDL | CREATE·ALTER·DROP |
| DML | SELECT·INSERT·UPDATE·DELETE |
| DCL | COMMIT·ROLLBACK·GRANT·REVOKE |
| WHERE | 개별 튜플 조건 |
| HAVING | 그룹 조건 |
| UNION | 중복 제거 |
| UNION ALL | 중복 유지 |
| 트리거(Trigger) | 이벤트 발생 시 자동 실행 |
