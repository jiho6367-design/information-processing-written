첨부한 정보처리기사 필기 핵심요약 PDF를 분석하여, 1과목의 시험 대비 정리본을 Markdown(.md) 파일로 작성해주세요.

이번 작업 대상:
- 1과목: 소프트웨어 설계

중요:
- 단순히 채팅창에 내용을 출력하지 말고 실제 `.md` 파일을 생성해주세요.
- 파일명은 다음과 같이 저장해주세요.
  `정보처리기사_1과목_소프트웨어설계_핵심정리.md`
- PDF의 첫 페이지부터 `2과목 소프트웨어 개발`이라는 제목이 등장하기 직전까지를 1과목 범위로 판단해주세요.
- PDF 첫 페이지에서는 `1과목 소프트웨어 설계` 제목보다 앞에 폭포수 모형, 프로토타입 모형, 나선형 모형, 애자일 모형 등의 내용이 배치되어 있으므로 이 내용도 반드시 1과목에 포함해주세요.
- `2과목 소프트웨어 개발` 제목 이후의 자료구조, 정렬, 테스트, 인터페이스 구현 내용은 포함하지 마세요.
- PDF 내용을 단순 복사하지 말고 시험 대비용으로 이해하기 쉽게 재구성해주세요.
- 원문의 오탈자나 명백한 기술적 오류가 있으면 올바른 내용으로 수정해주세요.
- 작업이 끝나면 생성된 Markdown 파일의 정확한 경로를 알려주세요.

────────────────────
1. 문서 목적
────────────────────

이 문서는 정보처리기사 필기시험 직전 복습에 사용하는 학습 노트입니다.

다음 기준으로 작성해주세요.

- 대학생 초보자가 이해할 수 있는 수준
- 시험에 필요한 핵심 개념과 구분 기준 중심
- 단순한 용어 나열이 아니라 개념의 의미가 이해되도록 설명
- 서로 혼동하기 쉬운 개념은 비교표로 정리
- 순서가 있는 개념은 흐름과 암기법을 함께 표시
- UML 관계·다이어그램·디자인 패턴은 표로 체계적으로 구분
- 예상문제, 연습문제, 퀴즈, 정답 부분은 작성하지 않음

────────────────────
2. 문서 제목과 전체 흐름
────────────────────

문서 제목은 다음과 같이 작성해주세요.

# 정보처리기사 필기 1과목 — 소프트웨어 설계 핵심 정리

문서 시작 부분에 전체 학습 흐름을 한 줄로 작성해주세요.

예시:

> 소프트웨어 생명주기 → 애자일 개발 → 요구사항 분석 → UML → 사용자 인터페이스 → 모듈 설계 → 객체지향 설계 → 디자인 패턴 → 미들웨어

이후 다음과 같은 구조로 작성해주세요.

# 1. 소프트웨어 공학

# 2. 소프트웨어 생명주기 모형

# 3. 애자일 개발

# 4. 요구사항의 종류

# 5. 요구사항 개발 프로세스

# 6. 요구사항 분석 도구

# 7. UML

# 8. UML의 관계

# 9. UML 다이어그램

# 10. 유스케이스와 순차 다이어그램

# 11. 사용자 인터페이스

# 12. UI 설계 원칙과 프로토타입 도구

# 13. 소프트웨어 품질 표준

# 14. 모듈화와 추상화

# 15. 정보 은닉과 캡슐화

# 16. 소프트웨어 아키텍처 패턴

# 17. 객체지향 기본 개념

# 18. 럼바우 분석 기법

# 19. 객체지향 설계 원칙 SOLID

# 20. 결합도와 응집도

# 21. 효과적인 모듈 설계

# 22. 디자인 패턴

# 23. 요구사항 검증

# 24. 미들웨어

# 시험 직전 필수 암기표

PDF 흐름에 따라 세부 제목은 조정할 수 있지만 핵심 내용을 누락하지 마세요.

────────────────────
3. 한글과 영어 용어 표기 규칙
────────────────────

모든 주요 전문 용어는 처음 등장할 때 다음 형식으로 작성해주세요.

`한글 용어(English Term)`

예시:

- 폭포수 모형(Waterfall Model)
- 프로토타입 모형(Prototyping Model)
- 나선형 모형(Spiral Model)
- 애자일(Agile)
- 요구사항 도출(Elicitation)
- 일반화 관계(Generalization)
- 사용자 인터페이스(User Interface)
- 정보 은닉(Information Hiding)
- 캡슐화(Encapsulation)
- 결합도(Coupling)
- 응집도(Cohesion)

한글과 영어를 별도 열로 분리하지 마세요.

잘못된 형식:

| 개념 | 영어 | 의미 |
|---|---|---|
| 캡슐화 | Encapsulation | 데이터와 함수를 묶음 |

올바른 형식:

| 개념 | 의미 |
|---|---|
| 캡슐화(Encapsulation) | 데이터와 데이터를 처리하는 기능을 하나로 묶음 |

────────────────────
4. 설명 형식
────────────────────

각 개념은 가능한 경우 다음 순서로 설명해주세요.

1. 정의
2. 주요 특징
3. 장점 또는 단점
4. 유사 개념과 비교
5. 시험에서 기억할 핵심

시험에 중요한 내용은 다음 형식으로 강조해주세요.

```markdown
**핵심:** 나선형 모형(Spiral Model)은 위험 분석을 중심으로 반복 개발한다.
```

────────────────────
5. 반드시 포함할 내용
────────────────────

## 소프트웨어 공학의 기본 원칙

- 현대적인 프로그래밍 기술의 지속적인 적용
- 소프트웨어 품질의 지속적인 검증
- 개발 과정과 결과에 대한 명확한 기록 유지

## 소프트웨어 생명주기 모형

- 폭포수 모형(Waterfall Model)
- 프로토타입 모형(Prototyping Model)
- 나선형 모형(Spiral Model)
- 애자일 모형(Agile Model)

비교표:

| 모형 | 핵심 특징 |
|---|---|
| 폭포수 모형(Waterfall Model) | 단계를 순차적으로 진행하며 이전 단계로 돌아가기 어려움 |
| 프로토타입 모형(Prototyping Model) | 견본을 통해 불명확한 요구사항을 확인 |
| 나선형 모형(Spiral Model) | 위험 분석을 중심으로 반복 개발 |
| 애자일 모형(Agile Model) | 짧은 반복과 고객 협업으로 변화에 대응 |

나선형 모형의 순서를 강조해주세요.

> 계획 수립 → 위험 분석 → 개발 및 검증 → 고객 평가

## 애자일 개발

- 스크럼(Scrum)
- 익스트림 프로그래밍(XP)
- 기능 중심 개발(FDD)
- 칸반(Kanban)
- 린(Lean)
- 스프린트(Sprint)
- 일일 스크럼(Daily Scrum)

애자일 선언의 4가지 핵심 가치를 정리해주세요.

- 프로세스와 도구보다 개인과 상호작용
- 방대한 문서보다 실행되는 소프트웨어
- 계약 협상보다 고객과 협업
- 계획 준수보다 변화에 대응

XP 핵심 가치:

- 의사소통(Communication)
- 단순성(Simplicity)
- 피드백(Feedback)
- 용기(Courage)
- 존중(Respect)

## 리팩토링

- 리팩토링(Refactoring)
- 외부 동작과 기능은 변경하지 않음
- 내부 구조를 개선
- 가독성·유지보수성 향상

## 요구사항

- 기능 요구사항(Functional Requirement)
- 비기능 요구사항(Non-Functional Requirement)
- 성능
- 보안
- 품질
- 제약사항
- 인터페이스 요구사항

비교표:

| 구분 | 의미 |
|---|---|
| 기능 요구사항(Functional Requirement) | 시스템이 수행해야 하는 기능 |
| 비기능 요구사항(Non-Functional Requirement) | 성능·보안·품질·제약 등 시스템의 품질 조건 |

## 요구사항 개발 프로세스

다음 순서를 정확하게 표시해주세요.

> 도출(Elicitation) → 분석(Analysis) → 명세(Specification) → 확인(Validation)

각 단계의 역할을 설명해주세요.

## 요구사항 분석 도구

- 자료 흐름도(DFD)
- 자료 사전(Data Dictionary)
- 소단위 명세서(Mini-Specification)
- 개체 관계도(ERD)
- 상태 전이도(State Transition Diagram)
- 제어 명세서(Control Specification)
- HIPO

자료 흐름도 구성 요소:

| 구성 요소 | 의미 |
|---|---|
| 프로세스(Process) | 입력 자료를 처리하여 출력 자료로 변환 |
| 자료 흐름(Data Flow) | 데이터가 이동하는 경로 |
| 자료 저장소(Data Store) | 데이터가 저장되는 장소 |
| 단말(Terminator) | 시스템 외부의 데이터 발생·수신 주체 |

HIPO의 특징:

- 하향식 문서화
- 기능과 자료의 의존 관계 표현
- 가시적 도표 사용

## UML

- 통합 모델링 언어(UML, Unified Modeling Language)
- 사물(Things)
- 관계(Relationships)
- 다이어그램(Diagram)
- 스테레오타입(Stereotype)
- 길러멧(Guillemet) `<< >>`

## UML의 주요 관계

- 연관 관계(Association)
- 집합 관계(Aggregation)
- 포함 관계(Composition)
- 일반화 관계(Generalization)
- 의존 관계(Dependency)
- 실체화 관계(Realization)

집합과 포함 관계도 구분해주세요.

| 관계 | 의미 |
|---|---|
| 집합 관계(Aggregation) | 부분 객체가 전체 객체와 독립적으로 존재 가능 |
| 포함 관계(Composition) | 전체 객체가 소멸하면 부분 객체도 함께 소멸 |

## UML 다이어그램

구조적 다이어그램(Structural Diagram):

- 클래스 다이어그램(Class Diagram)
- 객체 다이어그램(Object Diagram)
- 컴포넌트 다이어그램(Component Diagram)
- 배치 다이어그램(Deployment Diagram)
- 복합체 구조 다이어그램(Composite Structure Diagram)
- 패키지 다이어그램(Package Diagram)

행위 다이어그램(Behavioral Diagram):

- 유스케이스 다이어그램(Use Case Diagram)
- 순차 다이어그램(Sequence Diagram)
- 커뮤니케이션 다이어그램(Communication Diagram)
- 상태 다이어그램(State Diagram)
- 활동 다이어그램(Activity Diagram)
- 상호작용 개요 다이어그램(Interaction Overview Diagram)
- 타이밍 다이어그램(Timing Diagram)

다음 비교를 강조해주세요.

> 구조적 다이어그램은 시스템의 정적 구조, 행위 다이어그램은 동적인 동작을 표현한다.

## 유스케이스와 순차 다이어그램

- 액터(Actor)
- 주 액터(Primary Actor)
- 부 액터(Secondary Actor)
- 객체(Object)
- 생명선(Lifeline)
- 실행 상자(Activation Box)
- 메시지(Message)

## 사용자 인터페이스

- 명령줄 인터페이스(CLI)
- 그래픽 사용자 인터페이스(GUI)
- 자연어 사용자 인터페이스(NUI)

비교표:

| 인터페이스 | 특징 |
|---|---|
| 명령줄 인터페이스(CLI) | 텍스트 명령어를 입력 |
| 그래픽 사용자 인터페이스(GUI) | 아이콘·메뉴·포인터를 사용 |
| 자연어 사용자 인터페이스(NUI) | 말·동작·표정 등 자연스러운 행위로 조작 |

UI 기본 원칙:

- 직관성(Intuitiveness)
- 유효성(Effectiveness)
- 학습성(Learnability)
- 유연성(Flexibility)

PDF에 유연성이 누락되어 있더라도 표준적인 UI 기본 원칙에 포함하여 설명해주세요.

## 와이어프레임·목업·프로토타입

- 와이어프레임(Wireframe)
- 목업(Mockup)
- 스토리보드(Storyboard)
- 프로토타입(Prototype)

비교표:

| 도구 | 특징 |
|---|---|
| 와이어프레임(Wireframe) | 화면의 기본 구조와 배치를 표현 |
| 목업(Mockup) | 실제 화면과 유사하지만 정적인 모형 |
| 스토리보드(Storyboard) | 화면 설명과 이동 흐름을 문서화 |
| 프로토타입(Prototype) | 실제 상호작용을 일부 구현한 모형 |

## 소프트웨어 품질

- ISO/IEC 9126
- ISO/IEC 25000
- SQuaRE
- 기능성(Functionality)
- 신뢰성(Reliability)
- 사용성(Usability)
- 효율성(Efficiency)
- 유지보수성(Maintainability)
- 이식성(Portability)

ISO/IEC 9126의 6가지 품질 특성을 모두 포함해주세요.

> 기능성 → 신뢰성 → 사용성 → 효율성 → 유지보수성 → 이식성

## 모듈화와 추상화

- 모듈(Module)
- 모듈화(Modularity)
- 과정 추상화(Procedural Abstraction)
- 데이터 추상화(Data Abstraction)
- 제어 추상화(Control Abstraction)
- 하향식 설계(Top-Down Design)
- 단계적 분해(Stepwise Refinement)
- 정보 은닉(Information Hiding)

모듈을 지나치게 작게 또는 크게 나눴을 때의 문제도 설명해주세요.

## 소프트웨어 아키텍처 패턴

- 파이프-필터 패턴(Pipe-Filter Pattern)
- 모델-뷰-컨트롤러(MVC)

MVC 구성:

| 구성 요소 | 역할 |
|---|---|
| 모델(Model) | 핵심 데이터와 비즈니스 로직 관리 |
| 뷰(View) | 사용자에게 정보를 표시 |
| 컨트롤러(Controller) | 사용자 입력을 받아 모델과 뷰를 제어 |

## 객체지향 기본 개념

- 객체(Object)
- 클래스(Class)
- 인스턴스(Instance)
- 메시지(Message)
- 캡슐화(Encapsulation)
- 정보 은닉(Information Hiding)
- 상속(Inheritance)
- 다형성(Polymorphism)
- 오버로딩(Overloading)
- 오버라이딩(Overriding)

오버로딩과 오버라이딩 비교:

| 구분 | 의미 |
|---|---|
| 오버로딩(Overloading) | 같은 이름의 메소드를 매개변수 형식이나 개수를 다르게 정의 |
| 오버라이딩(Overriding) | 부모 클래스의 메소드를 자식 클래스에서 재정의 |

## 럼바우 분석 기법

- 객체 모델링(Object Modeling)
- 동적 모델링(Dynamic Modeling)
- 기능 모델링(Functional Modeling)

암기법:

> **객동기**

| 모델링 | 주요 표현 도구 |
|---|---|
| 객체 모델링(Object Modeling) | 객체 다이어그램 |
| 동적 모델링(Dynamic Modeling) | 상태 다이어그램 |
| 기능 모델링(Functional Modeling) | 자료 흐름도 |

## SOLID 원칙

- 단일 책임 원칙(SRP)
- 개방-폐쇄 원칙(OCP)
- 리스코프 치환 원칙(LSP)
- 인터페이스 분리 원칙(ISP)
- 의존 역전 원칙(DIP)

각 원칙을 한두 문장으로 쉽게 설명해주세요.

## 결합도와 응집도

**핵심:** 좋은 모듈은 결합도가 낮고 응집도가 높습니다.

결합도 약함 → 강함:

> 자료 → 스탬프 → 제어 → 외부 → 공통 → 내용

| 결합도 | 의미 |
|---|---|
| 자료 결합도(Data Coupling) | 단순한 데이터만 전달 |
| 스탬프 결합도(Stamp Coupling) | 배열·레코드 등 자료구조 전달 |
| 제어 결합도(Control Coupling) | 제어 정보나 플래그 전달 |
| 외부 결합도(External Coupling) | 외부 형식·장치·프로토콜 등에 공동 의존 |
| 공통 결합도(Common Coupling) | 전역 자료를 여러 모듈이 공유 |
| 내용 결합도(Content Coupling) | 다른 모듈의 내부를 직접 참조·수정 |

응집도 약함 → 강함:

> 우연적 → 논리적 → 시간적 → 절차적 → 통신적 → 순차적 → 기능적

PDF의 `교환적 응집도`는 일반적으로 `통신적 응집도(Communicational Cohesion)`라고 표현한다는 점을 반영해주세요.

## NS 차트

- 나시-슈나이더만 차트(NS Chart)
- 순차
- 선택
- 반복
- GOTO와 화살표를 사용하지 않음
- 구조적 프로그램 표현

## 디자인 패턴

디자인 패턴을 다음 세 종류로 분류해주세요.

- 생성 패턴(Creational Pattern)
- 구조 패턴(Structural Pattern)
- 행위 패턴(Behavioral Pattern)

### 생성 패턴

- 추상 팩토리(Abstract Factory)
- 빌더(Builder)
- 팩토리 메소드(Factory Method)
- 프로토타입(Prototype)
- 싱글톤(Singleton)

싱글톤 설명은 정확하게 작성해주세요.

> 싱글톤(Singleton)은 클래스의 인스턴스를 하나만 생성하고 전역적으로 접근할 수 있도록 하는 패턴이다.

원문의 “여러 프로세스가 동시에 참조할 수 없다”는 설명은 사용하지 마세요.

### 구조 패턴

- 어댑터(Adapter)
- 브리지(Bridge)
- 컴포지트(Composite)
- 데코레이터(Decorator)
- 퍼사드(Facade)
- 플라이웨이트(Flyweight)
- 프록시(Proxy)

### 행위 패턴

- 책임 연쇄(Chain of Responsibility)
- 커맨드(Command)
- 인터프리터(Interpreter)
- 반복자(Iterator)
- 중재자(Mediator)
- 메멘토(Memento)
- 옵서버(Observer)
- 상태(State)
- 전략(Strategy)
- 템플릿 메소드(Template Method)
- 방문자(Visitor)

각 패턴은 한 문장으로 핵심만 설명해주세요.

## 요구사항 검증 방법

- 동료 검토(Peer Review)
- 워크스루(Walkthrough)
- 인스펙션(Inspection)

비교:

| 검토 방식 | 특징 |
|---|---|
| 동료 검토(Peer Review) | 작성자가 설명하며 동료와 결함 확인 |
| 워크스루(Walkthrough) | 자료를 미리 배포한 뒤 검토 회의 수행 |
| 인스펙션(Inspection) | 작성자를 제외한 검토 전문가가 공식적으로 검토 |

## 미들웨어

- 미들웨어(Middleware)
- 서로 다른 시스템과 응용 프로그램 연결
- 운영체제와 응용 프로그램 사이에서 서비스 제공
- 위치 투명성(Location Transparency)

종류:

| 미들웨어 | 의미 |
|---|---|
| 데이터베이스 미들웨어(DB Middleware) | 데이터베이스 연결 지원 |
| 원격 프로시저 호출(RPC) | 원격 함수를 로컬 함수처럼 호출 |
| 메시지 지향 미들웨어(MOM) | 비동기 메시지 전달 |
| 트랜잭션 처리 모니터(TP-Monitor) | 트랜잭션 처리와 감시 |
| 객체 요청 브로커(ORB) | 분산 객체 간 통신 지원 |
| 웹 애플리케이션 서버(WAS) | 동적 웹 콘텐츠와 비즈니스 로직 처리 |

────────────────────
6. 시험 직전 필수 암기표
────────────────────

문서 마지막에는 다음 제목을 작성해주세요.

# 시험 직전 필수 암기표

2열 표로 다음 핵심 내용을 포함해주세요.

| 주제 | 핵심 내용 |
|---|---|
| 폭포수 모형(Waterfall Model) | 단계별 순차 진행, 변경 대응이 어려움 |
| 프로토타입 모형(Prototyping Model) | 견본을 통해 요구사항 확인 |
| 나선형 모형(Spiral Model) | 위험 분석 중심의 반복 개발 |
| 애자일(Agile) | 협업과 변화 대응 중시 |
| 요구사항 개발 순서 | 도출 → 분석 → 명세 → 확인 |
| 리팩토링(Refactoring) | 기능 변경 없이 내부 구조 개선 |
| DFD 프로세스 | 데이터를 처리하여 변환 |
| HIPO | 하향식 문서화 도구 |
| UML | 객체지향 분석·설계 모델링 언어 |
| 구조적 다이어그램 | 정적 구조 표현 |
| 행위 다이어그램 | 동적 행위 표현 |
| 액터(Actor) | 시스템과 상호작용하는 외부 요소 |
| CLI | 텍스트 명령 방식 |
| GUI | 그래픽 조작 방식 |
| NUI | 말과 행동으로 조작 |
| UI 기본 원칙 | 직관성·유효성·학습성·유연성 |
| 목업(Mockup) | 실제 화면과 유사한 정적 모형 |
| SQuaRE | ISO/IEC 25000 품질 평가 체계 |
| 정보 은닉 | 모듈 내부 정보의 외부 접근 제한 |
| 캡슐화 | 데이터와 기능을 하나로 묶음 |
| 상속 | 부모 클래스의 속성과 기능을 물려받음 |
| 오버로딩 | 매개변수를 다르게 하여 같은 이름 사용 |
| 오버라이딩 | 부모 메소드를 자식이 재정의 |
| 럼바우 | 객체·동적·기능 모델링 |
| SOLID | 객체지향 설계 5원칙 |
| 좋은 모듈 | 낮은 결합도·높은 응집도 |
| 가장 약한 결합도 | 자료 결합도 |
| 가장 강한 결합도 | 내용 결합도 |
| 가장 약한 응집도 | 우연적 응집도 |
| 가장 강한 응집도 | 기능적 응집도 |
| 싱글톤(Singleton) | 인스턴스를 하나만 생성 |
| 어댑터(Adapter) | 호환되지 않는 인터페이스 변환 |
| 옵서버(Observer) | 상태 변경을 관련 객체에 통보 |
| 전략(Strategy) | 알고리즘을 교환할 수 있게 구성 |
| 인스펙션(Inspection) | 공식적인 검토 방법 |
| RPC | 원격 프로시저를 로컬처럼 호출 |
| MOM | 비동기 메시지 전달 |
| WAS | 동적 웹 콘텐츠와 비즈니스 로직 처리 |

예상문제, 연습문제, 퀴즈, 정답은 절대로 추가하지 마세요.

────────────────────
7. 정확성 및 Markdown 검사
────────────────────

다음 사항을 검토해주세요.

1. 첫 페이지의 생명주기 모형 내용이 빠지지 않았는가
2. 2과목 자료구조 내용이 섞이지 않았는가
3. 한글과 영어가 같은 셀에 표시되었는가
4. UML 구조적·행위 다이어그램이 정확히 구분되었는가
5. UI 기본 원칙에 유연성이 포함되었는가
6. ISO/IEC 9126의 품질 특성이 정확한가
7. 오버로딩과 오버라이딩이 정확히 구분되었는가
8. SOLID 원칙이 정확한가
9. 결합도와 응집도 순서가 정확한가
10. 싱글톤 설명이 정확하게 수정되었는가
11. 디자인 패턴 분류가 정확한가
12. 예상문제 부분이 없는가
13. 표의 각 행에서 열 개수가 일치하는가
14. 한글이 UTF-8로 저장되었는가
15. 실제 `.md` 파일이 생성되었는가

Markdown 작성 규칙:

- 표준 Markdown 문법만 사용
- 제목은 `#`, `##`, `###` 형식
- 표는 파이프 기반 표준 Markdown 표
- HTML과 CSS 사용 금지
- Mermaid 사용 금지
- 외부 이미지와 URL 임의 추가 금지
- 과도한 이모지 사용 금지
- UTF-8로 저장
- 표가 지나치게 넓어지지 않도록 작성
- 코드 블록 시작과 종료가 정확히 대응하도록 확인

검토 과정에서 문제가 발견되면 수정한 뒤 최종 파일을 제공해주세요.
-----------------------------
첨부한 정보처리기사 필기 핵심요약 PDF를 분석하여, 2과목의 시험 대비 정리본을 Markdown(.md) 파일로 작성해주세요.

이번 작업 대상:
- 2과목: 소프트웨어 개발

중요:
- 단순히 채팅창에 내용만 출력하지 말고 실제 `.md` 파일을 생성해주세요.
- 파일명은 다음과 같이 저장해주세요.
  `정보처리기사_2과목_소프트웨어개발_핵심정리.md`
- PDF의 `2과목 소프트웨어 개발` 제목부터 3과목 내용이 시작되기 직전까지를 2과목 범위로 판단해주세요.
- PDF 16페이지에서는 2과목 내용인 Tripwire, 인터페이스 구현 검증 도구, IPSec 이후에 3과목의 논리적 설계가 시작됩니다.
- 따라서 `IPSec`까지는 2과목에 포함하고, `논리적 설계`, `물리적 설계`, 관계의 형태, 데이터 모델부터는 포함하지 마세요.
- PDF 내용을 단순 복사하지 말고 시험 대비용으로 재구성해주세요.
- 원문의 오탈자와 명백한 기술적 오류는 수정해주세요.
- 작업이 끝나면 생성된 Markdown 파일의 정확한 경로를 알려주세요.

────────────────────
1. 문서 목적
────────────────────

이 문서는 정보처리기사 필기시험 직전 복습에 사용하는 학습 노트입니다.

작성 기준:

- 대학생 초보자가 이해할 수 있는 수준
- 핵심 개념과 문제 풀이 방법 중심
- 자료구조와 정렬은 동작 과정을 단계적으로 설명
- 수식 표기법은 변환 원리와 스택 사용 방법을 쉽게 설명
- 테스트 관련 개념은 비교표로 명확히 구분
- 도구 이름은 용도와 함께 정리
- 예상문제, 연습문제, 퀴즈, 정답 부분은 작성하지 않음

────────────────────
2. 문서 제목과 전체 흐름
────────────────────

문서 제목:

# 정보처리기사 필기 2과목 — 소프트웨어 개발 핵심 정리

문서 시작 부분에 전체 학습 흐름을 작성해주세요.

예시:

> 자료구조 → 트리와 그래프 → 수식 표기법 → 정렬과 검색 → 해싱 → 데이터베이스 스키마 → 패키징과 형상 관리 → 소프트웨어 테스트 → 코드 품질 → 인터페이스 구현

권장 구조:

# 1. 자료구조의 분류

# 2. 스택과 큐

# 3. 그래프

# 4. 트리

# 5. 이진 트리 순회

# 6. 수식 표기법

# 7. 정렬 알고리즘

# 8. 검색 알고리즘

# 9. 해싱

# 10. 데이터베이스 스키마 3계층

# 11. 빌드 자동화

# 12. 소프트웨어 패키징

# 13. 디지털 저작권 관리

# 14. 설치 매뉴얼

# 15. 형상 관리와 버전 관리

# 16. 소프트웨어 테스트의 기본 원칙

# 17. 화이트박스와 블랙박스 테스트

# 18. V-모델과 테스트 단계

# 19. 통합 테스트

# 20. 테스트 드라이버와 스텁

# 21. 테스트 오라클

# 22. 시간 복잡도

# 23. 순환 복잡도

# 24. 클린 코드와 정적 분석

# 25. EAI

# 26. JSON과 AJAX

# 27. 인터페이스 보안과 검증 도구

# 시험 직전 필수 암기표

────────────────────
3. 영어 용어 표기 규칙
────────────────────

모든 주요 전문 용어는 다음 형식으로 작성해주세요.

`한글 용어(English Term)`

예시:

- 스택(Stack)
- 후입선출(LIFO)
- 이진 트리(Binary Tree)
- 전위 순회(Preorder Traversal)
- 삽입 정렬(Insertion Sort)
- 이진 검색(Binary Search)
- 형상 관리(Configuration Management)
- 화이트박스 테스트(White-Box Testing)
- 테스트 드라이버(Test Driver)
- 순환 복잡도(Cyclomatic Complexity)

영어를 별도 열로 분리하지 마세요.

올바른 형식:

| 개념 | 의미 |
|---|---|
| 스택(Stack) | 후입선출 방식으로 자료를 처리하는 선형 자료구조 |

────────────────────
4. 반드시 포함할 내용
────────────────────

## 자료구조의 분류

선형 구조(Linear Structure):

- 배열(Array)
- 선형 리스트(Linear List)
- 연결 리스트(Linked List)
- 스택(Stack)
- 큐(Queue)
- 데크(Deque)

비선형 구조(Non-Linear Structure):

- 트리(Tree)
- 그래프(Graph)

비교표:

| 구분 | 자료구조 |
|---|---|
| 선형 구조(Linear Structure) | 배열·리스트·스택·큐·데크 |
| 비선형 구조(Non-Linear Structure) | 트리·그래프 |

## 스택과 큐

- 스택(Stack)
- 후입선출(LIFO)
- 푸시(Push)
- 팝(Pop)
- 큐(Queue)
- 선입선출(FIFO)
- 프런트(Front)
- 리어(Rear)
- 데크(Deque)

스택의 응용:

- 함수 호출과 복귀 주소
- 인터럽트 처리
- 괄호 검사
- 수식 변환과 계산
- 깊이 우선 탐색

스택과 큐 비교:

| 구분 | 스택(Stack) | 큐(Queue) |
|---|---|---|
| 처리 방식 | 후입선출(LIFO) | 선입선출(FIFO) |
| 삽입·삭제 위치 | 한쪽 끝 | 서로 다른 양쪽 끝 |
| 대표 연산 | Push, Pop | Enqueue, Dequeue |

## 그래프

- 정점(Vertex)
- 간선(Edge)
- 방향 그래프(Directed Graph)
- 무방향 그래프(Undirected Graph)
- 차수(Degree)
- 경로(Path)
- 사이클(Cycle)

최대 간선 수 공식:

```text
무방향 그래프 최대 간선 수 = n(n - 1) / 2
```

```text
방향 그래프 최대 간선 수 = n(n - 1)
```

단, 자기 자신으로 향하는 간선이 없다는 조건을 설명해주세요.

## 트리

- 트리(Tree)
- 노드(Node)
- 가지(Branch)
- 루트 노드(Root Node)
- 부모 노드(Parent Node)
- 자식 노드(Child Node)
- 형제 노드(Sibling Node)
- 단말 노드(Leaf Node)
- 차수(Degree)
- 레벨(Level)
- 깊이(Depth)

## 이진 트리 순회

- 전위 순회(Preorder Traversal): Root → Left → Right
- 중위 순회(Inorder Traversal): Left → Root → Right
- 후위 순회(Postorder Traversal): Left → Right → Root

암기법:

> 전위: 루왼오  
> 중위: 왼루오  
> 후위: 왼오루

PDF의 트리 예제를 순회 단계별로 설명해주세요.

## 수식 표기법

- 중위 표기법(Infix Notation)
- 전위 표기법(Prefix Notation)
- 후위 표기법(Postfix Notation)
- 연산자(Operator)
- 피연산자(Operand)

다음 변환을 설명해주세요.

- 중위 → 후위
- 중위 → 전위
- 후위 → 중위
- 전위 → 중위
- 후위식 계산
- 전위식 계산

PDF 예제:

```text
X = A / B * (C + D) + E
```

후위 표기:

```text
X A B / C D + * E + =
```

전위 표기:

```text
= X + * / A B + C D E
```

단순히 결과만 쓰지 말고 연산자의 피연산자 두 개를 한 묶음으로 처리하는 원리를 설명해주세요.

## 정렬 알고리즘

- 삽입 정렬(Insertion Sort)
- 선택 정렬(Selection Sort)
- 버블 정렬(Bubble Sort)
- 퀵 정렬(Quick Sort)
- 힙 정렬(Heap Sort)
- 합병 정렬(Merge Sort)

각 알고리즘은 다음 내용을 설명해주세요.

- 정렬 원리
- 한 회전에서 수행하는 작업
- 평균·최악 시간 복잡도
- 안정 정렬 여부
- 제자리 정렬 여부
- 시험에서 구분하는 핵심

비교표 예시:

| 정렬 | 평균 시간 복잡도 | 최악 시간 복잡도 | 핵심 |
|---|---:|---:|---|
| 삽입 정렬(Insertion Sort) | O(n²) | O(n²) | 정렬된 부분에 새 값을 삽입 |
| 선택 정렬(Selection Sort) | O(n²) | O(n²) | 최솟값을 찾아 앞쪽과 교환 |
| 버블 정렬(Bubble Sort) | O(n²) | O(n²) | 인접한 두 값을 비교·교환 |
| 퀵 정렬(Quick Sort) | O(n log n) | O(n²) | 피벗을 기준으로 분할 |
| 힙 정렬(Heap Sort) | O(n log n) | O(n log n) | 완전 이진 트리인 힙 사용 |
| 합병 정렬(Merge Sort) | O(n log n) | O(n log n) | 분할 후 정렬된 부분을 병합 |

PDF의 `8, 5, 6, 2, 4` 예제를 삽입·선택·버블 정렬로 단계별 정리해주세요.

## 이진 검색

- 이진 검색(Binary Search)
- 정렬된 데이터에서만 사용 가능
- 중간값과 비교하여 검색 범위를 절반씩 축소

```text
중간 위치 = (첫 위치 + 마지막 위치) / 2
```

시간 복잡도:

```text
O(log n)
```

## 해싱

- 해싱(Hashing)
- 해시 함수(Hash Function)
- 해시 테이블(Hash Table)
- 홈 주소(Home Address)
- 충돌(Collision)
- 동의어(Synonym)
- 체이닝(Chaining)

주요 해싱 함수:

- 제산법(Division Method)
- 제곱법(Mid-Square Method)
- 폴딩법(Folding Method)
- 숫자 분석법(Digit Analysis Method)

각 방식의 홈 주소 산출 원리를 한 문장으로 설명해주세요.

## 스키마 3계층

- 외부 스키마(External Schema)
- 개념 스키마(Conceptual Schema)
- 내부 스키마(Internal Schema)

비교:

| 스키마 | 관점 |
|---|---|
| 외부 스키마(External Schema) | 개별 사용자와 응용 프로그램 관점 |
| 개념 스키마(Conceptual Schema) | 데이터베이스 전체의 논리적 구조 |
| 내부 스키마(Internal Schema) | 물리적 저장 구조 |

암기:

> 외부는 사용자, 개념은 전체, 내부는 저장장치

## 빌드 자동화 도구

- Ant
- Maven
- Gradle
- Jenkins

다음 차이를 정확히 작성해주세요.

- Ant·Maven·Gradle: 빌드 도구
- Jenkins: 지속적 통합과 자동화를 지원하는 자동화 서버

## 소프트웨어 패키징

- 소프트웨어 패키징(Software Packaging)
- 모듈별 실행 파일을 배포용 설치 파일로 구성
- 개발자보다 사용자 중심으로 작성
- 설치·배포·삭제를 고려

## DRM

- 디지털 저작권 관리(DRM)
- 클리어링 하우스(Clearing House)
- 콘텐츠 제공자(Content Provider)
- 패키저(Packager)
- 콘텐츠 분배자(Content Distributor)
- DRM 컨트롤러(DRM Controller)

기술 요소:

- 콘텐츠 암호화와 키 관리
- 콘텐츠 식별 체계
- 라이선스 발급과 관리
- 정책 관리
- 크랙 방지

## 설치 매뉴얼

- 사용자 중심 작성
- 소프트웨어 개요
- 설치 파일
- 설치 환경
- 설치 과정
- 삭제 방법
- 오류 발생 시 해결 방법

## 형상 관리

- 소프트웨어 형상 관리(SCM)
- 형상 항목(Configuration Item)
- 버전 관리(Version Control)
- 변경 관리(Change Control)
- 형상 감사(Configuration Audit)
- 형상 기록(Status Accounting)

도구:

- Git
- CVS
- Subversion(SVN)
- RCS
- BitKeeper

주요 기능:

- 체크아웃(Check-Out)
- 체크인(Check-In)
- 커밋(Commit)
- 병합(Merge)
- 충돌(Conflict)
- 차이 비교(Diff)

체크인과 커밋은 도구마다 의미가 다를 수 있으므로 지나치게 단정하지 말고, 저장소에 변경 내용을 반영하는 작업이라는 시험 범위 중심으로 설명해주세요.

## 테스트 기본 원칙

- 파레토 법칙(Pareto Principle)
- 살충제 패러독스(Pesticide Paradox)
- 오류-부재의 궤변(Absence-of-Errors Fallacy)
- 완벽한 테스트 불가능
- 결함 집중(Defect Clustering)
- 테스트의 조기 시작

PDF에 직접 나온 내용뿐 아니라 정보처리기사에서 자주 다루는 테스트 기본 원칙을 시험 범위 수준으로 간단히 포함해주세요.

## 화이트박스와 블랙박스 테스트

화이트박스 테스트(White-Box Testing):

- 내부 논리와 경로 검사
- 기초 경로 검사(Basis Path Testing)
- 조건 검사(Condition Testing)
- 루프 검사(Loop Testing)
- 데이터 흐름 검사(Data Flow Testing)

블랙박스 테스트(Black-Box Testing):

- 동치 분할 검사(Equivalence Partitioning)
- 경계값 분석(Boundary Value Analysis)
- 원인-효과 그래프(Cause-Effect Graphing)
- 오류 예측(Error Guessing)
- 비교 검사(Comparison Testing)

비교표:

| 구분 | 화이트박스 테스트 | 블랙박스 테스트 |
|---|---|---|
| 기준 | 내부 코드와 논리 구조 | 요구사항과 입출력 기능 |
| 코드 지식 | 필요 | 필수 아님 |
| 대표 기법 | 기초 경로·조건·루프 검사 | 동치 분할·경계값 분석 |

## V-모델과 테스트 단계

- 요구사항 분석 ↔ 인수 테스트
- 시스템 설계 ↔ 시스템 테스트
- 아키텍처 설계 ↔ 통합 테스트
- 모듈 설계·구현 ↔ 단위 테스트

단위 테스트(Unit Test):

- 모듈 또는 컴포넌트 단위
- 알고리즘 오류
- 잘못된 수식
- 반복문 종료 조건 오류

## 인수 테스트

- 알파 테스트(Alpha Test)
- 베타 테스트(Beta Test)

정확한 비교:

| 테스트 | 수행 환경 |
|---|---|
| 알파 테스트(Alpha Test) | 개발자 환경에서 사용자와 개발자가 함께 수행 |
| 베타 테스트(Beta Test) | 실제 사용자 환경에서 제한된 사용자들이 수행 |

원문의 “여러 명의 사용자 앞에서 수행”이라는 표현은 사용하지 마세요.

## 통합 테스트

- 하향식 통합 테스트(Top-Down Integration Testing)
- 상향식 통합 테스트(Bottom-Up Integration Testing)
- 빅뱅 통합 테스트(Big-Bang Integration Testing)
- 깊이 우선 통합
- 너비 우선 통합

## 테스트 드라이버와 스텁

다음 내용을 정확하게 작성해주세요.

| 도구 | 역할 | 사용 방식 |
|---|---|---|
| 테스트 드라이버(Test Driver) | 상위 모듈을 대신하여 테스트 대상 하위 모듈을 호출 | 상향식 통합 테스트 |
| 테스트 스텁(Test Stub) | 하위 모듈을 대신하여 호출 결과를 반환 | 하향식 통합 테스트 |

**핵심:** 드라이버는 호출하는 상위 모듈 대역, 스텁은 호출되는 하위 모듈 대역입니다.

## 테스트 오라클

- 테스트 오라클(Test Oracle)
- 참 오라클(True Oracle)
- 샘플링 오라클(Sampling Oracle)
- 휴리스틱 오라클(Heuristic Oracle)
- 일관성 검사 오라클(Consistent Oracle)

PDF의 `추정 오라클`은 일반적으로 휴리스틱 오라클(Heuristic Oracle)로도 표현된다는 점을 함께 표시해주세요.

## 시간 복잡도

- O(1)
- O(log n)
- O(n)
- O(n log n)
- O(n²)
- O(2ⁿ)
- O(n!)

빠름에서 느림 순서:

> O(1) → O(log n) → O(n) → O(n log n) → O(n²) → O(2ⁿ) → O(n!)

## 순환 복잡도

- 맥케이브 순환 복잡도(McCabe Cyclomatic Complexity)
- 독립적인 실행 경로 수
- 제어 흐름 그래프(Control Flow Graph)

계산 방법:

```text
V(G) = E - N + 2
```

- E: 간선 수
- N: 노드 수

또는:

```text
순환 복잡도 = 조건 분기 수 + 1
```

또는 평면 그래프에서는 영역 수로 계산할 수 있음을 설명해주세요.

## 클린 코드

- 가독성(Readability)
- 단순성(Simplicity)
- 의존성 최소화
- 중복 최소화
- 추상화(Abstraction)

외계인 코드(Alien Code):

- 오래된 코드
- 문서나 개발자가 없어 이해하기 어려운 코드
- 유지보수가 어려움

## 정적 분석 도구

- 정적 분석(Static Analysis)
- 프로그램을 실행하지 않고 소스코드 분석
- PMD
- Checkstyle
- Cppcheck

원문의 “하드웨어 또는 소프트웨어적인 방법”이라는 표현 대신 실행하지 않고 코드의 결함·규칙 위반 등을 분석한다는 핵심으로 작성해주세요.

## EAI

- 기업 애플리케이션 통합(EAI)
- 지점 간 연결(Point-to-Point)
- 허브 앤드 스포크(Hub and Spoke)
- 메시지 버스(Message Bus)
- 하이브리드(Hybrid)

정확한 비교:

| 유형 | 의미 |
|---|---|
| 지점 간 연결(Point-to-Point) | 애플리케이션을 1:1로 직접 연결 |
| 허브 앤드 스포크(Hub and Spoke) | 중앙 허브를 통해 데이터 교환 |
| 메시지 버스(Message Bus) | 공통 미들웨어 버스를 통해 애플리케이션 연결 |
| 하이브리드(Hybrid) | 허브 앤드 스포크와 메시지 버스를 결합 |

**주의:** 하이브리드는 Point-to-Point와 Hub and Spoke의 조합이 아니라, 일반적으로 Hub and Spoke와 Message Bus 방식의 결합으로 정리해주세요.

## JSON과 AJAX

JSON(JavaScript Object Notation):

- 속성-값 쌍
- 텍스트 기반 데이터 교환 형식
- 사람이 읽기 쉬움
- 언어 독립적으로 사용 가능

AJAX(Asynchronous JavaScript and XML):

- 비동기 웹 통신
- 전체 페이지를 다시 불러오지 않고 서버와 데이터 교환
- 이름에 XML이 포함되어 있지만 JSON도 널리 사용한다는 점

## Tripwire

- 트립와이어(Tripwire)
- 파일 및 시스템 설정 무결성 검사
- 기준 정보와 현재 상태 비교
- 비인가 변경 탐지

## 인터페이스 구현 검증 도구

- xUnit
- STAF
- FitNesse
- NTAF
- Watir

각 도구의 주요 용도를 한 문장으로 설명해주세요.

## IPSec

- 인터넷 프로토콜 보안(IPSec)
- 네트워크 계층
- IP 패킷 인증
- 무결성
- 기밀성
- 전송 모드(Transport Mode)
- 터널 모드(Tunnel Mode)
- AH
- ESP

다음처럼 구분해주세요.

| 구성 | 주요 기능 |
|---|---|
| 인증 헤더(AH) | 인증과 무결성 제공, 암호화는 제공하지 않음 |
| 캡슐화 보안 페이로드(ESP) | 암호화와 선택적인 인증·무결성 제공 |

원문의 “양방향 암호 방식”이라는 표현은 그대로 사용하지 마세요.

────────────────────
5. 시험 직전 필수 암기표
────────────────────

문서 마지막에는 다음 제목을 작성해주세요.

# 시험 직전 필수 암기표

다음 내용을 2열 표로 정리해주세요.

| 주제 | 핵심 내용 |
|---|---|
| 선형 자료구조 | 배열·리스트·스택·큐·데크 |
| 비선형 자료구조 | 트리·그래프 |
| 스택(Stack) | 후입선출(LIFO) |
| 큐(Queue) | 선입선출(FIFO) |
| 무방향 그래프 최대 간선 | n(n-1)/2 |
| 방향 그래프 최대 간선 | n(n-1) |
| 전위 순회 | Root → Left → Right |
| 중위 순회 | Left → Root → Right |
| 후위 순회 | Left → Right → Root |
| 중위 표기 | 연산자가 피연산자 사이 |
| 전위 표기 | 연산자가 피연산자 앞 |
| 후위 표기 | 연산자가 피연산자 뒤 |
| 삽입 정렬 | 정렬된 부분에 값을 삽입 |
| 선택 정렬 | 최솟값을 찾아 앞쪽과 교환 |
| 버블 정렬 | 인접한 값을 비교·교환 |
| 퀵 정렬 | 피벗을 기준으로 분할 |
| 힙 정렬 | 완전 이진 트리 이용 |
| 이진 검색 | 정렬된 데이터에서 범위를 절반씩 축소 |
| 체이닝(Chaining) | 충돌 데이터를 연결 리스트에 저장 |
| 외부 스키마 | 사용자 관점 |
| 개념 스키마 | 데이터베이스 전체 논리 구조 |
| 내부 스키마 | 물리적 저장 구조 |
| Ant·Maven·Gradle | 빌드 도구 |
| Jenkins | 자동화·지속적 통합 서버 |
| DRM | 디지털 콘텐츠의 사용 권한 관리 |
| 체크아웃 | 저장소에서 작업 파일을 가져옴 |
| 체크인·커밋 | 변경 내용을 저장소에 반영 |
| 파레토 법칙 | 오류의 80%가 모듈 20%에 집중 |
| 화이트박스 테스트 | 내부 논리와 경로 검사 |
| 블랙박스 테스트 | 요구사항과 입출력 기능 검사 |
| 단위 테스트 | 모듈·컴포넌트 단위 |
| 알파 테스트 | 개발자 환경 |
| 베타 테스트 | 실제 사용자 환경 |
| 드라이버 | 상위 모듈 대역 |
| 스텁 | 하위 모듈 대역 |
| 순환 복잡도 | 독립적인 실행 경로 수 |
| 좋은 코드 | 가독성·단순성·낮은 의존성·낮은 중복성 |
| Point-to-Point | 애플리케이션 1:1 연결 |
| Hub and Spoke | 중앙 허브 방식 |
| Message Bus | 공통 미들웨어 버스 방식 |
| Hybrid | Hub and Spoke와 Message Bus 결합 |
| JSON | 속성-값 쌍의 텍스트 데이터 형식 |
| AJAX | 비동기 웹 통신 |
| Tripwire | 파일 무결성 검사 |
| AH | 인증·무결성 |
| ESP | 암호화와 인증·무결성 |

예상문제, 연습문제, 퀴즈, 정답 부분은 절대로 추가하지 마세요.

────────────────────
6. 정확성 및 Markdown 검사
────────────────────

다음 사항을 반드시 검토해주세요.

1. `2과목 소프트웨어 개발`부터 시작했는가
2. 1과목 미들웨어 내용이 섞이지 않았는가
3. IPSec까지만 포함하고 3과목 논리적 설계를 제외했는가
4. 한글과 영어가 같은 셀에 표시되었는가
5. 스택과 큐를 정확히 구분했는가
6. 그래프 최대 간선 공식이 정확한가
7. 트리 순회 순서가 정확한가
8. 수식 표기법 예제가 정확한가
9. 정렬 알고리즘의 과정과 복잡도가 정확한가
10. 이진 검색은 정렬된 자료에서 사용한다고 표시했는가
11. 테스트 드라이버와 스텁의 역할이 정확한가
12. 알파 테스트와 베타 테스트가 정확한가
13. 순환 복잡도 공식이 정확한가
14. EAI 하이브리드 설명이 정확한가
15. AJAX가 XML만 사용하는 것으로 잘못 설명되지 않았는가
16. IPSec의 AH와 ESP가 정확히 구분되었는가
17. 예상문제 부분이 없는가
18. Markdown 표의 열 개수가 일치하는가
19. UTF-8로 저장되었는가
20. 실제 `.md` 파일이 생성되었는가

Markdown 작성 규칙:

- 표준 Markdown 문법만 사용
- 제목은 `#`, `##`, `###`
- 표는 파이프 기반 Markdown 표
- 코드와 공식은 삼중 백틱 사용
- HTML·CSS·JavaScript 사용 금지
- Mermaid 사용 금지
- 외부 이미지와 링크 임의 추가 금지
- 가로로 지나치게 넓은 표 금지
- 코드 블록의 시작과 종료 확인
- UTF-8 인코딩으로 저장

검토 과정에서 오류가 발견되면 직접 수정한 뒤 최종 파일을 제공해주세요.
--------------------------------------------
````text
첨부한 정보처리기사 필기 핵심요약 PDF를 분석하여, 지정한 과목의 시험 대비 정리본을 Markdown(.md) 파일로 작성해주세요.

이번 작업 대상:
- 3과목: 데이터베이스 구축

중요:
- 단순히 채팅창에 내용만 출력하지 말고, 실제 `.md` 파일을 생성해주세요.
- 파일명은 다음과 같이 저장해주세요.
  `정보처리기사_3과목_데이터베이스구축_핵심정리.md`
- PDF에서 3과목이 시작되는 지점부터 4과목이 시작되기 직전까지를 3과목 범위로 판단해주세요.
- 첨부 자료를 중심으로 정리하고, 자료에 없는 내용을 임의로 과도하게 추가하지 마세요.
- 다만 오탈자나 명백한 오류가 발견되면 그대로 옮기지 말고, 올바른 내용을 적은 뒤 간단히 주의사항을 표시해주세요.
- 작업 완료 후 생성된 Markdown 파일의 다운로드 경로를 알려주세요.

────────────────────
1. 문서 목적
────────────────────

이 문서는 정보처리기사 필기시험 직전 복습에 사용하는 학습 노트입니다.

다음 기준으로 작성해주세요.

- 대학생 초보자가 이해할 수 있는 설명
- 시험에 필요한 핵심 개념 중심
- 원문을 단순 복사하지 말고 이해하기 쉽게 재구성
- 지나치게 짧은 단어 나열이 아니라 개념의 의미가 이해되도록 설명
- 중요 개념은 비교표, 예시, 암기법을 활용
- 비슷하거나 혼동하기 쉬운 개념을 명확히 구분
- 예상문제 또는 연습문제 부분은 작성하지 않음

────────────────────
2. 전체 문서 구조
────────────────────

다음과 같은 Markdown 구조를 사용해주세요.

# 정보처리기사 필기 3과목 — 데이터베이스 구축 핵심 정리

문서 시작 부분에 3과목의 전체 학습 흐름을 한 줄로 작성해주세요.

예시:

> 데이터 모델링 → 관계형 데이터베이스 → 키와 무결성 → 관계대수 → 정규화 → 트랜잭션 → 데이터베이스 구조 → SQL

이후 각 개념을 다음처럼 번호가 있는 제목으로 구성해주세요.

# 1. 데이터베이스 설계
## 1-1. 논리적 설계(Logical Design)
## 1-2. 물리적 설계(Physical Design)

# 2. 데이터 모델(Data Model)

# 3. 관계의 형태

# 4. E-R 다이어그램(Entity-Relationship Diagram)

# 5. 관계형 데이터베이스의 기본 용어

# 6. 릴레이션의 특징

# 7. 키(Key)

# 8. 데이터 무결성(Data Integrity)

# 9. 관계대수와 관계해석

# 10. 관계 연산자

# 11. 정규화(Normalization)

# 12. 이상 현상(Anomaly)

# 13. 함수적 종속(Functional Dependency)

# 14. 정규화 과정

# 15. 반정규화(Denormalization)

# 16. 시스템 카탈로그(System Catalog)

# 17. 트랜잭션(Transaction)

# 18. 트랜잭션의 ACID 특성

# 19. 인덱스(Index)

# 20. 뷰(View)

# 21. 파티션(Partition)

# 22. 분산 데이터베이스

# 23. 분할(Fragmentation)

# 24. 접근통제(Access Control)

# 25. 저장장치 구성

# 26. SQL 언어의 분류

# 27. 데이터 정의어와 테이블 정의

# 28. 데이터 조작문

# 29. SELECT문

# 30. 조건 연산자

# 31. 그룹 함수와 GROUP BY

# 32. 하위 질의(Subquery)

# 33. 집합 연산자

# 34. 트리거(Trigger)

# 시험 직전 필수 암기표

PDF의 실제 내용 흐름에 따라 세부 제목은 조정할 수 있지만, 누락되는 핵심 개념이 없도록 해주세요.

────────────────────
3. 한글과 영어 용어 표기 규칙
────────────────────

모든 주요 전문 용어는 처음 등장할 때 다음 형식으로 작성해주세요.

`한글 용어(English Term)`

예시:

- 논리적 설계(Logical Design)
- 물리적 설계(Physical Design)
- 튜플(Tuple)
- 속성(Attribute)
- 후보키(Candidate Key)
- 참조 무결성(Referential Integrity)
- 함수적 종속(Functional Dependency)
- 반정규화(Denormalization)

표에서도 한글과 영어를 별도 열로 분리하지 마세요.

잘못된 형식:

| 특성 | 영어 | 의미 |
|---|---|---|
| 원자성 | Atomicity | 모두 실행되거나 모두 취소 |

올바른 형식:

| 특성 | 의미 |
|---|---|
| 원자성(Atomicity) | 모든 연산이 전부 실행되거나 전부 취소되어야 함 |

즉, 한글 용어와 영어 용어는 항상 같은 셀이나 같은 문장 안에 표시해주세요.

────────────────────
4. 개념 설명 형식
────────────────────

각 개념은 가능한 경우 다음 순서로 작성해주세요.

1. 개념 정의
2. 주요 특징
3. 시험에 자주 나오는 핵심
4. 간단한 예시
5. 유사 개념과 비교

예시:

## 후보키(Candidate Key)

튜플을 유일하게 식별할 수 있으면서 불필요한 속성이 없는 최소한의 키입니다.

- 유일성(Uniqueness)을 만족함
- 최소성(Minimality)을 만족함
- 기본키(Primary Key)로 선택될 수 있음

**핵심:** 후보키는 유일성과 최소성을 모두 만족합니다.

설명은 어렵고 추상적인 문장보다 쉬운 문장으로 작성해주세요.

────────────────────
5. 표 작성 규칙
────────────────────

비교가 필요한 내용은 Markdown 표를 적극적으로 사용해주세요.

예시:

| 용어 | 의미 |
|---|---|
| 튜플(Tuple) | 릴레이션의 행(Row) |
| 속성(Attribute) | 릴레이션의 열(Column) |
| 카디널리티(Cardinality) | 튜플의 수 |
| 디그리(Degree) | 속성의 수 |

표 작성 시 다음 규칙을 지켜주세요.

- 열을 지나치게 많이 만들지 않음
- 한글과 영어를 별도의 열로 분리하지 않음
- 셀 안의 설명은 지나치게 길지 않게 작성
- 비교 기준이 명확하도록 구성
- PDF 변환 시 깨지기 쉬운 복잡한 병합 표는 사용하지 않음
- HTML 표 문법은 사용하지 않고 표준 Markdown 표만 사용

────────────────────
6. 코드와 SQL 작성 규칙
────────────────────

SQL 명령문은 반드시 언어가 지정된 코드 블록으로 작성해주세요.

예시:

```sql
CREATE TABLE 학생 (
    학번 INT NOT NULL,
    이름 VARCHAR(20) NOT NULL,
    PRIMARY KEY (학번)
);
````

다음 SQL 항목을 이해하기 쉬운 예제와 함께 정리해주세요.

* CREATE TABLE
* ALTER TABLE
* DROP TABLE
* INSERT
* UPDATE
* DELETE
* SELECT
* DISTINCT
* WHERE
* GROUP BY
* HAVING
* ORDER BY
* LIKE
* BETWEEN
* 하위 질의(Subquery)
* UNION
* UNION ALL
* INTERSECT
* EXCEPT
* COMMIT
* ROLLBACK
* GRANT
* REVOKE

코드 블록 내부에서는 불필요한 특수문자나 장식 문자를 사용하지 마세요.

SQL의 작성 순서와 논리적 처리 순서도 구분하여 작성해주세요.

작성 순서:

```text
SELECT → FROM → WHERE → GROUP BY → HAVING → ORDER BY
```

논리적 처리 순서:

```text
FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY
```

────────────────────
7. 반드시 포함할 핵심 내용
────────────────────

다음 내용을 빠뜨리지 말고 포함해주세요.

## 데이터베이스 설계

* 논리적 설계(Logical Design)
* 물리적 설계(Physical Design)
* 논리적 설계와 물리적 설계 비교

## 데이터 모델

* 구조(Structure)
* 연산(Operation)
* 제약 조건(Constraint)
* 관계의 형태 1:1, 1:N, N:M

## E-R 다이어그램

* 개체(Entity)
* 관계(Relationship)
* 속성(Attribute)
* 다중값 속성(Multivalued Attribute)
* 각 기호의 의미

## 릴레이션

* 릴레이션(Relation)
* 튜플(Tuple)
* 속성(Attribute)
* 도메인(Domain)
* 카디널리티(Cardinality)
* 디그리(Degree)
* 릴레이션의 특징

## 키

* 슈퍼키(Super Key)
* 후보키(Candidate Key)
* 기본키(Primary Key)
* 대체키(Alternate Key)
* 외래키(Foreign Key)
* 유일성(Uniqueness)
* 최소성(Minimality)

## 무결성

* 개체 무결성(Entity Integrity)
* 참조 무결성(Referential Integrity)

## 관계대수와 관계해석

* 관계대수(Relational Algebra)
* 관계해석(Relational Calculus)
* 절차적 언어와 비절차적 언어 비교
* 셀렉트(Select)
* 프로젝트(Project)
* 조인(Join)
* 디비전(Division)
* 합집합(Union)
* 교집합(Intersection)
* 차집합(Difference)
* 교차곱(Cartesian Product)
* 교차곱의 디그리와 카디널리티 계산법

## 정규화

* 정규화(Normalization)
* 이상 현상(Anomaly)
* 삽입 이상(Insertion Anomaly)
* 삭제 이상(Deletion Anomaly)
* 갱신 이상(Update Anomaly)
* 함수적 종속(Functional Dependency)
* 이행적 함수 종속(Transitive Functional Dependency)
* 제1정규형(1NF)
* 제2정규형(2NF)
* 제3정규형(3NF)
* 보이스-코드 정규형(BCNF)
* 제4정규형(4NF)
* 제5정규형(5NF)
* 반정규화(Denormalization)

정규화 순서는 다음 내용을 기준으로 설명해주세요.

| 정규형              | 제거하거나 만족해야 하는 조건    |
| ---------------- | ------------------- |
| 제1정규형(1NF)       | 도메인을 원자값으로 구성       |
| 제2정규형(2NF)       | 부분 함수 종속 제거         |
| 제3정규형(3NF)       | 이행 함수 종속 제거         |
| 보이스-코드 정규형(BCNF) | 모든 결정자가 후보키가 되도록 구성 |
| 제4정규형(4NF)       | 다치 종속 제거            |
| 제5정규형(5NF)       | 조인 종속 제거            |

암기법 `원부이결다조`도 포함해주세요.

## 트랜잭션

* 트랜잭션(Transaction)
* 원자성(Atomicity)
* 일관성(Consistency)
* 독립성(Isolation)
* 영속성(Durability)
* 커밋(Commit)
* 롤백(Rollback)

ACID는 다음 형식처럼 작성해주세요.

| 특성               | 의미                             |
| ---------------- | ------------------------------ |
| 원자성(Atomicity)   | 모든 연산이 전부 실행되거나 전부 취소되어야 함     |
| 일관성(Consistency) | 트랜잭션 전후에 데이터베이스의 일관성이 유지되어야 함  |
| 독립성(Isolation)   | 동시에 실행되는 트랜잭션이 서로 간섭하지 않아야 함   |
| 영속성(Durability)  | 완료된 결과는 장애가 발생해도 영구적으로 보존되어야 함 |

## 데이터베이스 구조

* 시스템 카탈로그(System Catalog)
* 메타데이터(Metadata)
* 인덱스(Index)
* 뷰(View)
* 범위 분할(Range Partitioning)
* 해시 분할(Hash Partitioning)
* 목록 분할(List Partitioning)
* 조합 분할(Composite Partitioning)
* 라운드 로빈 분할(Round Robin Partitioning)

## 분산 데이터베이스

* 위치 투명성(Location Transparency)
* 중복 투명성(Replication Transparency)
* 병행 투명성(Concurrency Transparency)
* 장애 투명성(Failure Transparency)
* 수평 분할(Horizontal Fragmentation)
* 수직 분할(Vertical Fragmentation)

## 접근통제와 저장장치

* 임의 접근통제(DAC)
* 강제 접근통제(MAC)
* 역할기반 접근통제(RBAC)
* 직접 연결 저장장치(DAS)
* 저장 영역 네트워크(SAN)
* 각 방식의 기준과 사용 프로토콜

## SQL

* 데이터 정의어(DDL)
* 데이터 조작어(DML)
* 데이터 제어어(DCL)
* CREATE, ALTER, DROP
* SELECT, INSERT, UPDATE, DELETE
* COMMIT, ROLLBACK, GRANT, REVOKE
* 제약조건 PRIMARY KEY, FOREIGN KEY, REFERENCES, NOT NULL, UNIQUE, DEFAULT, CHECK
* CASCADE와 RESTRICT 비교
* WHERE와 HAVING 비교
* UNION과 UNION ALL 비교
* 그룹 함수 COUNT, SUM, AVG, MAX, MIN
* 트리거(Trigger)

────────────────────
8. 강조와 암기 표현
────────────────────

시험에 중요한 문장은 다음 형식으로 강조해주세요.

```markdown
**핵심:** 후보키(Candidate Key)는 유일성과 최소성을 모두 만족한다.
```

암기 순서가 있는 내용은 인용문 형식으로 표시해주세요.

```markdown
> 원자값 → 부분 함수 종속 제거 → 이행 함수 종속 제거 → 결정자 → 다치 종속 → 조인 종속
```

또는:

```markdown
> **원부이결다조**
```

다음과 같은 핵심 암기 표현을 포함해주세요.

* 데이터 모델: 구조·연산·제약 조건
* 후보키: 유일성+최소성
* 좋은 키 비교
* 셀렉트는 행, 프로젝트는 열
* 관계대수는 절차적, 관계해석은 비절차적
* 정규화: 원부이결다조
* ACID: 원일독영
* 분산 투명성: 위중병장
* DDL: CREATE·ALTER·DROP
* DML: SELECT·INSERT·UPDATE·DELETE
* DCL: COMMIT·ROLLBACK·GRANT·REVOKE

────────────────────
9. 시험 직전 암기표
────────────────────

문서 마지막에는 반드시 다음 제목을 추가해주세요.

# 시험 직전 필수 암기표

시험 직전 빠르게 복습할 수 있도록 핵심 내용을 2열 표로 정리해주세요.

형식:

| 주제                              | 핵심 내용                        |
| ------------------------------- | ---------------------------- |
| 데이터 모델 요소                       | 구조·연산·제약 조건                  |
| 튜플(Tuple)                       | 행                            |
| 속성(Attribute)                   | 열                            |
| 카디널리티(Cardinality)              | 튜플의 수                        |
| 디그리(Degree)                     | 속성의 수                        |
| 후보키(Candidate Key)              | 유일성+최소성                      |
| 슈퍼키(Super Key)                  | 유일성은 만족하지만 최소성은 필수 아님        |
| 개체 무결성(Entity Integrity)        | 기본키는 Null·중복 불가              |
| 참조 무결성(Referential Integrity)   | 외래키는 Null 또는 존재하는 기본키 값      |
| 셀렉트(Select)                     | 행 선택                         |
| 프로젝트(Project)                   | 열 선택                         |
| 관계대수(Relational Algebra)        | 절차적                          |
| 관계해석(Relational Calculus)       | 비절차적                         |
| 정규화 순서                          | 원부이결다조                       |
| ACID                            | 원자성·일관성·독립성·영속성              |
| 수평 분할(Horizontal Fragmentation) | 행 분할                         |
| 수직 분할(Vertical Fragmentation)   | 열 분할                         |
| 임의 접근통제(DAC)                    | 사용자 신원 또는 소유자 판단             |
| 강제 접근통제(MAC)                    | 보안 등급                        |
| 역할기반 접근통제(RBAC)                 | 역할                           |
| 데이터 정의어(DDL)                    | CREATE·ALTER·DROP            |
| 데이터 조작어(DML)                    | SELECT·INSERT·UPDATE·DELETE  |
| 데이터 제어어(DCL)                    | COMMIT·ROLLBACK·GRANT·REVOKE |
| WHERE                           | 개별 튜플 조건                     |
| HAVING                          | 그룹 조건                        |
| UNION                           | 중복 제거                        |
| UNION ALL                       | 중복 허용                        |

예상문제, 연습문제, 퀴즈, 정답 부분은 절대로 추가하지 마세요.

────────────────────
10. Markdown 호환성과 품질 검사
────────────────────

완성된 Markdown 파일이 일반적인 Markdown-to-PDF 변환기에서 깨지지 않도록 다음 규칙을 지켜주세요.

* 표준 Markdown 문법만 사용
* 제목은 `#`, `##`, `###` 형식 사용
* 표는 파이프(`|`) 기반 표준 Markdown 표 사용
* 코드 블록은 삼중 백틱과 언어명을 사용
* HTML, CSS, JavaScript를 사용하지 않음
* Mermaid 다이어그램을 사용하지 않음
* 이미지 링크나 외부 URL을 임의로 추가하지 않음
* 특수한 이모지와 장식 문자를 과도하게 사용하지 않음
* 같은 제목을 중복해서 만들지 않음
* 표의 각 행에서 열 개수가 일치하는지 확인
* 코드 블록의 시작과 종료 백틱이 정확히 대응하는지 확인
* 한글이 UTF-8로 정상 저장되도록 처리
* 파일 끝에 불필요한 빈 코드 블록을 만들지 않음

파일을 만든 후 다음 사항을 자체 검토해주세요.

1. 3과목 범위만 포함되었는가
2. 4과목 내용이 섞이지 않았는가
3. 주요 영어 용어가 한글 용어 옆 괄호 안에 들어갔는가
4. 영어 용어만 별도 열로 만든 표가 없는가
5. 예상문제 부분이 없는가
6. SQL 코드가 올바른 코드 블록에 들어갔는가
7. 정규화 순서와 ACID 내용이 정확한가
8. Markdown 문법 오류가 없는가
9. UTF-8 인코딩으로 저장되었는가
10. 실제 `.md` 파일이 생성되었는가
--------------------------------------
첨부한 정보처리기사 필기 핵심요약 PDF를 분석하여, 4과목의 시험 대비 정리본을 Markdown(.md) 파일로 작성해주세요.

이번 작업 대상:
- 4과목: 프로그래밍 언어 활용

중요:
- 단순히 채팅창에 내용만 출력하지 말고 실제 `.md` 파일을 생성해주세요.
- 파일명은 다음과 같이 저장해주세요.
  `정보처리기사_4과목_프로그래밍언어활용_핵심정리.md`
- PDF에서 `4과목 프로그래밍 언어 활용`이라는 제목이 나오는 지점부터 `5과목 정보시스템 구축 관리`가 시작되기 직전까지를 4과목 범위로 판단해주세요.
- 4과목 제목이 있는 페이지에 3과목 내용이 함께 존재할 수 있으므로, 반드시 `4과목 프로그래밍 언어 활용` 제목 이후의 내용부터 포함해주세요.
- 5과목 내용인 소프트웨어 재사용, 재공학, CASE 등은 포함하지 마세요.
- 첨부 자료를 중심으로 정리하고 자료에 없는 내용을 과도하게 추가하지 마세요.
- 원문의 오탈자나 명백한 기술적 오류는 그대로 복사하지 말고 올바르게 수정해주세요.
- 작업이 끝나면 생성된 Markdown 파일의 경로를 알려주세요.

────────────────────
1. 문서 목적
────────────────────

이 문서는 정보처리기사 필기시험 직전 복습에 사용하는 학습 노트입니다.

다음 기준으로 작성해주세요.

- 대학생 초보자가 이해할 수 있는 수준
- 시험에 필요한 핵심 개념 중심
- PDF의 문장을 단순 복사하지 말고 이해하기 쉽게 재구성
- 프로그래밍 코드는 실행 흐름과 출력 결과를 이해할 수 있도록 설명
- 비슷하거나 혼동하기 쉬운 개념은 비교표로 정리
- 계산 문제가 출제될 수 있는 개념은 공식과 풀이 방법을 간단히 설명
- 너무 짧은 단어 나열식 정리는 피함
- 예상문제, 연습문제, 퀴즈, 정답 부분은 작성하지 않음

────────────────────
2. 문서 제목과 전체 흐름
────────────────────

문서 제목은 다음과 같이 작성해주세요.

# 정보처리기사 필기 4과목 — 프로그래밍 언어 활용 핵심 정리

문서 시작 부분에는 4과목의 전체 학습 흐름을 한 줄로 작성해주세요.

예시:

> 프로그래밍 언어 기초 → 연산자와 제어문 → 배열과 포인터 → Python → 라이브러리와 예외 처리 → 운영체제 → 프로세스와 스케줄링 → 네트워크

그다음 PDF의 실제 내용 흐름을 따라 번호가 있는 제목으로 구성해주세요.

권장 구조:

# 1. 프로그래밍 언어의 자료형

# 2. 변수와 변수명 작성 규칙

# 3. 구조체와 가비지 컬렉터

# 4. 연산자의 종류

# 5. 연산자 우선순위와 결합 방향

# 6. 입출력 함수와 서식 문자열

# 7. 조건문

# 8. 반복문

# 9. 배열과 문자열

# 10. 포인터

# 11. Python의 입력과 출력

# 12. Python의 자료형

# 13. Python의 Range와 슬라이싱

# 14. Python의 반복문

# 15. Python의 클래스와 함수

# 16. 스크립트 언어

# 17. 라이브러리와 동적 메모리 할당

# 18. Java의 예외 처리

# 19. 기억장치 배치 전략

# 20. 페이지 교체 알고리즘

# 21. 스래싱

# 22. 프로세스와 스레드

# 23. CPU 스케줄링

# 24. UNIX 주요 명령어

# 25. IPv4와 IPv6

# 26. OSI 7계층

# 27. 네트워크 장비

# 28. TCP/IP 주요 프로토콜

# 29. CSMA/CD

# 시험 직전 필수 암기표

PDF 내용에 맞게 세부 제목을 조정할 수 있지만 핵심 항목을 빠뜨리지 마세요.

────────────────────
3. 한글과 영어 용어 표기 규칙
────────────────────

모든 주요 전문 용어는 처음 등장할 때 다음 형식으로 작성해주세요.

`한글 용어(English Term)`

예시:

- 가비지 컬렉터(Garbage Collector)
- 비트 연산자(Bitwise Operator)
- 조건 연산자(Conditional Operator)
- 포인터(Pointer)
- 슬라이싱(Slicing)
- 동적 메모리 할당(Dynamic Memory Allocation)
- 최초 적합(First Fit)
- 페이지 부재(Page Fault)
- 스래싱(Thrashing)
- 프로세스(Process)
- 스레드(Thread)
- 높은 응답률 우선 스케줄링(HRN)
- 데이터 링크 계층(Data Link Layer)
- 주소 결정 프로토콜(ARP)

표에서도 한글과 영어를 별도의 열로 나누지 마세요.

잘못된 형식:

| 개념 | 영어 | 의미 |
|---|---|---|
| 최초 적합 | First Fit | 첫 번째 공간에 배치 |

올바른 형식:

| 개념 | 의미 |
|---|---|
| 최초 적합(First Fit) | 배치 가능한 첫 번째 공간에 프로세스를 배치 |

즉, 한글 용어와 영어는 반드시 같은 셀 또는 같은 문장 안에 작성해주세요.

────────────────────
4. 개념 설명 형식
────────────────────

각 개념은 가능한 경우 다음 순서로 설명해주세요.

1. 정의
2. 주요 특징
3. 문법 또는 공식
4. 간단한 예시
5. 시험에서 구분해야 하는 핵심

예시:

## 전치 증가 연산자(Prefix Increment Operator)

변수의 값을 먼저 1 증가시킨 뒤 해당 값을 연산에 사용합니다.

```c
int a = 5;
int b = ++a;
```

실행 결과:

- `a = 6`
- `b = 6`

## 후치 증가 연산자(Postfix Increment Operator)

현재 값을 먼저 연산에 사용한 뒤 변수의 값을 1 증가시킵니다.

```c
int a = 5;
int b = a++;
```

실행 결과:

- `a = 6`
- `b = 5`

**핵심:** 전치는 증가 후 사용하고, 후치는 사용 후 증가합니다.

────────────────────
5. Markdown 표 작성 규칙
────────────────────

비교가 필요한 개념은 표준 Markdown 표로 정리해주세요.

예시:

| 개념 | 의미 |
|---|---|
| 프로세스(Process) | 실행 중인 프로그램 |
| 스레드(Thread) | 프로세스 내부의 실행 단위 |
| 프로그램(Program) | 실행되기 전의 명령어 집합 |

표 작성 시 다음 기준을 지켜주세요.

- 열은 가능하면 2~3개만 사용
- 한글과 영어를 별도 열로 분리하지 않음
- 셀 안의 설명을 지나치게 길게 작성하지 않음
- HTML 표를 사용하지 않음
- 셀 병합을 사용하지 않음
- 모든 행의 열 개수를 동일하게 유지
- PDF 변환 시 가로폭이 지나치게 넓어지지 않게 구성

────────────────────
6. 코드 작성 규칙
────────────────────

C, Java, Python, Shell 코드는 반드시 언어명이 지정된 코드 블록으로 작성해주세요.

C 코드:

```c
int a = 5;
printf("%d", a);
```

Java 코드:

```java
System.out.println("Hello");
```

Python 코드:

```python
a = int(input())
print(a)
```

Shell 코드:

```bash
chmod 755 file.txt
```

코드를 설명할 때 다음 내용을 지켜주세요.

- 실행 순서를 단계적으로 설명
- 변수값이 어떻게 변하는지 표시
- 출력 결과가 있으면 별도의 코드 블록이나 문장으로 표시
- 원문 코드에 문법 오류가 있으면 올바른 코드로 수정
- 스마트 따옴표가 아닌 일반 따옴표를 사용
- C 코드에는 필요한 경우 세미콜론을 정확히 작성
- Python 코드에는 들여쓰기를 정확히 적용
- 단순 개념 설명을 위해 불필요하게 긴 코드를 작성하지 않음

────────────────────
7. 반드시 포함할 핵심 내용
────────────────────

다음 내용을 빠뜨리지 말고 포함해주세요.

## 프로그래밍 언어의 기본 자료형

- 문자형(Character Type)
- 정수형(Integer Type)
- 논리형(Boolean Type)
- C와 Java의 자료형 차이
- 자료형별 크기는 언어와 구현 환경에 따라 달라질 수 있다는 주의사항
- 변수명 작성 규칙

## C언어의 구조체와 메모리

- 구조체(Structure)
- `struct` 예약어
- 가비지 컬렉터(Garbage Collector)
- C언어는 일반적으로 개발자가 메모리를 직접 관리하고 Java는 가비지 컬렉션을 지원한다는 차이
- PDF 내용과 실제 언어 특성이 충돌하면 실제 개념을 기준으로 정정

## 연산자

- 산술 연산자(Arithmetic Operator)
- 나머지 연산자(Modulus Operator)
- 증가 연산자(Increment Operator)
- 감소 연산자(Decrement Operator)
- 전치 연산(Prefix Operation)
- 후치 연산(Postfix Operation)
- 비트 연산자(Bitwise Operator)
- 논리 연산자(Logical Operator)
- 관계 연산자(Relational Operator)
- 조건 연산자(Conditional Operator)
- 대입 연산자(Assignment Operator)
- 시프트 연산자(Shift Operator)
- 연산자 우선순위(Operator Precedence)
- 결합 방향(Associativity)

다음 비트 연산자를 표로 정리해주세요.

| 연산자 | 의미 |
|---|---|
| 비트 AND(`&`) | 대응하는 두 비트가 모두 1일 때 1 |
| 비트 XOR(`^`) | 대응하는 두 비트가 다를 때 1 |
| 비트 OR(`|`) | 대응하는 비트 중 하나 이상이 1이면 1 |
| 비트 NOT(`~`) | 각 비트를 반전 |
| 왼쪽 시프트(`<<`) | 비트를 왼쪽으로 이동 |
| 오른쪽 시프트(`>>`) | 비트를 오른쪽으로 이동 |

논리 연산자와 비트 연산자를 반드시 구분해주세요.

| 구분 | 연산자 | 의미 |
|---|---|---|
| 논리 연산(Logical Operation) | `&&`, `||`, `!` | 참과 거짓을 대상으로 연산 |
| 비트 연산(Bitwise Operation) | `&`, `|`, `^`, `~` | 정수의 각 비트를 대상으로 연산 |

## 서식 문자열과 출력 함수

- 정수 서식 `%d`
- 문자 서식 `%c`
- 문자열 서식 `%s`
- C의 `printf()`
- Java의 `System.out.printf()`
- Java의 `System.out.print()`
- Java의 `System.out.println()`
- 각 함수의 줄바꿈 차이

## 조건문

- 단순 if문(Simple If Statement)
- if~else문
- switch문(Switch Statement)
- case
- default
- break
- switch문에서 break를 생략했을 때 발생하는 폴스루(Fall-through)

## 반복문

- for문(For Statement)
- while문(While Statement)
- do~while문(Do-While Statement)
- do~while문은 본문을 최소 한 번 실행한다는 특징
- 반복문 초기식, 조건식, 증감식
- 세 반복문의 차이 비교

비교 예시:

| 반복문 | 특징 |
|---|---|
| for문(For Statement) | 반복 횟수가 비교적 명확할 때 사용 |
| while문(While Statement) | 조건을 먼저 검사하고 반복 |
| do~while문(Do-While Statement) | 본문을 한 번 실행한 뒤 조건 검사 |

## 배열과 문자열

- 1차원 배열(One-Dimensional Array)
- 2차원 배열(Two-Dimensional Array)
- 배열 인덱스(Array Index)
- C 문자열(Character Array)
- 널 문자(Null Character) `\0`
- 문자열 저장 공간은 문자 수보다 1칸 더 필요하다는 점

예시:

```c
char text[5] = "love";
```

저장 구조:

```text
'l' 'o' 'v' 'e' '\0'
```

## 포인터

- 포인터(Pointer)
- 포인터 변수(Pointer Variable)
- 주소 연산자(Address Operator) `&`
- 간접 참조 연산자(Dereference Operator) `*`
- 포인터 선언
- 변수의 주소 저장
- 포인터를 통한 값 변경
- 포인터와 배열의 관계
- 배열 이름은 첫 번째 요소의 주소로 해석되는 경우가 있다는 점
- `a[i]`와 `*(a + i)` 관계

다음 코드는 실행 과정을 단계적으로 설명해주세요.

```c
int a = 50;
int *b;
b = &a;
*b = *b + 20;
printf("%d, %d", a, *b);
```

출력 결과:

```text
70, 70
```

## Python의 입출력과 형변환

- `input()`
- 입력값은 기본적으로 문자열이라는 점
- `int(input())`
- `map(int, input().split())`
- `print()`
- `sep`
- `end`

## Python의 자료형

- 리스트(List)
- 튜플(Tuple)
- 딕셔너리(Dictionary)
- 범위 객체(Range)
- 인덱스(Index)
- 키(Key)
- 리스트는 변경 가능(Mutable)
- 튜플은 변경 불가능(Immutable)
- 딕셔너리는 키와 값(Key-Value) 구조

비교표:

| 자료형 | 특징 |
|---|---|
| 리스트(List) | 순서가 있고 요소 변경 가능 |
| 튜플(Tuple) | 순서가 있지만 요소 변경 불가 |
| 딕셔너리(Dictionary) | 키와 값으로 구성 |
| 범위 객체(Range) | 연속된 정수 범위를 표현 |

## Python의 range와 슬라이싱

- `range(stop)`
- `range(start, stop)`
- `range(start, stop, step)`
- 종료값은 포함되지 않는다는 점
- 슬라이싱(Slicing)
- `[start:stop]`
- `[start:stop:step]`
- 시작과 종료 인덱스를 생략한 경우
- 슬라이싱의 종료 인덱스는 포함되지 않는다는 점

## Python의 반복문

- `for i in range(...)`
- 리스트를 직접 순회하는 for문
- Python의 while문
- 들여쓰기의 중요성

## Python의 클래스와 함수

- 클래스(Class)
- 객체(Object)
- 속성(Attribute)
- 메소드(Method)
- `self`
- 객체 생성
- 클래스 밖에서 정의하는 함수(Function)
- 매개변수(Parameter)
- 반환값(Return Value)

PDF의 클래스 예제를 분석하여 변수값과 반환값이 어떻게 계산되는지 단계적으로 설명해주세요.

## 스크립트 언어

- 자바스크립트(JavaScript)
- PHP
- 파이썬(Python)
- 쉘 스크립트(Shell Script)
- BASIC
- 클라이언트용과 서버용 스크립트 언어 구분
- 쉘 스크립트의 선택문 `if`, `case`
- 쉘 스크립트의 반복문 `for`, `while`, `until`

## 라이브러리와 메모리 할당

- 표준 라이브러리(Standard Library)
- 외부 라이브러리(External Library)
- C의 `stdlib.h`
- `atoi()`
- `atof()`
- `srand()`
- `rand()`
- `malloc()`
- `free()`
- 동적 메모리 할당(Dynamic Memory Allocation)
- `malloc()` 실패 시 `NULL` 반환
- 할당한 메모리는 `free()`로 해제

## Java의 예외 처리

- 예외(Exception)
- try
- catch
- finally
- finally는 일반적으로 예외 발생 여부와 관계없이 실행된다는 점

## 기억장치 배치 전략

- 최초 적합(First Fit)
- 최적 적합(Best Fit)
- 최악 적합(Worst Fit)
- 각 방식의 공간 선택 기준

비교표:

| 배치 전략 | 선택 기준 |
|---|---|
| 최초 적합(First Fit) | 들어갈 수 있는 첫 번째 공간 |
| 최적 적합(Best Fit) | 배치 후 남는 공간이 가장 작은 공간 |
| 최악 적합(Worst Fit) | 배치 후 남는 공간이 가장 큰 공간 |

## 페이지 교체

- 페이지(Page)
- 페이지 프레임(Page Frame)
- 페이지 부재(Page Fault)
- 선입선출 페이지 교체(FIFO Page Replacement)
- 가장 먼저 들어온 페이지를 가장 먼저 교체한다는 특징
- PDF에 제시된 FIFO 예제를 표 또는 단계별 과정으로 정리
- 페이지가 이미 프레임에 존재하면 페이지 부재가 발생하지 않는다는 점
- 페이지 부재 횟수 계산 방법

## 스래싱

- 스래싱(Thrashing)
- 실제 프로세스 실행보다 페이지 교체에 더 많은 시간이 소비되는 현상
- 페이지 부재 증가와 성능 저하의 관계

## 프로세스와 스레드

- 프로그램(Program)
- 프로세스(Process)
- 프로세스 제어 블록(PCB)
- 스레드(Thread)
- 프로세스는 실행 중인 프로그램이라는 점
- 스레드는 프로세스 내부의 실행 단위라는 점

## HRN 스케줄링

- 높은 응답률 우선(HRN, Highest Response Ratio Next)
- 우선순위 공식

다음 공식을 Markdown 수식 또는 일반 텍스트로 정확하게 표시해주세요.

```text
우선순위 = (대기시간 + 서비스시간) / 서비스시간
```

- 계산 결과가 큰 프로세스를 먼저 실행
- PDF에 제시된 작업 A, B, C, D의 값을 계산하여 설명
- 시험에서 계산 절차를 따라갈 수 있도록 작성

## UNIX 주요 명령어

다음 명령어를 표로 정리해주세요.

| 명령어 | 의미 |
|---|---|
| `fork` | 새로운 프로세스 생성 |
| `uname` | 시스템 또는 커널 정보 확인 |
| `wait` | 자식 프로세스의 종료 등을 기다림 |
| `chmod` | 파일 권한 변경 |
| `ls` | 파일 목록 출력 |
| `cat` | 파일 내용 출력 |
| `chown` | 소유자 변경 |
| `umask` | 새 파일과 디렉터리의 기본 권한 마스크 설정 |

`umask`는 단순히 666 또는 777에서 산술적으로 빼는 방식만으로 설명하지 말고, 실제로는 비트 마스크 방식이라는 주의사항을 덧붙여주세요.

시험용 간단 계산에서는 다음 기준을 사용할 수 있다고 설명해주세요.

- 일반 파일 기본 권한: `666`
- 디렉터리 기본 권한: `777`

## IPv4와 IPv6

- IPv4
- 32비트
- 8비트씩 4부분
- 점으로 구분된 10진수 표기
- IPv6
- 128비트
- 16비트씩 8부분
- 콜론으로 구분된 16진수 표기
- 유니캐스트(Unicast)
- 멀티캐스트(Multicast)
- 애니캐스트(Anycast)

비교표:

| 구분 | IPv4 | IPv6 |
|---|---|---|
| 주소 길이 | 32비트 | 128비트 |
| 기본 표기 | 10진수와 점 | 16진수와 콜론 |
| 주소 유형 | 유니캐스트·멀티캐스트·브로드캐스트 | 유니캐스트·멀티캐스트·애니캐스트 |

## OSI 7계층

PDF에 나온 계층을 중심으로 정리하되, 전체 계층 순서도 간단히 제시해주세요.

> 물리 → 데이터 링크 → 네트워크 → 전송 → 세션 → 표현 → 응용

다음 계층을 자세히 정리해주세요.

- 데이터 링크 계층(Data Link Layer)
  - 프레임(Frame)
  - 흐름 제어
  - 오류 제어
  - 순서 제어
  - HDLC, LAPB, PPP, LLC
- 네트워크 계층(Network Layer)
  - 패킷(Packet)
  - 경로 설정(Routing)
  - 트래픽 제어
- 전송 계층(Transport Layer)
  - 종단 간 전송(End-to-End Delivery)
  - 오류 제어
  - 흐름 제어
  - 다중화
- 세션 계층(Session Layer)
  - 대화 제어
  - 동기 제어
  - 세션 생성·관리·종료

## 네트워크 장비

다음 장비를 표로 정리해주세요.

| 장비 | 핵심 기능 |
|---|---|
| 리피터(Repeater) | 약해진 신호를 재생 |
| 브리지(Bridge) | LAN 구간 연결 |
| 스위치(Switch) | MAC 주소를 기반으로 프레임 전달 |
| 라우터(Router) | 네트워크 간 최적 경로 선택 |
| 브라우터(Brouter) | 브리지와 라우터 기능 결합 |
| 전처리기(Front-End Processor) | 통신 제어와 사전 처리를 담당하여 시스템 부담 감소 |

가능하면 OSI 계층과 연결하여 설명해주세요.

## TCP/IP 프로토콜

- MQTT
- 발행-구독(Publish-Subscribe)
- IoT 환경
- TCP
- 연결형(Connection-Oriented)
- 신뢰성 있는 전송
- 순서 제어
- 오류 제어
- 흐름 제어
- UDP
- 비연결형(Connectionless)
- 순서·흐름 제어를 제공하지 않음
- 전송 속도가 빠름
- 실시간 전송에 유리
- ARP
- IP 주소를 MAC 주소로 변환
- CSMA/CD
- IEEE 802.3
- 이더넷(Ethernet)
- 충돌 감지(Collision Detection)

TCP와 UDP는 반드시 비교표로 작성해주세요.

| 구분 | TCP | UDP |
|---|---|---|
| 연결 방식 | 연결형(Connection-Oriented) | 비연결형(Connectionless) |
| 신뢰성 | 높음 | 상대적으로 낮음 |
| 순서 보장 | 제공 | 제공하지 않음 |
| 속도 | 상대적으로 느림 | 빠름 |
| 주요 활용 | 정확성이 중요한 전송 | 실시간·저지연 전송 |

────────────────────
8. 강조와 암기 표현
────────────────────

시험에 중요한 문장은 다음 형식으로 강조해주세요.

```markdown
**핵심:** do~while문은 조건이 거짓이어도 본문을 최소 한 번 실행한다.
```

암기 순서가 있는 내용은 인용문 형식으로 표시해주세요.

```markdown
> 물리 → 데이터 링크 → 네트워크 → 전송 → 세션 → 표현 → 응용
```

다음 암기 표현을 포함해주세요.

- 전치: 증가 후 사용
- 후치: 사용 후 증가
- do~while: 최소 1회 실행
- 문자열: 문자 수+1만큼 공간 확보
- `&`: 주소 구하기
- `*`: 가리키는 값에 접근
- Python `input()`: 문자열로 입력
- `range()`의 종료값은 포함하지 않음
- 최초 적합: 첫 번째
- 최적 적합: 가장 작게 남김
- 최악 적합: 가장 크게 남김
- FIFO: 먼저 들어온 페이지를 먼저 교체
- 스래싱: 실행보다 페이지 교체에 시간을 더 소비
- HRN: 계산값이 클수록 우선
- IPv4: 32비트
- IPv6: 128비트
- TCP: 연결형·신뢰성
- UDP: 비연결형·빠른 전송
- ARP: IP 주소 → MAC 주소

────────────────────
9. 시험 직전 필수 암기표
────────────────────

문서 마지막에는 반드시 다음 제목을 추가해주세요.

# 시험 직전 필수 암기표

시험 직전 빠르게 복습할 수 있도록 핵심 내용을 2열 표로 정리해주세요.

형식 예시:

| 주제 | 핵심 내용 |
|---|---|
| 가비지 컬렉터(Garbage Collector) | 사용하지 않는 메모리 공간을 자동으로 회수 |
| 전치 증가(Prefix Increment) | 먼저 증가한 뒤 값 사용 |
| 후치 증가(Postfix Increment) | 값을 먼저 사용한 뒤 증가 |
| 비트 XOR(`^`) | 두 비트가 다르면 1 |
| 조건 연산자(Conditional Operator) | `조건 ? 참값 : 거짓값` |
| `%d` | 정수 출력 |
| `%c` | 문자 출력 |
| `%s` | 문자열 출력 |
| do~while문 | 최소 한 번 실행 |
| 문자열의 끝 | 널 문자 `\0` |
| 주소 연산자(`&`) | 변수의 주소를 구함 |
| 간접 참조 연산자(`*`) | 포인터가 가리키는 값에 접근 |
| Python `input()` | 문자열로 입력 |
| Python 리스트(List) | 변경 가능 |
| Python 튜플(Tuple) | 변경 불가능 |
| `range(start, stop)` | stop은 포함하지 않음 |
| `malloc()` | 동적 메모리 할당 |
| `free()` | 동적 메모리 해제 |
| 최초 적합(First Fit) | 첫 번째로 들어갈 수 있는 공간 |
| 최적 적합(Best Fit) | 남는 공간이 가장 작은 곳 |
| 최악 적합(Worst Fit) | 남는 공간이 가장 큰 곳 |
| FIFO | 가장 먼저 들어온 페이지 교체 |
| 스래싱(Thrashing) | 처리보다 페이지 교체 시간이 더 많음 |
| 프로세스(Process) | 실행 중인 프로그램 |
| 스레드(Thread) | 프로세스 내 실행 단위 |
| HRN | `(대기시간+서비스시간)/서비스시간` |
| `fork` | 프로세스 생성 |
| `chmod` | 권한 변경 |
| `chown` | 소유자 변경 |
| IPv4 | 32비트 |
| IPv6 | 128비트 |
| 데이터 링크 계층(Data Link Layer) | 프레임·오류 제어·흐름 제어 |
| 네트워크 계층(Network Layer) | 경로 설정과 패킷 전달 |
| 전송 계층(Transport Layer) | 종단 간 데이터 전송 |
| 세션 계층(Session Layer) | 대화와 동기 제어 |
| 라우터(Router) | 네트워크 간 최적 경로 선택 |
| TCP | 연결형·신뢰성 있는 전송 |
| UDP | 비연결형·빠른 전송 |
| ARP | IP 주소를 MAC 주소로 변환 |
| MQTT | 발행-구독 기반 IoT 메시징 |
| CSMA/CD | 이더넷의 충돌 감지 방식 |

예상문제, 연습문제, 퀴즈, 정답 부분은 절대로 추가하지 마세요.

────────────────────
10. 정확성 검토
────────────────────

PDF의 설명을 그대로 복사하기 전에 기술적으로 정확한지 확인해주세요.

특히 다음 항목을 검토해주세요.

- C언어에는 일반적으로 자동 가비지 컬렉터가 없다는 점
- Java의 `char` 크기는 일반적으로 2바이트라는 점
- Java의 `boolean` 크기는 JVM 구현에 따라 단순히 1바이트로 단정하기 어렵다는 점
- C 자료형의 크기는 구현 환경에 따라 달라질 수 있다는 점
- 나머지 연산자 `%`의 피연산자 규칙은 언어에 따라 다르다는 점
- C 문자열 끝에는 널 문자 `\0`가 필요하다는 점
- 배열 이름과 포인터가 완전히 동일한 개념은 아니라는 점
- `umask`는 단순 산술 뺄셈이 아닌 권한 비트 마스크라는 점
- TCP와 UDP의 특징
- ARP의 변환 방향은 IP 주소에서 MAC 주소라는 점
- IPv6에는 브로드캐스트가 없고 애니캐스트를 사용한다는 점

PDF의 설명이 부정확하면 본문은 정확한 개념으로 작성하고 다음처럼 짧게 주의사항을 표시해주세요.

```markdown
> **주의:** 원문에는 Java의 `char`가 1Byte로 표시되어 있으나, Java의 `char`는 UTF-16 코드 단위를 저장하는 16비트 자료형이다.
```

다만 주의사항을 지나치게 많이 작성하여 학습 흐름을 방해하지 마세요.

────────────────────
11. Markdown 호환성과 품질 검사
────────────────────

완성된 Markdown 파일이 일반적인 Markdown-to-PDF 변환기에서 깨지지 않도록 다음 규칙을 지켜주세요.

- 표준 Markdown 문법만 사용
- 제목은 `#`, `##`, `###` 형식 사용
- 표는 파이프(`|`) 기반 표준 Markdown 표 사용
- 코드 블록은 삼중 백틱과 언어명을 사용
- HTML, CSS, JavaScript를 사용하지 않음
- Mermaid를 사용하지 않음
- 이미지 링크나 외부 URL을 임의로 추가하지 않음
- 이모지와 장식용 특수문자를 과도하게 사용하지 않음
- 코드 블록 안에서 스마트 따옴표를 사용하지 않음
- 표의 각 행에서 열 개수가 일치하는지 확인
- 코드 블록의 시작과 종료 백틱이 정확히 대응하는지 확인
- 한글은 UTF-8로 저장
- 지나치게 넓은 표를 만들지 않음
- 파일 끝에 불필요한 빈 코드 블록을 만들지 않음

파일을 생성한 뒤 다음 사항을 자체 검토해주세요.

1. `4과목 프로그래밍 언어 활용` 이후부터 정리했는가
2. 3과목의 SQL·트리거 내용이 섞이지 않았는가
3. 5과목의 소프트웨어 재사용·재공학·CASE 내용이 섞이지 않았는가
4. 주요 용어가 `한글(English)` 형식으로 작성되었는가
5. 영어 용어가 별도 열로 분리된 표가 없는가
6. 예상문제 부분이 없는가
7. C, Java, Python 코드 블록이 올바른가
8. 코드의 들여쓰기와 따옴표가 정상적인가
9. 연산자와 포인터 설명이 정확한가
10. FIFO와 HRN 계산 방식이 정확한가
11. IPv4·IPv6와 TCP·UDP 설명이 정확한가
12. Markdown 문법 오류가 없는가
13. UTF-8로 저장되었는가
14. 실제 `.md` 파일이 생성되었는가

검토 과정에서 문제가 발견되면 수정한 뒤 최종 파일을 제공해주세요.
-------------------------------
첨부한 정보처리기사 필기 핵심요약 PDF를 분석하여, 5과목의 시험 대비 정리본을 Markdown(.md) 파일로 작성해주세요.

이번 작업 대상:
- 5과목: 정보시스템 구축 관리

중요:
- 단순히 채팅창에 내용만 출력하지 말고 실제 `.md` 파일을 생성해주세요.
- 파일명은 다음과 같이 저장해주세요.
  `정보처리기사_5과목_정보시스템구축관리_핵심정리.md`
- PDF에서 `5과목 정보시스템 구축 관리`라는 제목이 나오는 지점부터 PDF의 마지막 페이지까지를 5과목 범위로 판단해주세요.
- 5과목 제목과 같은 페이지에 4과목 내용인 ARP, CSMA/CD 등이 앞부분에 있을 수 있으므로 반드시 `5과목 정보시스템 구축 관리` 제목 이후의 내용만 포함해주세요.
- 첨부 PDF를 중심으로 정리하되, PDF에 있는 내용을 단순 복사하지 말고 시험 대비용으로 이해하기 쉽게 재구성해주세요.
- PDF에 오탈자, 오래된 표현, 용어 혼동 또는 명백한 기술적 오류가 있으면 그대로 옮기지 말고 올바르게 수정해주세요.
- 기술적 오류를 수정한 경우 필요한 위치에만 짧은 주의사항을 표시해주세요.
- 작업이 끝나면 생성한 Markdown 파일의 정확한 경로를 알려주세요.

────────────────────
1. 문서 목적
────────────────────

이 문서는 정보처리기사 필기시험 직전 복습에 사용하는 학습 노트입니다.

다음 기준으로 작성해주세요.

- 대학생 초보자가 이해할 수 있는 수준으로 설명
- 시험에 필요한 개념과 구분 기준 중심
- 단순한 용어 나열이 아니라 핵심 의미가 이해되도록 설명
- 비슷하거나 혼동하기 쉬운 개념은 비교표로 정리
- 순서가 있는 개념은 흐름과 암기법을 함께 표시
- 계산 문제는 공식, 변수의 의미, 간단한 계산 절차를 설명
- 보안 공격은 공격 원리와 핵심 구분점을 중심으로 설명
- 불필요하게 깊은 구현 코드나 시험 범위를 벗어난 세부사항은 추가하지 않음
- 예상문제, 연습문제, 퀴즈, 정답 부분은 작성하지 않음

────────────────────
2. 문서 제목과 전체 학습 흐름
────────────────────

문서 제목은 다음과 같이 작성해주세요.

# 정보처리기사 필기 5과목 — 정보시스템 구축 관리 핵심 정리

문서 시작 부분에는 5과목의 전체 흐름을 한 줄로 작성해주세요.

예시:

> 소프트웨어 유지·재공학 → 비용과 일정 관리 → 품질·위험 관리 → 정보시스템 신기술 → 데이터베이스 관리 → 보안과 암호화 → 공격 기법과 보안 시스템

이후 다음과 같은 번호 체계를 사용해주세요.

# 1. 소프트웨어 재사용

# 2. 소프트웨어 재공학

# 3. CASE

# 4. 소프트웨어 비용 산정

# 5. LOC 기법

# 6. COCOMO 모형

# 7. 일정 관리 기법

# 8. 소프트웨어 프로젝트 관리

# 9. 위험 관리

# 10. CMMI

# 11. 네트워크 및 컴퓨팅 신기술

# 12. 클라우드 컴퓨팅

# 13. 보안·인증 및 네트워크 기술

# 14. 컨테이너와 플랫폼 기술

# 15. 빅데이터 기술

# 16. 데이터베이스 회복

# 17. 병행제어와 로킹

# 18. 교착상태

# 19. 정보보안의 3대 요소

# 20. 웹 및 시스템 보안 약점

# 21. 접근 지정자

# 22. 암호화 방식

# 23. 해시와 솔트

# 24. 네트워크 공격

# 25. 악성 프로그램

# 26. 시스템 로그

# 27. 인증 방식

# 28. 침입 탐지 시스템

# 29. SSH

# 시험 직전 필수 암기표

PDF의 실제 내용 흐름에 따라 세부 제목은 조정할 수 있지만, PDF의 5과목 핵심 개념을 누락하지 마세요.

────────────────────
3. 한글과 영어 용어 표기 규칙
────────────────────

모든 주요 전문 용어는 처음 등장할 때 다음 형식으로 작성해주세요.

`한글 용어(English Term)`

예시:

- 소프트웨어 재공학(Software Reengineering)
- 분석(Analysis)
- 재구성(Restructuring)
- 역공학(Reverse Engineering)
- 이식(Migration)
- 기능 점수(Function Point)
- 위험 관리(Risk Management)
- 초기 단계(Initial)
- 소프트웨어 정의 네트워킹(SDN)
- 서비스형 인프라(IaaS)
- 즉각 갱신(Immediate Update)
- 상호 배제(Mutual Exclusion)
- 기밀성(Confidentiality)
- SQL 삽입(SQL Injection)
- 공개키 암호화(Public Key Encryption)
- 침입 탐지 시스템(IDS)

한글과 영어를 별도 열로 분리하지 마세요.

잘못된 형식:

| 개념 | 영어 | 의미 |
|---|---|---|
| 기밀성 | Confidentiality | 인가된 사용자만 접근 |

올바른 형식:

| 개념 | 의미 |
|---|---|
| 기밀성(Confidentiality) | 인가된 사용자만 정보에 접근할 수 있도록 보장하는 성질 |

즉, 한글과 영어는 항상 같은 셀 또는 같은 문장 안에 표시해주세요.

────────────────────
4. 개념 설명 형식
────────────────────

각 개념은 가능한 경우 다음 순서로 설명해주세요.

1. 정의
2. 주요 특징
3. 세부 종류 또는 단계
4. 시험에서 구분해야 할 핵심
5. 간단한 사례 또는 계산 예시

예시:

## 역공학(Reverse Engineering)

기존 소프트웨어를 분석하여 소스코드, 데이터 처리 과정, 설계 정보 등을 다시 찾아내는 활동입니다.

- 기존 시스템의 구조와 동작을 파악할 때 사용
- 시스템을 새로 변경하는 것보다 분석하고 정보를 복원하는 데 중점
- 재공학의 한 활동에 해당

**핵심:** 역공학은 기존 소프트웨어에서 상위 수준의 분석·설계 정보를 찾아내는 활동입니다.

────────────────────
5. Markdown 표 작성 규칙
────────────────────

비교가 필요한 내용은 표준 Markdown 표로 정리해주세요.

예시:

| 활동 | 의미 |
|---|---|
| 분석(Analysis) | 기존 소프트웨어를 이해하고 재공학 대상을 선정 |
| 재구성(Restructuring) | 기능은 유지하면서 코드나 구조를 개선 |
| 역공학(Reverse Engineering) | 기존 시스템에서 분석·설계 정보를 재발견 |
| 이식(Migration) | 다른 운영체제나 하드웨어 환경에서 사용할 수 있도록 변환 |

표 작성 시 다음 규칙을 지켜주세요.

- 열은 가능하면 2~3개만 사용
- 영어 용어를 별도 열로 분리하지 않음
- 셀 안의 설명은 핵심 위주로 작성
- HTML 표를 사용하지 않음
- 셀 병합을 사용하지 않음
- 각 행의 열 개수를 동일하게 유지
- PDF 변환 시 가로폭이 지나치게 넓어지지 않도록 구성

────────────────────
6. 수식과 계산 표현 규칙
────────────────────

계산식은 일반 Markdown에서 깨지지 않도록 텍스트 코드 블록이나 인라인 코드로 표시해주세요.

예시:

```text
노력(인월) = LOC / 1인당 월평균 생산 코드 라인 수
```

```text
개발 비용 = 노력(인월) × 1인당 월평균 인건비
```

공식을 제시할 때는 다음을 함께 설명해주세요.

- 각 변수의 의미
- 단위
- 계산 순서
- 계산 결과가 의미하는 것

복잡한 LaTeX 문법은 사용하지 마세요.

────────────────────
7. 반드시 포함할 핵심 내용
────────────────────

다음 내용을 빠뜨리지 말고 포함해주세요.

## 소프트웨어 재사용

- 소프트웨어 재사용(Software Reuse)
- 개발 시간과 비용 단축
- 품질 향상
- 생산성 향상
- 시스템 명세·설계·코드 등 산출물 공유
- 재사용의 장점

## 소프트웨어 재공학

- 소프트웨어 재공학(Software Reengineering)
- 분석(Analysis)
- 재구성(Restructuring)
- 역공학(Reverse Engineering)
- 이식(Migration)

다음 형식의 비교표를 포함해주세요.

| 활동 | 핵심 의미 |
|---|---|
| 분석(Analysis) | 기존 소프트웨어를 이해하고 재공학 대상 선정 |
| 재구성(Restructuring) | 코드와 구조를 더 나은 형태로 재구성 |
| 역공학(Reverse Engineering) | 기존 시스템에서 분석·설계 정보 재발견 |
| 이식(Migration) | 다른 운영체제·하드웨어 환경으로 이전 |

특히 역공학과 재공학을 구분해주세요.

- 역공학: 기존 소프트웨어를 분석하여 정보를 추출
- 재공학: 분석한 기존 소프트웨어를 개선하고 재구성하는 전체 활동

## CASE

- 컴퓨터 지원 소프트웨어 공학(CASE, Computer-Aided Software Engineering)
- 개발 과정의 자동화
- 소프트웨어 모듈의 재사용성 향상
- 개발 기간 단축
- 개발 비용 절감
- 품질 향상
- 유지보수 편의성 향상

## LOC 비용 산정

- 코드 라인 수(LOC, Lines of Code)
- 낙관치(Optimistic Estimate)
- 기대치 또는 최빈치(Most Likely Estimate)
- 비관치(Pessimistic Estimate)
- 노력(Effort)
- 인월(Person-Month)
- 개발 비용
- 개발 기간
- 생산성

다음 공식을 정확하게 포함해주세요.

```text
노력(인월) = 개발 기간 × 투입 인원
```

```text
노력(인월) = LOC / 1인당 월평균 생산 코드 라인 수
```

```text
개발 비용 = 노력(인월) × 1인당 월평균 인건비
```

```text
개발 기간 = 노력(인월) / 투입 인원
```

```text
생산성 = LOC / 노력(인월)
```

간단한 숫자 예시 하나를 추가해주세요.

예:

- LOC: 12,000줄
- 1인당 월평균 생산성: 1,000줄
- 노력: 12인월

다만 예상문제 형식으로 만들지는 마세요.

## COCOMO

- 건설적 비용 산정 모형(COCOMO, Constructive Cost Model)
- 배리 보엠(Barry Boehm)
- LOC 기반 비용 산정
- 조직형(Organic Mode)
- 반분리형(Semi-Detached Mode)
- 내장형(Embedded Mode)

다음 비교표를 포함해주세요.

| 유형 | 특징 |
|---|---|
| 조직형(Organic Mode) | 비교적 작고 단순하며 개발 경험이 풍부한 프로젝트 |
| 반분리형(Semi-Detached Mode) | 조직형과 내장형의 중간 규모·복잡도 |
| 내장형(Embedded Mode) | 하드웨어·실시간·규제 등 강한 제약을 받는 복잡한 시스템 |

PDF에 제시된 KDSI 규모 기준은 전통적 기본 COCOMO 설명과 표현이 다를 수 있으므로, 무조건 단정하지 말고 다음과 같이 주의사항을 표시해주세요.

```markdown
> **주의:** COCOMO 개발 유형은 단순히 코드 라인 수만이 아니라 프로젝트 규모, 복잡성, 개발 환경과 제약조건을 함께 고려하여 구분한다.
```

## PERT와 임계 경로

- 프로그램 평가 및 검토 기술(PERT)
- 낙관 시간(Optimistic Time)
- 최빈 시간(Most Likely Time)
- 비관 시간(Pessimistic Time)
- 작업 사이의 선후 관계
- 임계 경로(Critical Path)
- 임계 경로는 전체 프로젝트 완료 시간을 결정하는 가장 긴 경로라는 점

PERT 기대시간 공식을 추가해주세요.

```text
기대시간 = (낙관시간 + 4 × 최빈시간 + 비관시간) / 6
```

임계 경로에 있는 작업이 지연되면 전체 프로젝트 일정이 지연될 수 있음을 설명해주세요.

## 간트 차트

- 간트 차트(Gantt Chart)
- 작업 일정
- 가로 막대
- 막대 길이는 작업 기간을 나타냄
- 작업 진행 상태 확인
- 작업 간의 복잡한 의존 관계 표현에는 한계가 있다는 점

PERT와 간트 차트도 비교해주세요.

| 기법 | 핵심 특징 |
|---|---|
| PERT | 작업 순서와 일정의 불확실성을 분석 |
| 임계 경로(Critical Path) | 프로젝트 전체 기간을 결정하는 가장 긴 경로 |
| 간트 차트(Gantt Chart) | 작업별 일정을 가로 막대로 표현 |

## 소프트웨어 프로젝트 관리

- 소프트웨어 프로젝트 관리(Software Project Management)
- 일정 관리
- 비용 관리
- 인력 관리
- 품질 관리
- 위험 관리
- 정해진 기간과 비용 안에서 요구사항을 충족하는 것이 목적

## 위험 관리

- 위험(Risk)
- 위험 식별(Risk Identification)
- 위험 분석 및 평가(Risk Analysis and Assessment)
- 위험 관리 계획(Risk Management Planning)
- 위험 감시 및 조치(Risk Monitoring and Control)

다음 순서를 강조해주세요.

> 위험 식별 → 위험 분석 및 평가 → 위험 관리 계획 → 위험 감시 및 조치

**핵심:** 위험 감시는 위험이 현실화되는 징후를 지속적으로 확인하는 활동입니다.

## CMMI

- 능력 성숙도 통합 모델(CMMI, Capability Maturity Model Integration)
- 소프트웨어 개발 조직의 프로세스 능력과 성숙도 평가
- 초기(Initial)
- 관리(Managed)
- 정의(Defined)
- 정량적 관리(Quantitatively Managed)
- 최적화(Optimizing)

단계 순서를 다음처럼 표시해주세요.

> 초기 → 관리 → 정의 → 정량적 관리 → 최적화

암기법도 간단히 작성해주세요.

> **초관정정최**

각 단계의 의미를 한 문장으로 설명해주세요.

## SDN

- 소프트웨어 정의 네트워킹(SDN, Software-Defined Networking)
- 제어 평면(Control Plane)
- 데이터 평면(Data Plane)
- 네트워크 제어 기능을 소프트웨어적으로 중앙 관리
- 네트워크 가상화
- 정책 기반 제어

PDF의 모호한 설명을 단순 복사하지 말고, 제어부와 전달부를 분리하여 중앙에서 프로그래밍 가능하게 관리한다는 핵심을 설명해주세요.

## 유비쿼터스 컴퓨팅

- 유비쿼터스 컴퓨팅(Ubiquitous Computing)
- 언제 어디서나 컴퓨팅 가능
- 다양한 기기와 네트워크의 연결
- 사용자가 컴퓨터의 존재를 의식하지 않아도 서비스를 이용할 수 있는 환경

## 클라우드 컴퓨팅 서비스 유형

- 서비스형 인프라(IaaS)
- 서비스형 플랫폼(PaaS)
- 서비스형 소프트웨어(SaaS)

다음 비교표를 포함해주세요.

| 유형 | 사용자가 제공받는 범위 |
|---|---|
| 서비스형 인프라(IaaS) | 서버·스토리지·네트워크 등 인프라 |
| 서비스형 플랫폼(PaaS) | 애플리케이션 개발·실행 플랫폼 |
| 서비스형 소프트웨어(SaaS) | 완성된 소프트웨어를 서비스 형태로 이용 |

**핵심:** IaaS → PaaS → SaaS로 갈수록 사용자가 직접 관리하는 범위가 줄어듭니다.

## 서버 팜

- 서버 팜(Server Farm)
- 여러 서버를 집약하여 하나의 시스템처럼 운영
- 대용량 처리
- 부하 분산
- 서비스 안정성 향상

## 웹 크롤링

- 웹 크롤링(Web Crawling)
- 크롤러(Crawler)
- 웹 페이지를 자동으로 탐색하여 데이터 수집
- 파이썬(Python)
- 스크래피(Scrapy)
- 웹 스크래핑(Web Scraping)과의 관계를 간단히 설명

## 메타버스

- 메타버스(Metaverse)
- 현실과 가상 공간의 결합
- 사회·경제·문화 활동이 이루어지는 3차원 가상 공간

## 메시 네트워크

- 메시 네트워크(Mesh Network)
- 다수의 노드를 그물망처럼 연결
- 여러 경로를 통한 통신
- 일부 노드 장애 시 다른 경로 이용 가능
- 안정적인 무선망 구성

## 클라우드 기반 HSM

- 하드웨어 보안 모듈(HSM, Hardware Security Module)
- 암호화 키 생성
- 키 저장
- 키 연산
- 키를 하드웨어적으로 보호
- 클라우드 HSM(Cloud HSM)

원문의 “소프트웨어 암호 기술의 보안 취약점을 무시할 수 있다”는 표현을 그대로 쓰지 말고 다음처럼 정확히 설명해주세요.

> 하드웨어 기반의 보호 경계를 사용하여 키 탈취와 무단 접근 위험을 줄이지만, 모든 보안 취약점을 자동으로 제거하는 것은 아니다.

## PaaS-TA

- 파스-타(PaaS-TA)
- 국내에서 개발된 개방형 클라우드 플랫폼
- 애플리케이션 개발·배포·운영 환경 제공
- 개방형 PaaS 플랫폼

최신 명칭이나 구성에 대해 확실하지 않은 내용을 임의로 확대하지 말고 PDF의 시험 범위 수준으로 정리해주세요.

## 근거리 무선통신 Zing

- 징(Zing)
- 근접 무선통신
- 짧은 거리
- 고속 데이터 전송

PDF의 수치나 표현이 오래된 기술 소개일 수 있으므로 핵심 시험 용어 위주로 작성해주세요.

## SSO

- 통합 인증(SSO, Single Sign-On)
- 한 번의 인증으로 여러 서비스에 접근
- 사용자 편의성
- 중앙 인증 시스템
- 중앙 인증 정보가 침해되면 피해 범위가 커질 수 있다는 점

## WPA

- Wi-Fi 보호 접속(WPA, Wi-Fi Protected Access)
- 무선 LAN 인증
- 무선 통신 암호화
- WEP의 취약점을 보완하기 위해 등장
- WPA, WPA2, WPA3는 서로 다른 세대라는 점을 간단히 표시

## RIP와 OSPF

- 라우팅 정보 프로토콜(RIP)
- 거리 벡터 라우팅(Distance Vector Routing)
- 홉 수(Hop Count)
- 최대 홉 수 15
- 개방형 최단 경로 우선(OSPF)
- 링크 상태 라우팅(Link-State Routing)
- 다익스트라 알고리즘(Dijkstra Algorithm)
- 비용(Cost)을 기준으로 최단 경로 계산

비교표:

| 구분 | RIP | OSPF |
|---|---|---|
| 방식 | 거리 벡터(Distance Vector) | 링크 상태(Link State) |
| 주요 기준 | 홉 수 | 링크 비용 |
| 최대 홉 수 | 15 | 고정된 15홉 제한 없음 |
| 특징 | 구조가 단순하지만 대규모망에 한계 | 수렴이 빠르고 대규모망에 적합 |

## 증발품

- 증발품(Vaporware)
- 제품 출시 계획은 발표했지만 실제로 출시되지 않거나 지나치게 지연된 제품

## 도커

- 도커(Docker)
- 컨테이너(Container)
- 애플리케이션과 실행 환경을 패키징
- 운영체제 수준 가상화
- 이미지(Image)
- 컨테이너와 가상머신(Virtual Machine)의 차이를 간단히 비교

| 구분 | 컨테이너(Container) | 가상머신(Virtual Machine) |
|---|---|---|
| 실행 구조 | 호스트 운영체제의 커널 공유 | 별도의 게스트 운영체제 사용 |
| 크기 | 상대적으로 작음 | 상대적으로 큼 |
| 시작 속도 | 빠름 | 상대적으로 느림 |

## 매시업과 디지털 트윈

- 매시업(Mashup)
- 여러 웹 서비스·데이터·기능을 결합하여 새로운 서비스 생성
- 디지털 트윈(Digital Twin)
- 현실의 사물·설비·공정을 디지털 공간에 복제한 가상 모델
- 실제 객체의 상태를 반영하고 분석·예측에 활용

## 서비스형 블록체인

- 서비스형 블록체인(BaaS, Blockchain as a Service)
- 클라우드 기반 블록체인 개발 환경
- 인프라 구성 부담 감소

BaaS가 Backend as a Service를 의미하는 경우도 있으므로, 이 문서에서는 PDF 문맥상 Blockchain as a Service임을 명확하게 표시해주세요.

## TCP 래퍼

- TCP 래퍼(TCP Wrapper)
- 네트워크 서비스 접근 통제
- 접속 요청의 허용·거부
- 호스트 기반 접근 제어
- 과거 UNIX 계열 시스템에서 사용된 보안 도구라는 점

## DPI

- 심층 패킷 검사(DPI, Deep Packet Inspection)
- 패킷 헤더뿐 아니라 페이로드까지 분석
- 침입 탐지
- 악성 트래픽 식별
- 트래픽 관리
- 암호화된 트래픽에서는 분석에 제한이 있을 수 있다는 점

## 허니팟

- 허니팟(Honeypot)
- 공격자를 유인하기 위해 의도적으로 설치한 시스템
- 공격 행위 관찰
- 침입 탐지
- 실제 운영 시스템과 격리해야 한다는 점

## 고가용성

- 고가용성(High Availability)
- 장애 발생 시 대체 시스템으로 전환
- 페일오버(Failover)
- 서비스 중단 최소화
- HACMP는 IBM 계열의 고가용성 클러스터 솔루션 명칭이라는 점

## 하둡과 스쿱

- 하둡(Hadoop)
- 분산 저장
- 분산 처리
- 하둡 분산 파일 시스템(HDFS)
- 관계형 데이터베이스(RDB)
- 스쿱(Sqoop)
- RDB와 Hadoop 사이의 대용량 데이터 전송

## 맵리듀스

- 맵리듀스(MapReduce)
- 맵(Map)
- 리듀스(Reduce)
- 대용량 데이터의 분산 병렬 처리
- Map 단계는 데이터를 가공하고, Reduce 단계는 결과를 모아 집계한다는 흐름

## 데이터 마이닝

- 데이터 마이닝(Data Mining)
- 대량의 데이터에서 의미 있는 패턴과 지식 발견
- 분류(Classification)
- 군집화(Clustering)
- 연관 규칙(Association Rule)
- 예측(Prediction)

PDF 범위를 지나치게 벗어나지 않는 범위에서 대표 기능만 간단히 설명해주세요.

## OLAP

- 온라인 분석 처리(OLAP, Online Analytical Processing)
- 다차원 데이터 분석
- 의사결정 지원
- 롤업(Roll-Up)
- 드릴다운(Drill-Down)
- 드릴스루(Drill-Through)
- 드릴어크로스(Drill-Across)
- 피벗(Pivot)
- 슬라이싱(Slicing)
- 다이싱(Dicing)

다음 표를 포함해주세요.

| 연산 | 의미 |
|---|---|
| 롤업(Roll-Up) | 더 높은 수준으로 데이터를 요약 |
| 드릴다운(Drill-Down) | 더 세부적인 수준으로 이동 |
| 드릴스루(Drill-Through) | 요약 데이터에서 원천 상세 데이터로 이동 |
| 드릴어크로스(Drill-Across) | 관련된 다른 데이터 큐브나 사실 테이블로 이동 |
| 피벗(Pivot) | 분석 축의 방향을 변경 |
| 슬라이싱(Slicing) | 한 차원의 특정 값을 선택 |
| 다이싱(Dicing) | 여러 차원의 일부 범위를 선택 |

────────────────────
8. 데이터베이스 회복과 병행제어
────────────────────

## 회복

- 회복(Recovery)
- 장애 발생 이전의 정상 상태로 복구
- 로그(Log)
- 재실행(Redo)
- 취소(Undo)

## 즉각 갱신

- 즉각 갱신(Immediate Update)
- 트랜잭션이 완료되기 전에도 실제 데이터베이스에 변경 내용을 반영
- 장애에 대비해 로그를 기록
- 일반적으로 Undo와 Redo가 모두 필요할 수 있음

필요하면 지연 갱신(Deferred Update)과 간단히 비교해주세요.

| 방식 | 핵심 특징 |
|---|---|
| 즉각 갱신(Immediate Update) | 커밋 전에도 데이터베이스에 반영 가능, Undo·Redo 고려 |
| 지연 갱신(Deferred Update) | 커밋 후 데이터베이스에 반영, 주로 Redo 사용 |

## 병행제어

- 병행제어(Concurrency Control)
- 데이터베이스 공유 최대화
- 시스템 활용도 향상
- 일관성 유지
- 응답 시간 단축
- 동시에 실행되는 트랜잭션 사이의 간섭 방지

## 로킹 단위

- 로킹(Locking)
- 로킹 단위(Locking Granularity)
- 데이터베이스
- 테이블
- 페이지
- 행

다음 비교표를 포함해주세요.

| 로킹 단위 | 장점 | 단점 |
|---|---|---|
| 큰 로킹 단위 | 로크 수가 적어 관리가 쉬움 | 병행성이 낮음 |
| 작은 로킹 단위 | 병행성이 높음 | 로크 수와 오버헤드 증가 |

**핵심:** 로킹 단위가 작을수록 병행성은 높아지지만 관리 오버헤드는 증가합니다.

## 교착상태

- 교착상태(Deadlock)
- 상호 배제(Mutual Exclusion)
- 점유와 대기(Hold and Wait)
- 비선점(Non-Preemption)
- 환형 대기(Circular Wait)

다음 순서를 강조해주세요.

> 상호 배제 → 점유와 대기 → 비선점 → 환형 대기

네 조건이 모두 성립하면 교착상태가 발생할 수 있다는 점을 설명해주세요.

## 교착상태 처리

- 예방(Prevention)
- 회피(Avoidance)
- 탐지(Detection)
- 회복(Recovery)
- 은행원 알고리즘(Banker’s Algorithm)
- 안전 상태(Safe State)

PDF에는 회피 기법 설명이 다소 모호하므로 다음 내용을 정확하게 작성해주세요.

> 교착상태 회피는 자원을 할당한 뒤의 시스템 상태가 안전 상태인지 검사하고, 안전 상태를 유지할 수 있을 때만 자원을 할당하는 방법이다.

────────────────────
9. 보안 3대 요소
────────────────────

다음 표를 반드시 포함해주세요.

| 요소 | 의미 |
|---|---|
| 기밀성(Confidentiality) | 인가된 사용자만 정보에 접근할 수 있도록 보장 |
| 무결성(Integrity) | 인가되지 않은 변경이나 훼손을 방지 |
| 가용성(Availability) | 인가된 사용자가 필요할 때 정보와 서비스를 이용할 수 있도록 보장 |

암기법:

> **기무가**

**핵심:** “오직 인가된 사용자만 정보를 수정할 수 있다”는 성질은 무결성(Integrity)입니다.

────────────────────
10. 보안 약점과 공격 기법
────────────────────

## 세션 하이재킹

- 세션 하이재킹(Session Hijacking)
- 정상 사용자의 세션을 가로채 권한을 탈취
- 세션 식별자(Session ID)
- 비동기화 상태 탐지
- ACK Storm
- 패킷 유실
- 예상하지 못한 연결 재설정

탐지 방법은 PDF 범위 안에서 정리하되, 공격 수행 절차는 작성하지 마세요.

## SQL 삽입

- SQL 삽입(SQL Injection)
- 사용자 입력값을 통해 악의적인 SQL문을 실행
- 데이터 조회·변조
- 인증 우회
- 입력값 검증
- 준비된 문장(Prepared Statement)
- 매개변수화 질의(Parameterized Query)

## 경로 조작 및 자원 삽입

- 경로 조작(Path Traversal)
- 경로 조작 및 자원 삽입
- 외부 입력값으로 파일 경로를 조작
- 서버 내부 파일에 접근하거나 자원을 수정·삭제
- 허용 목록 기반 검증
- 경로 정규화

## 크로스사이트 스크립팅

- 크로스사이트 스크립팅(XSS, Cross-Site Scripting)
- 웹 페이지에 악성 스크립트 삽입
- 사용자의 브라우저에서 실행
- 쿠키·세션 정보 탈취 가능
- 저장형(Stored)
- 반사형(Reflected)
- DOM 기반(DOM-Based)
- 출력 인코딩과 입력 검증

## 스택 가드

- 스택 가드(Stack Guard)
- 스택 카나리(Stack Canary)
- 복귀 주소와 지역 변수 사이에 특정 값 삽입
- 값이 변조되면 버퍼 오버플로로 판단
- 비정상적인 복귀 주소 사용 차단

────────────────────
11. 접근 지정자
────────────────────

- 접근 지정자(Access Modifier)
- public
- protected
- default
- private

다음 표를 포함해주세요.

| 접근 지정자 | 일반적인 접근 범위 |
|---|---|
| public | 모든 위치에서 접근 가능 |
| protected | 같은 패키지 및 상속 관계에서 접근 가능 |
| default | 같은 패키지에서 접근 가능 |
| private | 해당 클래스 내부에서만 접근 가능 |

이 표는 Java 기준임을 표시해주세요. 다른 언어에서는 접근 범위가 다를 수 있습니다.

────────────────────
12. 암호화와 해시
────────────────────

## 대칭키 암호화

PDF의 “개인키 암호화(Private Key Encryption)”라는 표현은 혼동을 줄 수 있으므로, 본문에서는 다음과 같이 표기해주세요.

> 대칭키 암호화(Symmetric Key Encryption, 관용적으로 개인키 암호화라고도 표현)

특징:

- 암호화와 복호화에 동일한 비밀키 사용
- 처리 속도가 빠름
- 키 배송과 관리가 어려움
- n명의 사용자가 서로 직접 통신하기 위해 필요한 키 수는 일반적으로 다음과 같음

```text
대칭키 수 = n(n - 1) / 2
```

## 블록 암호와 스트림 암호

| 구분 | 의미 | 대표 알고리즘 |
|---|---|---|
| 블록 암호(Block Cipher) | 일정 크기의 데이터 블록 단위로 암호화 | DES, SEED, AES, ARIA, IDEA |
| 스트림 암호(Stream Cipher) | 비트 또는 바이트 단위의 연속된 데이터 스트림 암호화 | RC4, LFSR 기반 방식 |

## 공개키 암호화

- 공개키 암호화(Public Key Encryption)
- 비대칭키 암호화(Asymmetric Key Encryption)
- 공개키(Public Key)
- 개인키(Private Key)
- 서로 다른 한 쌍의 키 사용
- 일반적으로 암호화·서명·키 교환 등에 활용
- n명의 사용자가 각각 한 쌍의 키를 사용하면 전체 키 수는 2n

```text
공개키 체계의 전체 키 수 = 2n
```

원문에 있는 “복호화용 비밀키를 관리자가 관리한다”는 표현을 그대로 쓰지 마세요. 개인키는 일반적으로 해당 키 쌍의 소유자가 비밀리에 관리한다고 설명해주세요.

## 암호화 알고리즘

- DES
- AES
- RSA
- ECC
- Rabin
- SEED
- ARIA

다음처럼 분류해주세요.

| 구분 | 알고리즘 |
|---|---|
| 대칭키 암호 | DES, AES, SEED, ARIA, IDEA |
| 공개키 암호 | RSA, ECC, Rabin |

DES 설명 시 다음 사항을 정확히 작성해주세요.

- 전체 블록 크기는 64비트
- 유효 키 길이는 56비트
- 현재는 짧은 키 길이 때문에 안전한 신규 시스템에서는 사용이 권장되지 않음

AES는 128비트 블록을 사용하며 128·192·256비트 키를 지원한다고 설명해주세요.

RSA는 큰 정수의 소인수분해 어려움에 기반합니다.

ECC는 타원곡선 이산로그 문제에 기반합니다.

## 해시

- 해시(Hash)
- 해시 함수(Hash Function)
- 임의 길이 입력을 고정 길이 결과로 변환
- 일방향성(One-Wayness)
- 충돌 저항성(Collision Resistance)
- 무결성 검증
- 비밀번호 저장

다음 알고리즘을 분류해주세요.

- SHA 계열
- MD4
- MD5
- SNEFRU
- N-HASH

MD4와 MD5는 충돌 취약점 때문에 보안 목적에 권장되지 않는다는 점을 간단히 표시해주세요.

해시는 암호화와 다르며 일반적으로 복호화를 전제로 하지 않는다는 점을 강조해주세요.

## 솔트

- 솔트(Salt)
- 비밀번호나 원문에 무작위 값을 추가한 뒤 해시
- 동일한 비밀번호라도 서로 다른 해시값 생성
- 레인보우 테이블(Rainbow Table) 공격 방어에 도움
- 솔트는 일반적으로 비밀값일 필요는 없지만 사용자마다 고유하고 충분히 무작위여야 함

────────────────────
13. 서비스 거부 공격과 네트워크 공격
────────────────────

공격의 원리를 시험 대비 수준으로만 설명하고, 실제 공격을 수행하기 위한 명령어·코드·구체적인 실행 절차는 작성하지 마세요.

## 죽음의 핑

- 죽음의 핑(Ping of Death)
- 비정상적으로 큰 ICMP 패킷
- 패킷 재조립 과정에서 시스템 오류 유발
- 서비스 거부 공격(DoS)

## 스머핑

- 스머핑(Smurfing)
- 출발지 IP 주소를 공격 대상의 주소로 위조
- 브로드캐스트 네트워크에 ICMP 요청 전송
- 다수의 응답을 공격 대상으로 집중시키는 증폭 공격

## LAND 공격

- LAND 공격
- 송신 IP 주소와 수신 IP 주소를 모두 공격 대상의 주소로 설정
- 공격 대상이 자기 자신과 연결을 시도하도록 유발
- 서비스 장애 유발

## DDoS

- 분산 서비스 거부 공격(DDoS)
- 여러 감염 시스템 또는 공격 지점에서 동시에 공격
- 서버·네트워크 자원 고갈
- 봇넷(Botnet)

PDF에 나온 도구명은 시험용 명칭으로만 간단히 정리해주세요.

- Trin00
- TFN
- TFN2K
- Stacheldraht

공격 도구의 사용법은 절대로 작성하지 마세요.

## 파밍

- 파밍(Pharming)
- 사용자가 정상 주소를 입력해도 가짜 사이트로 유도
- DNS 또는 호스트 정보 변조
- 개인정보와 금융정보 탈취

피싱(Phishing)과 비교해주세요.

| 공격 | 핵심 방식 |
|---|---|
| 피싱(Phishing) | 가짜 메시지나 사이트로 사용자를 속여 접속 유도 |
| 파밍(Pharming) | 주소 변조 등을 통해 정상 주소로 접속해도 가짜 사이트로 이동 |

────────────────────
14. 악성 프로그램
────────────────────

## 웜

- 웜(Worm)
- 네트워크를 통해 스스로 복제
- 다른 파일에 기생하지 않고 독립적으로 확산 가능
- 네트워크와 시스템 자원 소모

바이러스와 간단히 비교해주세요.

| 구분 | 웜(Worm) | 바이러스(Virus) |
|---|---|---|
| 확산 방식 | 네트워크를 통해 스스로 확산 | 보통 다른 파일이나 프로그램에 기생 |
| 사용자 실행 | 없이도 확산 가능 | 감염 파일 실행이 필요한 경우가 많음 |

## 랜섬웨어

- 랜섬웨어(Ransomware)
- 파일이나 시스템을 암호화 또는 잠금
- 복구를 조건으로 금전 요구
- 백업
- 최신 보안 패치
- 의심스러운 첨부파일 주의

────────────────────
15. 시스템 로그
────────────────────

다음 로그를 표로 정리해주세요.

| 로그 | 의미 |
|---|---|
| `wtmp` | 로그인·로그아웃 및 시스템 시작·종료 이력 |
| `utmp` | 현재 로그인한 사용자 정보 |
| `btmp` | 로그인 실패 이력 |
| `lastlog` | 사용자별 마지막 로그인 정보 |

각 로그의 조회에 흔히 사용되는 명령어 이름을 시험 참고 수준으로만 짧게 덧붙일 수 있습니다.

- `last`
- `who`
- `lastb`
- `lastlog`

명령어 사용법을 길게 작성하지 마세요.

────────────────────
16. 인증
────────────────────

- 인증(Authentication)
- 사용자 신원 확인
- 접근 권한 검증
- 인가(Authorization)와 구분

비교표:

| 구분 | 의미 |
|---|---|
| 인증(Authentication) | 사용자가 누구인지 확인 |
| 인가(Authorization) | 인증된 사용자가 무엇을 할 수 있는지 결정 |

인증 요소는 다음 형식으로 정리해주세요.

| 인증 요소 | 의미 | 예시 |
|---|---|---|
| 지식 기반 인증(Something You Know) | 사용자가 알고 있는 정보 | 비밀번호, PIN |
| 소유 기반 인증(Something You Have) | 사용자가 소유한 물건 | OTP, 스마트카드, 보안 토큰 |
| 생체 기반 인증(Something You Are) | 사용자의 신체적 특징 | 지문, 홍채, 얼굴 |
| 행위 기반 인증(Something You Do) | 사용자의 행동 특성 | 서명, 키 입력 패턴, 음성 패턴 |

PDF에는 행위 기반 인증만 일부 제시되어 있을 수 있으나, 생체 기반과 행위 기반을 혼동하지 않도록 정확하게 구분해주세요.

다중요소 인증(MFA)은 서로 다른 인증 요소를 두 가지 이상 결합하는 방식이라고 설명해주세요.

────────────────────
17. 침입 탐지 시스템
────────────────────

- 침입 탐지 시스템(IDS, Intrusion Detection System)
- 비정상적인 사용·오용·공격 탐지
- 오용 탐지(Misuse Detection)
- 이상 탐지(Anomaly Detection)

비교표:

| 탐지 방식 | 기준 | 특징 |
|---|---|---|
| 오용 탐지(Misuse Detection) | 알려진 공격 패턴·서명 | 알려진 공격 탐지에 강하지만 새로운 공격에 약할 수 있음 |
| 이상 탐지(Anomaly Detection) | 정상 행위 기준에서 벗어난 행동 | 새로운 공격을 탐지할 수 있지만 오탐 가능성이 있음 |

IDS와 IPS도 간단히 구분해주세요.

| 시스템 | 역할 |
|---|---|
| 침입 탐지 시스템(IDS) | 공격이나 이상 행위를 탐지하고 알림 |
| 침입 방지 시스템(IPS) | 공격을 탐지하고 차단까지 수행 |

────────────────────
18. SSH
────────────────────

- 시큐어 셸(SSH, Secure Shell)
- 암호화된 원격 로그인
- 원격 명령 실행
- 안전한 파일 전송
- 기본 포트 22
- Telnet보다 안전한 원격 접속 방식

Telnet과 간단히 비교해주세요.

| 구분 | SSH | Telnet |
|---|---|---|
| 전송 보호 | 암호화 | 일반적으로 평문 |
| 기본 포트 | 22 | 23 |
| 보안성 | 높음 | 낮음 |

────────────────────
19. 강조와 암기 표현
────────────────────

시험에 중요한 문장은 다음 형식으로 강조해주세요.

```markdown
**핵심:** 임계 경로(Critical Path)는 프로젝트 완료 시간을 결정하는 가장 긴 경로이다.
```

순서가 있는 내용은 인용문으로 작성해주세요.

```markdown
> 위험 식별 → 위험 분석 및 평가 → 위험 관리 계획 → 위험 감시 및 조치
```

다음 암기 표현을 반드시 포함해주세요.

- 재공학 활동: 분석·재구성·역공학·이식
- CMMI: 초기·관리·정의·정량적 관리·최적화
- CMMI 암기: 초관정정최
- 임계 경로: 가장 긴 경로
- IaaS·PaaS·SaaS
- RIP: 거리 벡터·최대 15홉
- OSPF: 링크 상태·최단 경로
- OLAP: 롤업·드릴다운·피벗·슬라이싱·다이싱
- 로킹 단위가 작음: 병행성 증가·오버헤드 증가
- 교착상태: 상호 배제·점유와 대기·비선점·환형 대기
- 보안 3요소: 기밀성·무결성·가용성
- 보안 3요소 암기: 기무가
- 대칭키: 같은 키·빠른 처리
- 공개키: 공개키와 개인키 한 쌍
- 해시: 고정 길이·일방향
- ARP 등 4과목 내용은 포함하지 않음
- SQL 삽입: 입력값을 통한 비정상 SQL 실행
- XSS: 사용자 브라우저에서 악성 스크립트 실행
- 스택 가드: 카나리값으로 스택 변조 탐지
- 웜: 스스로 복제·네트워크 확산
- IDS: 탐지
- IPS: 탐지와 차단
- SSH: 22번 포트

────────────────────
20. 시험 직전 필수 암기표
────────────────────

문서 마지막에는 반드시 다음 제목을 추가해주세요.

# 시험 직전 필수 암기표

시험 직전 빠르게 확인할 수 있도록 핵심 내용을 2열 표로 정리해주세요.

다음 내용을 포함해주세요.

| 주제 | 핵심 내용 |
|---|---|
| 소프트웨어 재사용(Software Reuse) | 개발 시간·비용 단축, 품질·생산성 향상 |
| 분석(Analysis) | 기존 시스템 이해 및 재공학 대상 선정 |
| 재구성(Restructuring) | 코드와 구조 개선 |
| 역공학(Reverse Engineering) | 기존 시스템에서 분석·설계 정보 추출 |
| 이식(Migration) | 다른 운영체제·하드웨어 환경으로 이전 |
| CASE | 소프트웨어 개발 과정 자동화 지원 |
| LOC | 코드 라인 수 기반 비용 산정 |
| 노력(인월) | 개발 기간 × 투입 인원 |
| COCOMO | LOC 기반 비용 산정 모형 |
| 조직형(Organic Mode) | 작고 비교적 단순한 프로젝트 |
| 반분리형(Semi-Detached Mode) | 조직형과 내장형의 중간 |
| 내장형(Embedded Mode) | 강한 제약이 있는 복잡한 시스템 |
| PERT | 낙관·최빈·비관 시간을 이용한 일정 분석 |
| 임계 경로(Critical Path) | 프로젝트 완료 시간을 결정하는 가장 긴 경로 |
| 간트 차트(Gantt Chart) | 작업 일정을 가로 막대로 표현 |
| 위험 관리 순서 | 식별 → 분석·평가 → 계획 → 감시·조치 |
| CMMI 순서 | 초기 → 관리 → 정의 → 정량적 관리 → 최적화 |
| SDN | 제어 평면과 데이터 평면을 분리하여 소프트웨어로 제어 |
| IaaS | 인프라 제공 |
| PaaS | 개발·실행 플랫폼 제공 |
| SaaS | 완성된 소프트웨어 제공 |
| SSO | 한 번의 인증으로 여러 서비스 이용 |
| RIP | 거리 벡터, 최대 15홉 |
| OSPF | 링크 상태, 최단 경로 |
| Docker | 컨테이너 기반 애플리케이션 실행 환경 |
| 디지털 트윈(Digital Twin) | 현실의 객체를 디지털 공간에 가상화 |
| Hadoop | 대용량 데이터의 분산 저장·처리 |
| Sqoop | RDB와 Hadoop 사이의 대용량 데이터 전송 |
| MapReduce | Map과 Reduce 단계의 분산 처리 |
| OLAP | 다차원 분석과 의사결정 지원 |
| 즉각 갱신(Immediate Update) | 커밋 전에도 DB에 반영 가능 |
| 큰 로킹 단위 | 관리가 쉽지만 병행성 낮음 |
| 작은 로킹 단위 | 병행성이 높지만 오버헤드 증가 |
| 교착상태 조건 | 상호 배제·점유와 대기·비선점·환형 대기 |
| 은행원 알고리즘 | 안전 상태를 유지하는 교착상태 회피 기법 |
| 기밀성(Confidentiality) | 인가된 사용자만 접근 |
| 무결성(Integrity) | 인가되지 않은 변경 방지 |
| 가용성(Availability) | 필요할 때 사용 가능 |
| 세션 하이재킹(Session Hijacking) | 정상 사용자의 세션 탈취 |
| SQL 삽입(SQL Injection) | 악의적인 SQL을 입력해 DB 공격 |
| XSS | 웹 페이지에 악성 스크립트 삽입 |
| 스택 가드(Stack Guard) | 카나리값으로 스택 변조 탐지 |
| 대칭키 암호 | 같은 키로 암호화·복호화 |
| 공개키 암호 | 공개키와 개인키 사용 |
| 블록 암호 | 일정 크기의 블록 단위 암호화 |
| 스트림 암호 | 비트·바이트 스트림 단위 암호화 |
| 해시(Hash) | 임의 길이 입력을 고정 길이 값으로 변환 |
| 솔트(Salt) | 해시 전 무작위 값을 추가 |
| 죽음의 핑(Ping of Death) | 비정상적으로 큰 ICMP 패킷 공격 |
| 스머핑(Smurfing) | ICMP 브로드캐스트 증폭 공격 |
| LAND 공격 | 송신·수신 IP를 공격 대상 주소로 설정 |
| DDoS | 여러 지점에서 동시에 수행하는 서비스 거부 공격 |
| 파밍(Pharming) | 정상 주소로 접속해도 가짜 사이트로 유도 |
| 웜(Worm) | 스스로 복제하며 네트워크로 확산 |
| 랜섬웨어(Ransomware) | 파일 암호화 후 금전 요구 |
| `wtmp` | 로그인·로그아웃 및 시스템 시작·종료 이력 |
| `utmp` | 현재 로그인한 사용자 |
| `btmp` | 로그인 실패 이력 |
| `lastlog` | 마지막 로그인 정보 |
| 인증(Authentication) | 사용자가 누구인지 확인 |
| 인가(Authorization) | 사용자의 허용 권한 결정 |
| 오용 탐지(Misuse Detection) | 알려진 공격 패턴 기반 탐지 |
| 이상 탐지(Anomaly Detection) | 정상 상태와 다른 행위 탐지 |
| IDS | 침입 탐지 |
| IPS | 침입 탐지와 차단 |
| SSH | 암호화된 원격 접속, 기본 포트 22 |

예상문제, 연습문제, 퀴즈, 정답 부분은 절대로 추가하지 마세요.

────────────────────
21. 기술적 정확성 검토
────────────────────

PDF의 내용을 그대로 복사하기 전에 기술적으로 정확한지 검토해주세요.

특히 다음 내용을 확인해주세요.

1. COCOMO 유형은 코드 라인 수뿐 아니라 복잡도와 환경 제약을 함께 고려하는가
2. 임계 경로는 가장 짧은 경로가 아니라 전체 일정상 가장 긴 경로인가
3. SDN은 제어 평면과 데이터 평면을 분리하는 개념인가
4. HSM은 보안 위험을 줄이지만 모든 취약점을 제거하는 것은 아닌가
5. RIP의 최대 유효 홉 수가 15인가
6. OSPF는 거리 벡터가 아니라 링크 상태 라우팅인가
7. BaaS가 이 문서에서는 Blockchain as a Service를 뜻하는가
8. 즉각 갱신은 커밋 전에도 데이터베이스에 반영될 수 있는가
9. 로킹 단위가 작을수록 병행성과 오버헤드가 함께 증가하는가
10. 은행원 알고리즘은 시스템이 안전 상태를 유지할 수 있을 때만 자원을 할당하는가
11. 보안 3요소가 기밀성·무결성·가용성인가
12. SQL 삽입과 XSS의 공격 대상과 동작 위치가 서로 다른가
13. 대칭키 암호와 공개키 암호가 정확하게 구분되었는가
14. n명 사용 시 대칭키 수는 `n(n-1)/2`, 공개키 체계의 전체 키 수는 `2n`인가
15. DES의 블록 크기는 64비트이고 유효 키 길이는 56비트인가
16. AES는 대칭키 블록 암호인가
17. RSA·ECC·Rabin은 공개키 암호인가
18. 해시는 일반적인 암호화·복호화 방식과 다른 일방향 함수인가
19. 솔트는 암호화 자체가 아니라 주로 비밀번호 해시를 강화하기 위한 무작위 값인가
20. LAND, Smurfing, Ping of Death, DDoS가 각각 올바르게 구분되었는가
21. `wtmp`, `utmp`, `btmp`, `lastlog`의 의미가 정확한가
22. 인증과 인가가 정확히 구분되었는가
23. IDS와 IPS가 정확히 구분되었는가
24. SSH의 기본 포트가 22인가

원문의 내용이 부정확하면 본문은 정확한 내용으로 작성하고 필요한 경우 다음 형식으로 짧게 표시해주세요.

```markdown
> **주의:** 원문에서는 대칭키 암호를 개인키 암호화로 표현하지만, 공개키 암호의 개인키와 혼동될 수 있으므로 본문에서는 대칭키 암호(Symmetric Key Encryption)로 표기한다.
```

주의사항을 지나치게 많이 작성하여 학습 흐름을 방해하지 마세요.

────────────────────
22. Markdown 호환성과 품질 검사
────────────────────

완성된 Markdown 파일이 일반적인 Markdown-to-PDF 변환기에서 정상적으로 변환되도록 다음 규칙을 지켜주세요.

- 표준 Markdown 문법만 사용
- 제목은 `#`, `##`, `###` 형식으로 작성
- 표는 파이프(`|`) 기반 표준 Markdown 표로 작성
- 수식은 단순 텍스트 또는 코드 블록으로 작성
- HTML, CSS, JavaScript를 사용하지 않음
- Mermaid 다이어그램을 사용하지 않음
- 이미지 링크나 외부 URL을 임의로 추가하지 않음
- 이모지와 장식용 특수문자를 과도하게 사용하지 않음
- 표의 각 행에서 열 개수가 일치하도록 확인
- 코드 블록의 시작과 종료 백틱이 정확히 대응하도록 확인
- 가로로 지나치게 넓은 표를 만들지 않음
- 한글은 UTF-8 인코딩으로 저장
- 파일 끝에 불필요한 빈 코드 블록을 만들지 않음
- 동일한 제목을 중복하여 작성하지 않음
- 예상문제 섹션을 만들지 않음

파일을 생성한 뒤 다음 사항을 자체 검토해주세요.

1. `5과목 정보시스템 구축 관리` 제목 이후의 내용만 포함했는가
2. 앞부분의 4과목 ARP·CSMA/CD 내용이 섞이지 않았는가
3. PDF 마지막 페이지까지 누락 없이 검토했는가
4. 주요 용어가 `한글(English)` 형식으로 작성되었는가
5. 영어 용어를 별도 열로 분리한 표가 없는가
6. 소프트웨어 재공학의 네 활동이 모두 포함되었는가
7. LOC 공식과 COCOMO 유형이 정확한가
8. PERT와 임계 경로 설명이 정확한가
9. CMMI 5단계 순서가 정확한가
10. 클라우드 서비스 유형이 정확히 구분되었는가
11. RIP와 OSPF가 정확히 비교되었는가
12. OLAP 연산이 누락되지 않았는가
13. 데이터베이스 회복·병행제어·교착상태가 포함되었는가
14. 보안 3요소가 정확한가
15. 대칭키·공개키·해시가 서로 구분되었는가
16. 보안 공격 설명에 실제 공격 실행법이나 악성 코드가 포함되지 않았는가
17. 시스템 로그와 인증 방식이 정확한가
18. IDS와 IPS가 구분되었는가
19. 예상문제·연습문제·퀴즈·정답이 없는가
20. Markdown 표와 코드 블록 문법에 오류가 없는가
21. UTF-8로 저장되었는가
22. 실제 `.md` 파일이 생성되었는가

검토 과정에서 문제가 발견되면 직접 수정한 뒤 최종 파일을 제공해주세요.