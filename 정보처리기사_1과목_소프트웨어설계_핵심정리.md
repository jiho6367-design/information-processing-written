# 정보처리기사 필기 1과목 — 소프트웨어 설계 핵심 정리

> 소프트웨어 생명주기 → 애자일 개발 → 요구사항 분석 → UML → 사용자 인터페이스 → 모듈 설계 → 객체지향 설계 → 디자인 패턴 → 요구사항 검증 → 미들웨어

이 문서는 정보처리기사 필기시험 직전 복습용 학습 노트입니다. 주요 용어는 한글(English) 형식으로 함께 표기했습니다.

# 1. 소프트웨어 공학

소프트웨어 공학(Software Engineering)은 소프트웨어를 체계적으로 개발하고 유지보수하는 방법과 기술입니다.

- 현대적인 프로그래밍 기술을 지속적으로 적용합니다.
- 개발 과정 전체에서 품질을 계속 검증합니다.
- 개발 과정과 결과를 문서로 명확하게 기록합니다.

# 2. 소프트웨어 생명주기 모형

| 모형 | 핵심 특징 |
|---|---|
| 폭포수 모형(Waterfall Model) | 단계를 순차적으로 진행하며 이전 단계로 돌아가기 어렵습니다. |
| 프로토타입 모형(Prototyping Model) | 견본을 먼저 만들어 불명확한 요구사항을 확인합니다. |
| 나선형 모형(Spiral Model) | 위험 분석을 중심으로 반복하며 점진적으로 개발합니다. |
| 애자일 모형(Agile Model) | 짧은 반복과 고객 협업으로 변화에 대응합니다. |

## 2-1. 폭포수 모형(Waterfall Model)

요구사항 분석, 설계, 구현, 시험처럼 각 단계를 끝낸 뒤 다음 단계로 진행하는 방식입니다. 단계별 문서화에는 유리하지만 요구사항이 바뀌면 대응하기 어렵습니다.

## 2-2. 프로토타입 모형(Prototyping Model)

실제 제품을 만들기 전에 견본인 프로토타입(Prototype)을 제작합니다. 사용자가 견본을 확인하면서 요구사항을 구체화할 수 있으므로 요구가 모호할 때 적합합니다.

## 2-3. 나선형 모형(Spiral Model)

위험 분석(Risk Analysis)을 중심으로 개발 단계를 여러 번 반복합니다.

> 계획 수립 → 위험 분석 → 개발 및 검증 → 고객 평가

**핵심:** 나선형 모형(Spiral Model)은 위험 분석을 중심으로 반복 개발합니다.

# 3. 애자일 개발

애자일(Agile)은 짧은 개발 주기를 반복하면서 고객 의견과 변경 요구를 빠르게 반영하는 개발 방식입니다.

## 3-1. 애자일 선언의 핵심 가치

- 프로세스와 도구보다 개인과 상호작용
- 방대한 문서보다 실행되는 소프트웨어
- 계약 협상보다 고객과 협업
- 계획 준수보다 변화에 대응

## 3-2. 주요 애자일 방법론

| 방법론 | 핵심 |
|---|---|
| 스크럼(Scrum) | 역할과 스프린트(Sprint)를 중심으로 개발합니다. |
| 익스트림 프로그래밍(XP, eXtreme Programming) | 소통, 단순한 설계, 빠른 피드백을 중시합니다. |
| 기능 중심 개발(FDD, Feature-Driven Development) | 기능 단위로 설계와 개발을 진행합니다. |
| 칸반(Kanban) | 작업 흐름을 시각화하여 진행 중인 작업을 관리합니다. |
| 린(Lean) | 낭비를 줄이고 가치 있는 작업에 집중합니다. |

스크럼(Scrum)의 스프린트(Sprint)는 보통 2~4주 정도의 짧은 개발 주기입니다. 일일 스크럼(Daily Scrum)으로 진행 상황을 공유합니다.

## 3-3. XP의 핵심 가치

> 의사소통 → 단순성 → 피드백 → 용기 → 존중

| 가치 | 의미 |
|---|---|
| 의사소통(Communication) | 개발자, 고객, 팀원 사이에서 정보를 자주 공유합니다. |
| 단순성(Simplicity) | 현재 필요한 가장 단순한 해결책을 우선 선택합니다. |
| 피드백(Feedback) | 시험, 고객 의견, 팀 검토를 통해 빠르게 개선합니다. |
| 용기(Courage) | 문제가 있으면 코드를 개선하거나 계획을 바꿉니다. |
| 존중(Respect) | 팀원의 역할과 기여를 존중하며 협업합니다. |

## 3-4. 리팩토링(Refactoring)

리팩토링은 외부 기능을 바꾸지 않고 내부 구조를 개선하는 작업입니다. 중복 코드를 제거하거나, 긴 함수를 나누거나, 이해하기 어려운 이름을 개선하는 것이 예입니다.

**핵심:** 리팩토링(Refactoring)은 새 기능을 추가하는 작업이 아니라 기존 기능을 유지하며 구조를 개선하는 작업입니다.

# 4. 요구사항의 종류

요구사항(Requirement)은 사용자가 시스템에 기대하는 기능과 조건입니다.

| 구분 | 의미 |
|---|---|
| 기능 요구사항(Functional Requirement) | 시스템이 무엇을 해야 하는지를 나타냅니다. |
| 비기능 요구사항(Non-Functional Requirement) | 성능, 보안, 품질, 제약, 인터페이스 같은 품질 조건을 나타냅니다. |

회원가입과 상품 검색은 기능 요구사항의 예입니다. 2초 안에 결과를 보여 주거나 개인정보를 암호화하는 조건은 비기능 요구사항의 예입니다.

# 5. 요구사항 개발 프로세스

> 도출(Elicitation) → 분석(Analysis) → 명세(Specification) → 확인(Validation)

| 단계 | 역할 |
|---|---|
| 도출(Elicitation) | 사용자, 문서, 현장 조사 등을 통해 요구사항을 수집합니다. |
| 분석(Analysis) | 충돌, 누락, 타당성, 비용, 일정 등을 검토합니다. |
| 명세(Specification) | 합의된 요구사항을 문서로 정확하게 작성합니다. |
| 확인(Validation) | 요구사항이 사용자 요구와 일치하고 오류가 없는지 검증합니다. |

# 6. 요구사항 분석 도구

## 6-1. 자료 흐름도(DFD, Data Flow Diagram)

자료 흐름도는 시스템 안에서 데이터가 어디서 들어와 어떻게 처리되고 어디로 나가는지를 표현합니다.

| 구성 요소 | 의미 |
|---|---|
| 프로세스(Process) | 입력 자료를 처리하여 출력 자료로 변환합니다. |
| 자료 흐름(Data Flow) | 데이터가 이동하는 경로입니다. |
| 자료 저장소(Data Store) | 데이터가 저장되는 장소입니다. |
| 단말(Terminator) | 시스템 외부의 데이터 발생 주체 또는 수신 주체입니다. |

## 6-2. 그 밖의 분석 도구

| 도구 | 용도 |
|---|---|
| 자료 사전(Data Dictionary) | 데이터의 이름, 형식, 길이, 의미를 정의합니다. |
| 소단위 명세서(Mini-Specification) | 복잡한 처리 절차를 세부적으로 설명합니다. |
| 개체 관계도(ERD, Entity-Relationship Diagram) | 데이터 개체와 관계를 표현합니다. |
| 상태 전이도(State Transition Diagram) | 사건에 따라 상태가 어떻게 바뀌는지 표현합니다. |
| 제어 명세서(Control Specification) | 제어 조건과 처리 규칙을 설명합니다. |

## 6-3. HIPO

HIPO(Hierarchy plus Input-Process-Output)는 하향식(Top-Down) 개발을 위한 문서화 도구입니다.

- 기능을 계층 구조로 표현합니다.
- 기호와 도표를 사용하여 이해하기 쉽습니다.
- 기능과 자료의 의존 관계를 함께 표현할 수 있습니다.

# 7. UML

통합 모델링 언어(UML, Unified Modeling Language)는 객체지향 시스템을 분석하고 설계할 때 사용하는 표준 모델링 언어입니다. 개발자와 고객이 시스템을 같은 방식으로 이해하도록 돕습니다.

> 사물(Things) + 관계(Relationships) + 다이어그램(Diagrams)

스테레오타입(Stereotype)은 UML 기본 요소에 추가 의미를 부여하는 표기입니다. 보통 길러멧(Guillemet)인 이중 꺾쇠 사이에 쓰며, 표준 표기 예는 `«interface»`, ASCII 대체 표기는 `<<interface>>`입니다.

# 8. UML의 관계

| 관계 | 의미 |
|---|---|
| 연관 관계(Association) | 객체들이 서로 연결되어 있음을 나타냅니다. |
| 집합 관계(Aggregation) | 전체와 부분의 관계이며 부분 객체는 전체와 독립적으로 존재할 수 있습니다. |
| 포함 관계(Composition) | 강한 전체-부분 관계이며 전체가 사라지면 부분도 함께 사라집니다. |
| 일반화 관계(Generalization) | 일반적인 것과 구체적인 것의 관계로 상속과 관련됩니다. |
| 의존 관계(Dependency) | 한 요소가 다른 요소를 일시적으로 사용하여 영향을 받는 관계입니다. |
| 실체화 관계(Realization) | 인터페이스나 추상 기능을 실제 클래스가 구현하는 관계입니다. |

일반화 관계(Generalization)는 is-a 관계로 이해하면 쉽습니다. 예를 들어 자동차는 교통수단의 한 종류입니다.

# 9. UML 다이어그램

## 9-1. 구조적 다이어그램(Structural Diagram)

구조적 다이어그램은 시스템의 정적인 구조를 표현합니다.

- 클래스 다이어그램(Class Diagram)
- 객체 다이어그램(Object Diagram)
- 컴포넌트 다이어그램(Component Diagram)
- 배치 다이어그램(Deployment Diagram)
- 복합체 구조 다이어그램(Composite Structure Diagram)
- 패키지 다이어그램(Package Diagram)

## 9-2. 행위 다이어그램(Behavioral Diagram)

행위 다이어그램은 시스템의 동적인 동작과 상호작용을 표현합니다.

- 유스케이스 다이어그램(Use Case Diagram)
- 순차 다이어그램(Sequence Diagram)
- 커뮤니케이션 다이어그램(Communication Diagram)
- 상태 다이어그램(State Diagram)
- 활동 다이어그램(Activity Diagram)
- 상호작용 개요 다이어그램(Interaction Overview Diagram)
- 타이밍 다이어그램(Timing Diagram)

**핵심:** 구조적 다이어그램은 정적 구조, 행위 다이어그램은 동적 동작을 표현합니다.

# 10. 유스케이스와 순차 다이어그램

## 10-1. 유스케이스 다이어그램(Use Case Diagram)

유스케이스 다이어그램은 외부 사용자가 시스템에서 어떤 기능을 사용하는지 표현합니다.

| 요소 | 의미 |
|---|---|
| 액터(Actor) | 시스템과 상호작용하는 사람, 외부 시스템, 기관입니다. |
| 주 액터(Primary Actor) | 시스템을 사용하여 직접적인 이익을 얻는 대상입니다. |
| 부 액터(Secondary Actor) | 주 액터의 목표를 위해 서비스를 제공하는 외부 대상입니다. |

## 10-2. 순차 다이어그램(Sequence Diagram)

순차 다이어그램은 시간 흐름에 따라 객체 사이에 메시지가 전달되는 순서를 표현합니다.

| 구성 요소 | 의미 |
|---|---|
| 액터(Actor) | 시스템 밖에서 상호작용을 시작하는 대상입니다. |
| 객체(Object) | 메시지를 주고받는 시스템 내부 대상입니다. |
| 생명선(Lifeline) | 시간 흐름에 따라 객체가 존재함을 나타내는 세로선입니다. |
| 실행 상자(Activation Box) | 객체가 실제 처리를 수행하는 기간입니다. |
| 메시지(Message) | 객체에 특정 동작을 요청하는 전달 내용입니다. |

# 11. 사용자 인터페이스

사용자 인터페이스(UI, User Interface)는 사용자와 시스템이 상호작용하는 접점입니다.

| 인터페이스 | 특징 |
|---|---|
| 명령줄 인터페이스(CLI, Command Line Interface) | 텍스트 명령어를 입력하여 조작합니다. |
| 그래픽 사용자 인터페이스(GUI, Graphical User Interface) | 아이콘, 메뉴, 창, 포인터로 조작합니다. |
| 자연어 사용자 인터페이스(NUI, Natural User Interface) | 말, 동작, 표정 같은 자연스러운 행동으로 조작합니다. |

## 11-1. 사용자 인터페이스의 특징

- 사용자의 편의성과 가독성을 높여줍니다.
- 작업 시간을 단축시킵니다.
- 업무에 대한 이해도를 높여줍니다.
- 사용자 중심으로 설계되어 있습니다.

# 12. UI 설계 원칙과 프로토타입 도구

## 12-1. UI 기본 원칙

| 원칙 | 의미 |
|---|---|
| 직관성(Intuitiveness) | 사용자가 설명 없이도 쉽게 이해하고 사용할 수 있어야 합니다. |
| 유효성(Effectiveness) | 사용자가 목적을 정확하고 완전하게 달성할 수 있어야 합니다. |
| 학습성(Learnability) | 사용 방법을 쉽게 배우고 익힐 수 있어야 합니다. |
| 유연성(Flexibility) | 사용자의 다양한 요구와 작업 방식에 대응할 수 있어야 합니다. |

## 12-2. 화면 설계 도구

| 도구 | 특징 |
|---|---|
| 와이어프레임(Wireframe) | 화면의 기본 구조와 배치를 간단히 표현합니다. |
| 목업(Mockup) | 실제 화면과 유사한 정적 모형입니다. |
| 스토리보드(Storyboard) | 화면 구성과 화면 사이의 이동 흐름을 문서화합니다. |
| 프로토타입(Prototype) | 일부 상호작용을 실제처럼 구현한 동적 모형입니다. |

# 13. 소프트웨어 품질 표준

ISO/IEC 25000은 ISO/IEC 9126과 ISO/IEC 14598을 통합한 소프트웨어 품질 평가 체계이며, SQuaRE(Software Quality Requirements and Evaluation)라고도 합니다.

| 품질 특성 | 의미 |
|---|---|
| 기능성(Functionality) | 요구사항에 맞는 기능을 정확하게 제공하는 정도입니다. |
| 신뢰성(Reliability) | 정해진 조건에서 오류 없이 기능을 수행하는 정도입니다. |
| 사용성(Usability) | 사용자가 쉽게 배우고 사용할 수 있는 정도입니다. |
| 효율성(Efficiency) | 시간과 자원을 효율적으로 사용하는 정도입니다. |
| 유지보수성(Maintainability) | 오류 수정과 기능 변경을 쉽게 할 수 있는 정도입니다. |
| 이식성(Portability) | 다른 환경으로 쉽게 옮겨 사용할 수 있는 정도입니다. |

> 기능성 → 신뢰성 → 사용성 → 효율성 → 유지보수성 → 이식성

# 14. 모듈화와 추상화

모듈(Module)은 독립적으로 컴파일·시험할 수 있는 하나의 기능 단위입니다. 모듈화(Modularity)는 큰 시스템을 여러 기능 단위로 나누는 작업입니다.

- 기능을 분리하면 관리와 재사용이 쉬워집니다.
- 오류가 다른 부분으로 퍼지는 영향을 줄일 수 있습니다.
- 모듈을 너무 작게 나누면 모듈 수와 통합 비용이 증가합니다.
- 모듈을 너무 크게 나누면 하나의 모듈이 복잡해져 개발과 유지보수가 어려워집니다.

| 추상화 유형 | 의미 |
|---|---|
| 과정 추상화(Procedural Abstraction) | 수행 절차를 하나의 의미 있는 기능으로 묶습니다. |
| 데이터 추상화(Data Abstraction) | 데이터와 관련 연산을 하나의 개념으로 표현합니다. |
| 제어 추상화(Control Abstraction) | 복잡한 제어 구조를 단순한 형태로 표현합니다. |

하향식 설계(Top-Down Design)는 상위 기능부터 시작하여 하위 기능으로 점차 나누는 설계 방식입니다. 대표 전략은 단계적 분해(Stepwise Refinement)입니다.

# 15. 정보 은닉과 캡슐화

정보 은닉(Information Hiding)은 모듈 내부의 자료와 처리 방법을 외부에서 직접 알거나 변경하지 못하게 숨기는 방법입니다. 객체지향 언어에서는 private 접근 제어자가 대표적인 예입니다.

캡슐화(Encapsulation)는 데이터와 그 데이터를 처리하는 함수를 하나로 묶는 성질입니다. 캡슐화는 정보 은닉을 실현하는 데 도움을 줍니다.

# 16. 소프트웨어 아키텍처 패턴

## 16-1. 파이프-필터 패턴(Pipe-Filter Pattern)

각 처리 단계인 필터(Filter)가 데이터를 변환하고 결과를 파이프(Pipe)로 다음 필터에 전달합니다.

> 입력 → 필터 1 → 필터 2 → 필터 3 → 출력

처리 단계의 독립성과 재사용성이 높지만 데이터 형식을 계속 변환하면 오버헤드가 생길 수 있습니다.

## 16-2. 모델-뷰-컨트롤러(MVC, Model-View-Controller)

| 구성 요소 | 역할 |
|---|---|
| 모델(Model) | 핵심 데이터와 비즈니스 로직을 관리합니다. |
| 뷰(View) | 사용자에게 화면과 정보를 보여 줍니다. |
| 컨트롤러(Controller) | 사용자 입력을 처리하고 모델과 뷰를 제어합니다. |

# 17. 객체지향 기본 개념

| 개념 | 의미 |
|---|---|
| 객체(Object) | 데이터와 기능을 가진 실제 대상입니다. |
| 클래스(Class) | 공통 속성과 기능을 가진 객체들의 설계도입니다. |
| 인스턴스(Instance) | 클래스로부터 만들어진 구체적인 객체입니다. |
| 메시지(Message) | 객체에게 특정 동작을 요청하는 명령입니다. |
| 상속(Inheritance) | 자식 클래스가 부모 클래스의 속성과 기능을 물려받는 성질입니다. |
| 다형성(Polymorphism) | 같은 요청이 상황이나 객체에 따라 다르게 동작하는 성질입니다. |

| 구분 | 의미 |
|---|---|
| 오버로딩(Overloading) | 같은 이름의 메소드를 매개변수의 개수나 형식을 다르게 하여 여러 개 정의합니다. |
| 오버라이딩(Overriding) | 부모 클래스의 메소드를 자식 클래스에서 자신의 동작에 맞게 다시 정의합니다. |

# 18. 럼바우 분석 기법

럼바우(Rumbaugh)의 객체지향 분석은 객체, 동적, 기능 모델링으로 구성됩니다.

> 객체 → 동적 → 기능

| 모델링 | 주요 표현 도구 |
|---|---|
| 객체 모델링(Object Modeling) | 객체 다이어그램으로 객체의 속성과 관계를 표현합니다. |
| 동적 모델링(Dynamic Modeling) | 상태 다이어그램으로 시간에 따른 상태 변화를 표현합니다. |
| 기능 모델링(Functional Modeling) | 자료 흐름도로 자료의 흐름과 처리를 표현합니다. |

# 19. 객체지향 설계 원칙 SOLID

| 원칙 | 의미 |
|---|---|
| 단일 책임 원칙(SRP, Single Responsibility Principle) | 클래스는 하나의 책임에 집중해야 합니다. |
| 개방-폐쇄 원칙(OCP, Open-Closed Principle) | 기능 확장에는 열려 있고 기존 코드 변경에는 닫혀 있어야 합니다. |
| 리스코프 치환 원칙(LSP, Liskov Substitution Principle) | 자식 클래스는 부모 클래스가 사용되는 자리에 대체될 수 있어야 합니다. |
| 인터페이스 분리 원칙(ISP, Interface Segregation Principle) | 사용하지 않는 기능까지 의존하지 않도록 인터페이스를 작게 나눕니다. |
| 의존 역전 원칙(DIP, Dependency Inversion Principle) | 구체적인 구현보다 추상화에 의존하도록 설계합니다. |

# 20. 결합도와 응집도

**핵심:** 좋은 모듈 설계는 결합도(Coupling)는 낮고 응집도(Cohesion)는 높습니다.

## 20-1. 결합도(Coupling)

결합도는 모듈과 모듈 사이의 의존 정도입니다. 약할수록 좋은 설계입니다.

> 자료 → 스탬프 → 제어 → 외부 → 공통 → 내용

| 결합도 | 의미 |
|---|---|
| 자료 결합도(Data Coupling) | 필요한 단순 데이터만 전달합니다. |
| 스탬프 결합도(Stamp Coupling) | 배열이나 레코드 같은 자료구조 전체를 전달합니다. |
| 제어 결합도(Control Coupling) | 제어 플래그나 제어 정보를 전달합니다. |
| 외부 결합도(External Coupling) | 외부 장치, 파일 형식, 통신 규약 등에 함께 의존합니다. |
| 공통 결합도(Common Coupling) | 여러 모듈이 공통 데이터 영역을 공유합니다. |
| 내용 결합도(Content Coupling) | 다른 모듈의 내부 자료나 처리 내용을 직접 참조하거나 수정합니다. |

## 20-2. 응집도(Cohesion)

응집도는 한 모듈 안의 요소들이 하나의 목적을 위해 얼마나 밀접하게 관련되는지를 나타냅니다. 강할수록 좋은 설계입니다.

> 우연적 → 논리적 → 시간적 → 절차적 → 통신적 → 순차적 → 기능적

| 응집도 | 의미 |
|---|---|
| 우연적 응집도(Coincidental Cohesion) | 관련 없는 기능을 우연히 한 모듈에 모은 상태입니다. |
| 논리적 응집도(Logical Cohesion) | 논리적으로 비슷한 기능을 한 모듈에 모읍니다. |
| 시간적 응집도(Temporal Cohesion) | 같은 시점에 수행되는 기능을 모읍니다. |
| 절차적 응집도(Procedural Cohesion) | 정해진 순서대로 실행되는 기능을 모읍니다. |
| 통신적 응집도(Communicational Cohesion) | 같은 입력 또는 출력을 사용하는 기능을 모읍니다. |
| 순차적 응집도(Sequential Cohesion) | 앞 기능의 출력이 다음 기능의 입력이 됩니다. |
| 기능적 응집도(Functional Cohesion) | 하나의 명확한 기능만 수행합니다. |

> **주의:** 원문에서 교환적 응집도라고 부른 항목은 일반적으로 통신적 응집도(Communicational Cohesion)로 표현합니다.

# 21. 효과적인 모듈 설계

- 결합도는 낮게, 응집도는 높게 유지합니다.
- 제어 흐름을 지나치게 복잡하게 만들지 않습니다.
- 모듈의 인터페이스와 역할을 문서로 명확히 합니다.
- 재사용과 유지보수가 가능하도록 모듈의 책임을 분명히 합니다.

나시-슈나이더만 차트(NS Chart)는 프로그램의 논리 구조를 도형으로 표현하는 도구입니다.

- 순차, 선택, 반복 구조를 표현합니다.
- GOTO문이나 화살표를 사용하지 않습니다.
- 구조적 프로그램을 명확하게 표현하고 코드로 옮기기 쉽습니다.

## 21-1. 소프트웨어 재사용(Software Reuse)

소프트웨어 재사용은 이미 개발된 기능을 새로운 시스템이나 기능 개발에 활용하는 것입니다. 검증된 기능을 다시 사용하여 개발 시간과 노력을 줄일 수 있습니다.

| 재사용 규모 | 재사용 단위 |
|---|---|
| 함수(Function) | 하나의 기능을 수행하는 함수 |
| 객체(Object) | 데이터와 기능을 묶은 객체 |
| 컴포넌트(Component) | 독립적으로 결합할 수 있는 소프트웨어 구성요소 |
| 애플리케이션(Application) | 완성된 응용 소프트웨어 전체 |

# 22. 디자인 패턴

디자인 패턴(Design Pattern)은 설계에서 반복적으로 발생하는 문제를 해결하기 위한 검증된 설계 방법입니다.

> 생성 패턴 → 구조 패턴 → 행위 패턴

## 22-1. 생성 패턴(Creational Pattern)

| 패턴 | 핵심 의미 |
|---|---|
| 추상 팩토리(Abstract Factory) | 관련된 객체들의 집합을 생성합니다. |
| 빌더(Builder) | 복잡한 객체를 단계적으로 조립합니다. |
| 팩토리 메소드(Factory Method) | 객체 생성 책임을 하위 클래스에 위임합니다. |
| 프로토타입(Prototype) | 기존 객체를 복제하여 새 객체를 만듭니다. |
| 싱글톤(Singleton) | 클래스의 인스턴스를 하나만 만들고 공통으로 접근하게 합니다. |

## 22-2. 구조 패턴(Structural Pattern)

| 패턴 | 핵심 의미 |
|---|---|
| 어댑터(Adapter) | 호환되지 않는 인터페이스를 연결합니다. |
| 브리지(Bridge) | 추상화와 구현을 분리하여 각각 바꿀 수 있게 합니다. |
| 컴포지트(Composite) | 단일 객체와 복합 객체를 같은 방식으로 다룹니다. |
| 데코레이터(Decorator) | 객체에 기능을 동적으로 추가합니다. |
| 퍼사드(Facade) | 복잡한 하위 시스템에 단순한 통합 인터페이스를 제공합니다. |
| 플라이웨이트(Flyweight) | 공유 가능한 객체를 재사용하여 메모리를 절약합니다. |
| 프록시(Proxy) | 실제 객체를 대신하여 접근을 제어합니다. |

## 22-3. 행위 패턴(Behavioral Pattern)

| 패턴 | 핵심 의미 |
|---|---|
| 책임 연쇄(Chain of Responsibility) | 처리하지 못한 요청을 다음 처리자에게 넘깁니다. |
| 커맨드(Command) | 요청을 객체로 캡슐화합니다. |
| 인터프리터(Interpreter) | 언어의 문법이나 표현식을 해석합니다. |
| 반복자(Iterator) | 내부 구조를 노출하지 않고 순차 접근합니다. |
| 중재자(Mediator) | 객체 사이의 복잡한 상호작용을 중재합니다. |
| 메멘토(Memento) | 객체의 이전 상태를 저장하고 복원합니다. |
| 옵서버(Observer) | 상태 변화가 발생하면 관련 객체에 알립니다. |
| 상태(State) | 객체 상태에 따라 동작을 바꿉니다. |
| 전략(Strategy) | 알고리즘을 교체하여 사용할 수 있게 합니다. |
| 템플릿 메소드(Template Method) | 전체 처리 순서는 상위 클래스에 두고 세부 구현을 하위 클래스에 맡깁니다. |
| 방문자(Visitor) | 데이터 구조와 처리 기능을 분리합니다. |

# 23. 요구사항 검증

요구사항 검증(Requirements Validation)은 요구사항 명세가 정확하고 완전하며 실현 가능한지 확인하는 활동입니다.

| 검토 방식 | 특징 |
|---|---|
| 동료 검토(Peer Review) | 작성자가 설명하면서 동료와 함께 결함을 찾습니다. |
| 워크스루(Walkthrough) | 검토 자료를 미리 배포한 뒤 참여자가 함께 검토합니다. |
| 인스펙션(Inspection) | 작성자를 제외한 전문가가 절차에 따라 공식적으로 검토합니다. |

**핵심:** 인스펙션(Inspection)은 공식적인 검토 방법입니다.

# 24. 미들웨어

미들웨어(Middleware)는 운영체제와 응용 프로그램 사이에서 공통 서비스를 제공하고, 서로 다른 시스템을 연결하는 소프트웨어입니다.

- 서로 다른 기종과 분산 환경의 연결을 돕습니다.
- 응용 프로그램이 통신과 데이터 처리 같은 공통 기능을 쉽게 사용하게 합니다.
- 실제 위치를 몰라도 서비스를 이용하게 하는 위치 투명성(Location Transparency)을 지원할 수 있습니다.

| 미들웨어 | 의미 |
|---|---|
| 데이터베이스 미들웨어(DB Middleware) | 응용 프로그램과 데이터베이스의 연결을 지원합니다. |
| 원격 프로시저 호출(RPC, Remote Procedure Call) | 원격 함수를 로컬 함수처럼 호출합니다. |
| 메시지 지향 미들웨어(MOM, Message Oriented Middleware) | 비동기 방식으로 메시지를 전달합니다. |
| 트랜잭션 처리 모니터(TP-Monitor) | 대량 트랜잭션 처리와 감시를 지원합니다. |
| 객체 요청 브로커(ORB, Object Request Broker) | 분산 객체 사이의 통신을 지원합니다. |
| 웹 애플리케이션 서버(WAS, Web Application Server) | 동적 웹 콘텐츠와 비즈니스 로직 실행을 지원합니다. |

# 시험 직전 필수 암기표

| 주제 | 핵심 내용 |
|---|---|
| 폭포수 모형(Waterfall Model) | 단계별 순차 진행, 변경 대응이 어려움 |
| 프로토타입 모형(Prototyping Model) | 견본을 통해 요구사항 확인 |
| 나선형 모형(Spiral Model) | 위험 분석 중심의 반복 개발 |
| 애자일(Agile) | 협업과 변화 대응 중시 |
| 요구사항 개발 순서 | 도출 → 분석 → 명세 → 확인 |
| 리팩토링(Refactoring) | 기능 변경 없이 내부 구조 개선 |
| DFD 프로세스(Process) | 데이터를 처리하여 변환 |
| HIPO | 하향식 문서화 도구 |
| UML | 객체지향 분석과 설계의 표준 모델링 언어 |
| 구조적 다이어그램 | 시스템의 정적 구조 표현 |
| 행위 다이어그램 | 시스템의 동적 동작 표현 |
| 액터(Actor) | 시스템과 상호작용하는 외부 요소 |
| CLI | 텍스트 명령 방식 |
| GUI | 그래픽 조작 방식 |
| NUI | 말과 행동으로 조작 |
| UI 기본 원칙 | 직관성·유효성·학습성·유연성 |
| 목업(Mockup) | 실제 화면과 유사한 정적 모형 |
| SQuaRE | ISO/IEC 25000 품질 평가 체계 |
| 정보 은닉(Information Hiding) | 모듈 내부 정보의 외부 접근 제한 |
| 캡슐화(Encapsulation) | 데이터와 기능을 하나로 묶음 |
| 상속(Inheritance) | 부모 클래스의 속성과 기능을 물려받음 |
| 오버로딩(Overloading) | 매개변수를 다르게 하여 같은 이름을 사용 |
| 오버라이딩(Overriding) | 부모 메소드를 자식이 재정의 |
| 럼바우(Rumbaugh) | 객체·동적·기능 모델링 |
| SOLID | 객체지향 설계 5원칙 |
| 좋은 모듈 | 낮은 결합도·높은 응집도 |
| 가장 약한 결합도 | 자료 결합도(Data Coupling) |
| 가장 강한 결합도 | 내용 결합도(Content Coupling) |
| 가장 약한 응집도 | 우연적 응집도(Coincidental Cohesion) |
| 가장 강한 응집도 | 기능적 응집도(Functional Cohesion) |
| 싱글톤(Singleton) | 인스턴스를 하나만 생성 |
| 어댑터(Adapter) | 호환되지 않는 인터페이스 변환 |
| 옵서버(Observer) | 상태 변경을 관련 객체에 통보 |
| 전략(Strategy) | 알고리즘을 교환할 수 있게 구성 |
| 인스펙션(Inspection) | 공식적인 검토 방법 |
| RPC | 원격 프로시저를 로컬처럼 호출 |
| MOM | 비동기 메시지 전달 |
| WAS | 동적 웹 콘텐츠와 비즈니스 로직 처리 |
