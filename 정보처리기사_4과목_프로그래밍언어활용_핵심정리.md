# 정보처리기사 필기 4과목 — 프로그래밍 언어 활용 핵심 정리

> 프로그래밍 언어 기초 → 연산자와 제어문 → 배열과 포인터 → Python → 라이브러리와 예외 처리 → 운영체제 → 프로세스와 스케줄링 → 네트워크

이 문서는 원본 PDF의 4과목 시작 부분부터 5과목 시작 직전까지를 시험 대비용으로 재구성한 정리본입니다. 원문에 있는 핵심 개념을 중심으로 설명하되, 명백하게 부정확하거나 오해하기 쉬운 내용은 주의사항으로 바로잡았습니다.

# 1. 프로그래밍 언어의 자료형

자료형(Data Type)은 변수에 어떤 종류의 값을 저장할지 정하는 규칙입니다. 자료형에 따라 표현할 수 있는 값, 필요한 메모리 크기, 사용할 수 있는 연산이 달라집니다.

| 언어 | 종류 | 자료형 | 크기와 표현의 주의점 |
|---|---|---|---|
| C | 문자형(Character Type) | `char` | 크기는 1바이트이지만 1바이트의 비트 수는 구현 환경의 영향을 받음 |
| C | 정수형(Integer Type) | `short`, `int`, `long` 등 | 정확한 크기는 구현 환경에 따라 달라질 수 있음 |
| C | 논리형(Boolean Type) | `_Bool`, `stdbool.h`의 `bool` | `_Bool`은 기본 자료형이고 `bool`은 `_Bool`을 가리키는 매크로 |
| Java | 문자형(Character Type) | `char` | 16비트 UTF-16 코드 단위를 저장 |
| Java | 정수형(Integer Type) | `byte`, `short`, `int`, `long` | 각 자료형의 크기가 언어 규칙으로 정해짐 |
| Java | 논리형(Boolean Type) | `boolean` | `true`와 `false`를 저장하며 저장 크기는 단순히 1바이트로 고정하지 않음 |

> **주의:** 원문은 C와 Java의 `char`를 모두 1Byte로 표시하지만 Java의 `char`는 16비트 UTF-16 코드 단위를 저장합니다. C 자료형의 정확한 크기는 구현 환경에 따라 달라질 수 있고, Java의 `boolean` 저장 크기도 언어 명세에서 단순히 1Byte로 고정하지 않습니다.

**핵심:** 시험에서 자료형의 일반적인 크기를 묻더라도 언어가 C인지 Java인지 먼저 확인해야 합니다.

# 2. 변수와 변수명 작성 규칙

변수(Variable)는 프로그램 실행 중 값을 저장하는 이름 있는 공간입니다.

- 영문자, 숫자, 밑줄(`_`)을 사용할 수 있습니다.
- 첫 글자에는 숫자를 사용할 수 없습니다.
- 공백이나 `*`, `+`, `-`, `/` 같은 연산 기호는 사용할 수 없습니다.
- 대문자와 소문자를 구분합니다.
- 언어가 미리 정한 예약어(Reserved Word)는 변수명으로 사용할 수 없습니다.

```c
int score1;     // 가능
int _count;     // 가능
// int 1score;  // 불가능: 숫자로 시작
// int for;     // 불가능: 예약어
```

# 3. 구조체와 가비지 컬렉터

## 3-1. 구조체(Structure)

구조체는 서로 다른 자료형의 변수들을 하나의 묶음으로 다루기 위한 사용자 정의 자료형입니다. C에서는 `struct` 예약어를 사용합니다.

```c
struct Student {
    int number;
    char name[20];
    double score;
};
```

위 구조체는 정수형 학번, 문자열 이름, 실수형 점수를 하나의 학생 정보로 묶습니다.

## 3-2. 가비지 컬렉터(Garbage Collector)

가비지 컬렉터는 프로그램이 더 이상 사용하지 않는 객체의 메모리를 찾아 자동으로 회수하는 기능입니다. Java처럼 가비지 컬렉션(Garbage Collection)을 지원하는 언어에서는 개발자가 모든 객체 메모리를 직접 해제하지 않아도 됩니다.

| 구분 | 메모리 관리 방식 |
|---|---|
| C | 개발자가 `malloc()`으로 할당하고 `free()`로 직접 해제 |
| Java | 사용하지 않는 객체를 가비지 컬렉터가 자동 회수 |

> **주의:** 원문의 설명만 보면 C에도 일반적인 자동 가비지 컬렉터가 있는 것처럼 오해할 수 있습니다. 표준 C는 자동 가비지 컬렉션을 기본 제공하지 않습니다.

# 4. 연산자의 종류

## 4-1. 산술 연산자(Arithmetic Operator)

| 연산자 | 의미 | 예시 |
|---|---|---|
| `+` | 덧셈 | `a + b` |
| `-` | 뺄셈 | `a - b` |
| `*` | 곱셈 | `a * b` |
| `/` | 나눗셈 | `a / b` |
| `%` | 나머지 | `a % b` |

나머지 연산자(Modulus Operator) `%`의 피연산자 규칙은 언어별로 다릅니다.

| 언어 | `%`에 사용할 수 있는 피연산자 |
|---|---|
| C | 정수형 피연산자 |
| Java | 정수형 또는 부동소수점형 피연산자 |

## 4-2. 증가 연산자(Increment Operator) 및 감소 연산자(Decrement Operator)

```c
int a = 5;
int b = ++a;
```

- `a`를 먼저 6으로 증가시킵니다.
- 증가한 값 6을 `b`에 저장합니다.
- 결과: `a = 6`, `b = 6`

```c
int a = 5;
int b = a++;
```

- 현재 값 5를 먼저 `b`에 저장합니다.
- 그다음 `a`를 6으로 증가시킵니다.
- 결과: `a = 6`, `b = 5`

> 전치(Prefix): 증가 또는 감소 후 사용  
> 후치(Postfix): 현재 값을 사용한 후 증가 또는 감소

## 4-3. 비트 연산자(Bitwise Operator)

비트 연산자는 정수 값을 2진수 비트 단위로 계산합니다.

| 연산자 | 의미 |
|---|---|
| 비트 AND(`&`) | 대응하는 두 비트가 모두 1일 때 1 |
| 비트 XOR(`^`) | 대응하는 두 비트가 서로 다를 때 1 |
| 비트 OR(`\|`) | 대응하는 비트 중 하나 이상이 1이면 1 |
| 비트 NOT(`~`) | 각 비트를 반전 |
| 왼쪽 시프트(`<<`) | 비트를 왼쪽으로 이동 |
| 오른쪽 시프트(`>>`) | 비트를 오른쪽으로 이동 |

예를 들어 `5`는 2진수 `0101`, `3`은 `0011`입니다.

```text
0101 & 0011 = 0001
0101 ^ 0011 = 0110
0101 | 0011 = 0111
```

## 4-4. 논리 연산자(Logical Operator)

| 연산자 | 의미 |
|---|---|
| `!` | 논리 NOT: 참과 거짓을 반전 |
| `&&` | 논리 AND: 양쪽이 모두 참일 때 참 |
| `\|\|` | 논리 OR: 한쪽 이상이 참이면 참 |

| 구분 | 연산자 | 대상 |
|---|---|---|
| 논리 연산(Logical Operation) | `&&`, `\|\|`, `!` | 참과 거짓 |
| 비트 연산(Bitwise Operation) | `&`, `\|`, `^`, `~` | 정수의 각 비트 |

## 4-5. 관계, 조건, 대입 연산자

- 관계 연산자(Relational Operator): `<`, `<=`, `>`, `>=`, `==`, `!=`
- 조건 연산자(Conditional Operator): `조건 ? 참일_때_값 : 거짓일_때_값`
- 대입 연산자(Assignment Operator): `=`, `+=`, `-=`, `*=`, `/=`, `%=` 등

```c
int mx = a < b ? b : a;
```

`a < b`가 참이면 `b`, 거짓이면 `a`를 `mx`에 저장하므로 두 값 중 큰 값이 저장됩니다.

# 5. 연산자 우선순위와 결합 방향

연산자 우선순위(Operator Precedence)는 여러 연산자가 함께 있을 때 무엇을 먼저 계산할지 정합니다. 결합 방향(Associativity)은 우선순위가 같은 연산자를 어느 방향부터 묶을지 정합니다.

C의 우선순위는 다음과 같습니다.

| 순위 | 종류 | 대표 연산자 | 결합 방향 |
|---:|---|---|---|
| 1(높음) | 후치 연산 | `()`, `[]`, `.`, `->`, 후치 `++`, `--` | 왼쪽에서 오른쪽 |
| 2 | 전치·단항 연산 | 전치 `++`, `--`, `+`, `-`, `!`, `~`, `sizeof` | 오른쪽에서 왼쪽 |
| 3 | 곱셈 계열 | `*`, `/`, `%` | 왼쪽에서 오른쪽 |
| 4 | 덧셈 계열 | `+`, `-` | 왼쪽에서 오른쪽 |
| 5 | 시프트 | `<<`, `>>` | 왼쪽에서 오른쪽 |
| 6 | 관계 | `<`, `<=`, `>`, `>=` | 왼쪽에서 오른쪽 |
| 7 | 동등 | `==`, `!=` | 왼쪽에서 오른쪽 |
| 8 | 비트 AND | `&` | 왼쪽에서 오른쪽 |
| 9 | 비트 XOR | `^` | 왼쪽에서 오른쪽 |
| 10 | 비트 OR | `\|` | 왼쪽에서 오른쪽 |
| 11 | 논리 AND | `&&` | 왼쪽에서 오른쪽 |
| 12 | 논리 OR | `\|\|` | 왼쪽에서 오른쪽 |
| 13 | 조건 연산 | `?:` | 오른쪽에서 왼쪽 |
| 14 | 대입 연산 | `=`, `+=`, `-=`, `*=`, `/=` 등 | 오른쪽에서 왼쪽 |
| 15(낮음) | 쉼표 연산 | `,` | 왼쪽에서 오른쪽 |

Java의 우선순위는 다음과 같습니다. C 전용 연산자인 `sizeof`와 쉼표 연산자는 포함하지 않습니다.

| 순위 | 종류 | 대표 연산자 | 결합 방향 |
|---:|---|---|---|
| 1(높음) | 후치 연산 | 후치 `++`, `--` | 왼쪽에서 오른쪽 |
| 2 | 전치·단항 연산 | 전치 `++`, `--`, `+`, `-`, `!`, `~` | 오른쪽에서 왼쪽 |
| 3 | 곱셈 계열 | `*`, `/`, `%` | 왼쪽에서 오른쪽 |
| 4 | 덧셈 계열 | `+`, `-` | 왼쪽에서 오른쪽 |
| 5 | 시프트 | `<<`, `>>`, `>>>` | 왼쪽에서 오른쪽 |
| 6 | 관계 | `<`, `<=`, `>`, `>=`, `instanceof` | 왼쪽에서 오른쪽 |
| 7 | 동등 | `==`, `!=` | 왼쪽에서 오른쪽 |
| 8 | 비트 AND | `&` | 왼쪽에서 오른쪽 |
| 9 | 비트 XOR | `^` | 왼쪽에서 오른쪽 |
| 10 | 비트 OR | `\|` | 왼쪽에서 오른쪽 |
| 11 | 논리 AND | `&&` | 왼쪽에서 오른쪽 |
| 12 | 논리 OR | `\|\|` | 왼쪽에서 오른쪽 |
| 13 | 조건 연산 | `?:` | 오른쪽에서 왼쪽 |
| 14(낮음) | 대입 연산 | `=`, `+=`, `-=`, `*=`, `/=` 등 | 오른쪽에서 왼쪽 |

**핵심:** 괄호가 있으면 우선순위보다 괄호 안을 먼저 계산합니다. 헷갈리면 식을 괄호로 나누어 변수값의 변화를 순서대로 적습니다.

# 6. 입출력 함수와 서식 문자열

## 6-1. 주요 서식 문자열(Format Specifier)

| 서식 | 의미 |
|---|---|
| `%d` | 10진 정수 입출력 |
| `%c` | 문자 입출력 |
| `%s` | 문자열 입출력 |

## 6-2. C의 `printf()`

```c
printf("%d, %c", a, b);
```

`a`를 정수로 출력하고 쉼표와 공백을 출력한 뒤, `b`를 문자로 출력합니다.

## 6-3. Java의 출력 함수

| 함수 | 특징 |
|---|---|
| `System.out.printf()` | 서식 문자열에 맞춰 출력 |
| `System.out.print()` | 값을 출력하고 줄을 바꾸지 않음 |
| `System.out.println()` | 값을 출력한 뒤 줄을 바꿈 |

```java
System.out.printf("%d", r);
System.out.print(r + s);
System.out.println(r + "은(는) 소수");
```

# 7. 조건문

## 7-1. 단순 if문(Simple If Statement)

조건이 참일 때만 특정 문장을 실행합니다.

```c
if (a > b) {
    printf("Gilbut");
}
```

## 7-2. if-else문

조건이 참일 때와 거짓일 때 서로 다른 문장을 실행합니다.

```c
if (a > b) {
    printf("참");
} else {
    printf("거짓");
}
```

## 7-3. switch문(Switch Statement)

하나의 값에 따라 여러 경로 중 하나를 선택할 때 사용합니다.

```c
switch (a) {
case 1:
    printf("바나나");
    break;
case 2:
    printf("딸기");
    break;
default:
    printf("없음");
}
```

- `case`는 비교할 값을 나타냅니다.
- `default`는 어떤 `case`와도 일치하지 않을 때 실행됩니다.
- `break`는 현재 `switch`문을 끝냅니다.
- `break`를 생략하면 다음 `case`의 문장도 계속 실행되는 폴스루(Fall-through)가 발생합니다.

# 8. 반복문

## 8-1. for문(For Statement)

반복 횟수가 비교적 명확할 때 사용합니다.

```c
int sum = 0;
for (int i = 1; i <= 10; i++) {
    sum = sum + i;
}
```

실행 순서는 `초기식 -> 조건식 -> 본문 -> 증감식 -> 조건식`입니다.

## 8-2. while문(While Statement)

조건을 먼저 검사하고, 참인 동안 본문을 반복합니다.

```c
while (i <= 10) {
    i = i + 1;
}
```

처음부터 조건이 거짓이면 본문을 한 번도 실행하지 않습니다.

## 8-3. do-while문(Do-While Statement)

본문을 먼저 실행한 다음 조건을 검사합니다.

```c
do {
    i = i + 1;
} while (i <= 10);
```

| 반복문 | 특징 |
|---|---|
| for문(For Statement) | 반복 횟수가 비교적 명확할 때 사용 |
| while문(While Statement) | 조건을 먼저 검사하고 반복 |
| do-while문(Do-While Statement) | 본문을 한 번 실행한 뒤 조건 검사 |

**핵심:** do-while문은 조건이 처음부터 거짓이어도 본문을 최소 한 번 실행합니다.

# 9. 배열과 문자열

## 9-1. 1차원 배열(One-Dimensional Array)

같은 자료형의 값을 일렬로 저장합니다. 배열 인덱스(Array Index)는 0부터 시작합니다.

```c
char a[3] = {'A', 'B', 'C'};
```

| 요소 | `a[0]` | `a[1]` | `a[2]` |
|---|---|---|---|
| 값 | `'A'` | `'B'` | `'C'` |

## 9-2. 2차원 배열(Two-Dimensional Array)

값을 행과 열의 평면 형태로 저장합니다.

```c
int b[2][3] = {
    {11, 22, 33},
    {44, 55, 66}
};
```

`b[0][1]`은 첫 번째 행의 두 번째 값인 `22`입니다.

## 9-3. C 문자열(Character Array)

C 문자열은 문자 배열에 저장되며 문자열의 끝에는 널 문자(Null Character) `\0`가 필요합니다.

```c
char text[5] = "love";
```

```text
'l' 'o' 'v' 'e' '\0'
```

**핵심:** 문자열을 저장할 배열은 보통 실제 문자 수보다 최소 1칸 더 확보해야 합니다.

# 10. 포인터

## 10-1. 포인터와 포인터 변수

포인터(Pointer)는 메모리 주소를 다루는 개념이고, 포인터 변수(Pointer Variable)는 주소를 저장하는 변수입니다.

- 주소 연산자(Address Operator) `&`: 변수의 주소를 구합니다.
- 간접 참조 연산자(Dereference Operator) `*`: 포인터가 가리키는 값에 접근합니다.

```c
int a = 50;
int *b;
b = &a;
*b = *b + 20;
printf("%d, %d", a, *b);
```

실행 과정:

1. `a`에 50을 저장합니다.
2. `b`를 정수형 주소를 저장할 포인터 변수로 선언합니다.
3. `b = &a`로 `a`의 주소를 `b`에 저장합니다.
4. `*b`는 `b`가 가리키는 `a`의 값입니다.
5. `*b + 20`은 `50 + 20`이므로 `a`의 값도 70으로 바뀝니다.

출력 결과:

```text
70, 70
```

## 10-2. 포인터와 배열

배열 이름은 대부분의 식에서 첫 번째 요소의 주소로 변환됩니다.

```c
int a[5];
int *b;

b = a;
// 다음 문장과 같은 시작 주소를 저장한다.
b = &a[0];
```

| 배열 표기 | 포인터 연산 표기 |
|---|---|
| `a[0]` | `*(a + 0)` |
| `a[1]` | `*(a + 1)` |
| `a[i]` | `*(a + i)` |

> **주의:** 배열 이름과 포인터 변수는 관련은 깊지만 완전히 같은 개념은 아닙니다. 배열 이름 자체에 다른 주소를 대입할 수는 없습니다.

# 11. Python의 입력과 출력

## 11-1. `input()` 함수

`input()`은 키보드 입력을 받아 문자열로 반환합니다.

```python
name = input("이름을 입력하세요: ")
```

숫자로 계산하려면 형변환이 필요합니다.

```python
a = int(input())
a, b = map(int, input().split())
```

두 번째 문장은 공백으로 나뉜 여러 문자열을 각각 정수로 변환하여 `a`와 `b`에 저장합니다.

## 11-2. `print()` 함수

```python
print(82, 24, sep="-", end=",")
```

- `sep`는 여러 출력값 사이의 구분 문자를 정합니다.
- `end`는 출력 마지막에 붙일 문자를 정합니다.
- 출력 결과는 `82-24,`입니다.

**핵심:** Python의 `input()` 결과는 기본적으로 문자열입니다.

# 12. Python의 자료형

| 자료형 | 특징 | 예시 |
|---|---|---|
| 리스트(List) | 순서가 있고 요소 변경 가능(Mutable) | `[10, "mike", 23.45]` |
| 튜플(Tuple) | 순서가 있지만 요소 변경 불가능(Immutable) | `(10, "mike", 23.45)` |
| 딕셔너리(Dictionary) | 키와 값(Key-Value)으로 구성 | `{"이름": "홍길동"}` |
| 범위 객체(Range) | 연속된 정수 범위를 표현 | `range(5)` |

## 12-1. 리스트(List)

```python
a = [10, "mike", 23.45]
a[0] = 1
```

- 인덱스는 0부터 시작합니다.
- 서로 다른 자료형의 값을 한 리스트에 넣을 수 있습니다.
- 위 코드 실행 후 `a`는 `[1, "mike", 23.45]`입니다.

## 12-2. 튜플(Tuple)

튜플은 리스트처럼 순서가 있지만 생성 후 요소를 추가, 삭제, 변경할 수 없습니다.

```python
point = (10, 20)
```

## 12-3. 딕셔너리(Dictionary)

```python
person = {"이름": "홍길동", "나이": 25, "주소": "서울"}
person["이름"] = "이순신"
```

리스트는 위치 인덱스로 접근하지만 딕셔너리는 사용자가 지정한 키(Key)로 값에 접근합니다.

# 13. Python의 range와 슬라이싱

## 13-1. `range()`

| 형식 | 생성 범위 |
|---|---|
| `range(stop)` | 0부터 `stop - 1`까지 |
| `range(start, stop)` | `start`부터 `stop` 직전까지 |
| `range(start, stop, step)` | `start`부터 `step`만큼 증가하며 `stop` 직전까지 |

```python
list(range(5))        # [0, 1, 2, 3, 4]
list(range(4, 9))     # [4, 5, 6, 7, 8]
list(range(1, 15, 3)) # [1, 4, 7, 10, 13]
```

**핵심:** `range()`의 종료값 `stop`은 결과에 포함되지 않습니다.

## 13-2. 슬라이싱(Slicing)

문자열, 리스트 같은 순차형 객체에서 일부 구간을 잘라 반환합니다.

```python
a = ["a", "b", "c", "d", "e"]

a[1:3]   # ["b", "c"]
a[0:5:2] # ["a", "c", "e"]
a[3:]    # ["d", "e"]
a[:3]    # ["a", "b", "c"]
a[::3]   # ["a", "d"]
```

형식은 `[start:stop:step]`이며 `stop` 위치의 요소는 포함하지 않습니다.

# 14. Python의 반복문

## 14-1. range를 이용한 for문

```python
total = 0
for i in range(1, 11):
    total = total + i
```

`i`에 1부터 10까지 차례대로 저장하며 합계를 누적합니다.

## 14-2. 리스트를 직접 순회하는 for문

```python
values = [1, 2, 3, 4, 5]
total = 0

for value in values:
    total = total + value
```

## 14-3. while문

```python
i = 1
while i <= 10:
    i = i + 1
```

Python은 중괄호 대신 들여쓰기로 코드 블록을 구분하므로 들여쓰기가 문법의 일부입니다.

# 15. Python의 클래스와 함수

## 15-1. 클래스(Class)와 객체(Object)

클래스는 객체를 만들기 위한 설계도이고, 객체는 클래스로부터 생성한 실제 대상입니다.

- 속성(Attribute): 객체가 가진 데이터
- 메소드(Method): 클래스 안에서 객체의 동작을 정의한 함수
- `self`: 메소드를 호출한 현재 객체를 가리키는 매개변수

```python
class Cls:
    x = 10

    def add(self, a):
        return a + self.x

obj = Cls()
obj.x = 5
print(obj.add(5))
```

실행 과정:

1. `Cls`의 객체 `obj`를 만듭니다.
2. `obj.x = 5`로 객체 속성 `x`를 5로 설정합니다.
3. `obj.add(5)`를 호출하면 `self`는 `obj`, 매개변수 `a`는 5입니다.
4. `a + self.x`는 `5 + 5`이므로 10을 반환합니다.

출력 결과:

```text
10
```

## 15-2. 클래스 밖의 함수(Function)

원문에서 클래스 없이 사용하는 메소드라고 표현한 것은 Python 용어로는 함수(Function)라고 부르는 것이 정확합니다.

```python
def calc(x, y):
    x *= y
    return x

a, b = 3, 4
a = calc(a, b)
print(a, b)
```

`calc(3, 4)`가 12를 반환하여 `a`에 저장되고 `b`는 그대로 4이므로 결과는 다음과 같습니다.

```text
12 4
```

# 16. 스크립트 언어

스크립트 언어(Script Language)는 일반적으로 다른 프로그램이나 실행 환경 안에서 명령을 해석하며 실행하는 언어입니다.

| 언어 | 특징 |
|---|---|
| 자바스크립트(JavaScript) | 웹 페이지의 동작을 제어하는 데 널리 사용 |
| PHP | 웹 서버에서 동적 페이지를 만드는 데 사용하는 서버 측 언어 |
| 파이썬(Python) | 귀도 반 로섬이 만든 범용 인터프리터 언어 |
| 쉘 스크립트(Shell Script) | 운영체제 셸 명령을 조합하여 작업을 자동화 |
| BASIC | 초보자 교육 목적으로 시작된 대화형 언어 계열 |

쉘 스크립트의 대표 제어문은 다음과 같습니다.

- 선택문: `if`, `case`
- 반복문: `for`, `while`, `until`

# 17. 라이브러리와 동적 메모리 할당

동적 메모리 할당(Dynamic Memory Allocation)은 프로그램 실행 중 필요한 만큼 메모리를 확보하고, 사용이 끝나면 해제하는 방법입니다.

## 17-1. 라이브러리(Library)

| 구분 | 의미 |
|---|---|
| 표준 라이브러리(Standard Library) | 프로그래밍 언어와 함께 기본 제공되는 함수, 모듈, 패키지 |
| 외부 라이브러리(External Library) | 개발자가 별도로 설치하여 사용하는 라이브러리 |

## 17-2. C의 `stdlib.h`

`stdlib.h`는 자료형 변환, 난수, 동적 메모리 관리 등에 필요한 함수를 제공합니다.

| 함수 | 의미 |
|---|---|
| `atoi()` | 문자열을 `int` 값으로 변환 |
| `atof()` | 문자열을 실수 값으로 변환 |
| `srand()` | 의사 난수 생성기의 시드 설정 |
| `rand()` | 의사 난수 생성 |
| `malloc()` | 동적 메모리 할당 |
| `free()` | 동적 메모리 해제 |

## 17-3. `malloc()`과 `free()`

```c
#include <stdlib.h>

int *numbers = malloc(5 * sizeof(int));

if (numbers != NULL) {
    numbers[0] = 10;
    free(numbers);
    numbers = NULL;
}
```

- `malloc()`은 바이트 단위로 메모리를 동적 할당합니다.
- 할당에 실패하면 `NULL`을 반환합니다.
- 사용이 끝난 메모리는 `free()`로 해제해야 합니다.

# 18. Java의 예외 처리

예외(Exception)는 프로그램 실행 중 발생하는 비정상적인 상황입니다. Java에서는 `try`, `catch`, `finally`로 처리할 수 있습니다.

```java
try {
    int result = 10 / value;
    System.out.println(result);
} catch (ArithmeticException e) {
    System.out.println("계산 오류");
} finally {
    System.out.println("처리 종료");
}
```

- `try`: 예외가 발생할 수 있는 코드
- `catch`: 발생한 예외를 처리하는 코드
- `finally`: 예외 발생 여부와 관계없이 일반적으로 실행되는 코드

> **주의:** `finally`는 보통 실행되지만 JVM이 강제로 종료되는 등 정상적인 제어 흐름 자체가 끝나는 특수 상황에서는 실행이 보장되지 않을 수 있습니다.

# 19. 기억장치 배치 전략

가변 크기의 빈 기억 공간에 프로세스를 배치하는 대표 방법입니다.

| 배치 전략 | 선택 기준 |
|---|---|
| 최초 적합(First Fit) | 들어갈 수 있는 첫 번째 공간 |
| 최적 적합(Best Fit) | 배치 후 남는 공간이 가장 작은 공간 |
| 최악 적합(Worst Fit) | 배치 후 남는 공간이 가장 큰 공간 |

> 최초 적합: 첫 번째  
> 최적 적합: 가장 작게 남김  
> 최악 적합: 가장 크게 남김

# 20. 페이지 교체 알고리즘

## 20-1. 기본 용어

- 페이지(Page): 가상기억장치를 일정한 크기로 나눈 단위
- 페이지 프레임(Page Frame): 주기억장치에서 페이지가 들어가는 공간
- 페이지 부재(Page Fault): 필요한 페이지가 현재 주기억장치에 없는 상황

## 20-2. 선입선출 페이지 교체(FIFO Page Replacement)

FIFO는 주기억장치에 가장 먼저 들어와 가장 오래 있었던 페이지를 먼저 교체합니다.

원문의 참조열 `2, 3, 2, 1, 5, 2, 3, 5`를 3개의 빈 프레임으로 처리하면 다음과 같습니다.

| 순서 | 참조 페이지 | 프레임 상태 | 결과 |
|---:|---:|---|---|
| 1 | 2 | `[2, -, -]` | 부재 |
| 2 | 3 | `[2, 3, -]` | 부재 |
| 3 | 2 | `[2, 3, -]` | 적중 |
| 4 | 1 | `[2, 3, 1]` | 부재 |
| 5 | 5 | `[5, 3, 1]` | 부재, 가장 오래된 2 교체 |
| 6 | 2 | `[5, 2, 1]` | 부재, 가장 오래된 3 교체 |
| 7 | 3 | `[5, 2, 3]` | 부재, 가장 오래된 1 교체 |
| 8 | 5 | `[5, 2, 3]` | 적중 |

페이지 부재 횟수는 총 6회입니다.

**핵심:** 페이지가 이미 프레임에 있으면 페이지 부재도, 교체도 발생하지 않습니다.

# 21. 스래싱

스래싱(Thrashing)은 프로세스의 실제 처리보다 페이지를 불러오고 교체하는 데 더 많은 시간을 쓰는 현상입니다.

```text
프레임 부족 -> 페이지 부재 증가 -> 페이지 교체 증가 -> 실제 처리 시간 감소 -> 성능 급락
```

**핵심:** 스래싱은 프로세스 실행보다 페이지 교체에 시간을 더 많이 소비하는 상태입니다.

# 22. 프로세스와 스레드

| 개념 | 의미 |
|---|---|
| 프로그램(Program) | 실행되기 전 저장장치에 있는 명령어 집합 |
| 프로세스(Process) | 실행 중인 프로그램 |
| 프로세스 제어 블록(PCB, Process Control Block) | 운영체제가 프로세스 상태와 관리 정보를 저장하는 자료구조 |
| 스레드(Thread) | 프로세스 내부에서 실행되는 작업 단위 |

하나의 프로세스는 하나 이상의 스레드를 가질 수 있습니다. 같은 프로세스의 스레드는 코드와 데이터 등의 자원을 공유하지만, 각자 실행 흐름을 가집니다.

# 23. CPU 스케줄링

높은 응답률 우선(HRN, Highest Response Ratio Next)은 대기시간이 긴 작업과 서비스시간이 짧은 작업을 함께 고려하는 비선점 스케줄링 방식입니다.

```text
우선순위 = (대기시간 + 서비스시간) / 서비스시간
```

| 작업 | 대기시간 | 서비스시간 | 계산 | 우선순위 |
|---|---:|---:|---|---:|
| A | 5 | 5 | `(5 + 5) / 5` | 2.0 |
| B | 10 | 6 | `(10 + 6) / 6` | 약 2.67 |
| C | 15 | 7 | `(15 + 7) / 7` | 약 3.14 |
| D | 20 | 8 | `(20 + 8) / 8` | 3.5 |

계산값이 가장 큰 D의 우선순위가 가장 높습니다.

**핵심:** HRN은 계산 결과가 클수록 먼저 실행합니다.

# 24. UNIX 주요 명령어

| 명령어 | 의미 |
|---|---|
| `fork` | 새로운 프로세스 생성 |
| `uname` | 시스템 및 커널 정보 확인 |
| `wait` | 부모 프로세스가 자식 프로세스의 상태 변화를 기다림 |
| `chmod` | 파일 또는 디렉터리 권한 변경 |
| `ls` | 디렉터리의 파일 목록 출력 |
| `cat` | 파일 내용 출력 |
| `chown` | 파일 또는 디렉터리 소유자 변경 |
| `umask` | 새 파일과 디렉터리의 기본 권한에서 제거할 권한 비트 설정 |

일반적인 기본 권한 기준은 파일 `666`, 디렉터리 `777`입니다. 최종 권한은 개념적으로 다음과 같이 구합니다.

```text
최종 권한 = 기본 권한 & (~umask)
```

예를 들어 `umask`가 `022`이면 일반 파일은 보통 `644`, 디렉터리는 `755`로 생성됩니다.

> **주의:** 시험의 간단한 예에서는 `666 - umask`, `777 - umask`처럼 설명하기도 하지만 실제 처리는 산술 뺄셈이 아니라 권한 비트 마스크 연산입니다.

# 25. IPv4와 IPv6

| 구분 | IPv4 | IPv6 |
|---|---|---|
| 주소 길이 | 32비트 | 128비트 |
| 구성 | 8비트씩 4부분 | 16비트씩 8부분 |
| 기본 표기 | 10진수와 점 | 16진수와 콜론 |
| 주소 유형 | 유니캐스트, 멀티캐스트, 브로드캐스트 | 유니캐스트, 멀티캐스트, 애니캐스트 |

IPv4의 A~E 클래스는 오늘날의 CIDR 이전에 사용한 역사적인 클래스풀(Classful) 구분입니다.

| 클래스 | 용도 |
|---|---|
| A | 대규모 네트워크 |
| B | 중규모 네트워크 |
| C | 소규모 네트워크 |
| D | 멀티캐스트 |
| E | 실험·예약 용도 |

IPv6는 넓은 주소 공간뿐 아니라 확장성·융통성, IPv4와의 상호운용성, QoS 지원의 용이성을 특징으로 합니다.

IPv4 예시:

```text
192.168.0.1
```

IPv6 예시:

```text
2001:0db8:0000:0000:0000:ff00:0042:8329
```

- 유니캐스트(Unicast): 한 송신자에서 한 수신자로 전송
- 멀티캐스트(Multicast): 특정 수신자 그룹으로 전송
- 애니캐스트(Anycast): 같은 주소를 가진 여러 대상 중 경로상 가까운 하나로 전송

> **주의:** IPv6에는 IPv4 방식의 브로드캐스트가 없으며 멀티캐스트와 애니캐스트를 활용합니다.

# 26. OSI 7계층

> 물리 → 데이터 링크 → 네트워크 → 전송 → 세션 → 표현 → 응용

| 계층 | 전송 단위 또는 핵심 역할 |
|---|---|
| 1. 물리 계층(Physical Layer) | 비트(Bit)를 전기적, 광학적 신호로 전송 |
| 2. 데이터 링크 계층(Data Link Layer) | 인접 노드 사이의 프레임(Frame) 전송 |
| 3. 네트워크 계층(Network Layer) | 패킷(Packet)의 경로 설정과 중계 |
| 4. 전송 계층(Transport Layer) | 종단 간 전달·분할/재조립·포트 다중화를 담당하며, 신뢰성은 TCP 등 프로토콜에 따라 제공 |
| 5. 세션 계층(Session Layer) | 대화와 연결 세션 관리 |
| 6. 표현 계층(Presentation Layer) | 데이터 형식 변환, 압축, 암호화 |
| 7. 응용 계층(Application Layer) | 사용자 응용 프로그램에 네트워크 서비스 제공 |

## 26-1. 데이터 링크 계층(Data Link Layer)

인접한 시스템 사이에서 프레임을 신뢰성 있고 효율적으로 전송합니다.

- 흐름 제어
- 프레임 동기화
- 오류 제어
- 순서 제어
- 대표 프로토콜: HDLC, LAPB, PPP, LLC

## 26-2. 네트워크 계층(Network Layer)

서로 다른 네트워크 사이에서 패킷을 목적지까지 전달합니다.

- 경로 설정(Routing)
- 트래픽 제어
- 패킷 전달과 중계

## 26-3. 전송 계층(Transport Layer)

종단 시스템 사이의 종단 간 전송(End-to-End Delivery)을 담당합니다.

- 연결 설정과 해제
- 주소 설정
- 다중화
- 오류 제어
- 흐름 제어

## 26-4. 세션 계층(Session Layer)

송신 측과 수신 측의 대화를 생성, 관리, 종료합니다.

- 대화 제어
- 동기 제어
- 데이터 교환 관리

# 27. 네트워크 장비

| 장비 | 관련 계층 | 핵심 기능 |
|---|---|---|
| 리피터(Repeater) | 물리 계층 | 약해진 신호를 재생하여 다시 전송 |
| 브리지(Bridge) | 데이터 링크 계층 | LAN 구간을 연결하고 프레임을 중계 |
| 스위치(Switch) | 주로 데이터 링크 계층 | MAC 주소를 바탕으로 프레임 전달 |
| 라우터(Router) | 네트워크 계층 | 서로 다른 네트워크 사이의 최적 경로 선택 |
| 브라우터(Brouter) | 데이터 링크 및 네트워크 계층 | 브리지와 라우터 기능 결합 |
| 전처리기(Front-End Processor) | 통신 처리 장치 | 통신 제어와 사전 처리를 담당하여 중앙 시스템 부담 감소 |

# 28. TCP/IP 주요 프로토콜

## 28-1. MQTT(Message Queuing Telemetry Transport)

MQTT는 발행-구독(Publish-Subscribe) 방식의 경량 메시징 프로토콜로, 네트워크 대역폭과 자원이 제한된 IoT 환경에서 자주 사용합니다.

## 28-2. TCP와 UDP

| 구분 | TCP | UDP |
|---|---|---|
| 연결 방식 | 연결형(Connection-Oriented) | 비연결형(Connectionless) |
| 신뢰성 | 높음 | 상대적으로 낮음 |
| 순서 보장 | 제공 | 제공하지 않음 |
| 오류 및 흐름 제어 | 제공 | 제한적이며 흐름 제어 없음 |
| 데이터 형태 | 바이트 스트림 | 데이터그램 |
| 속도와 부담 | 상대적으로 느리고 제어 부담이 큼 | 빠르고 제어 부담이 작음 |
| 주요 활용 | 정확성과 순서가 중요한 전송 | 실시간, 저지연 전송 |

TCP(Transmission Control Protocol)는 연결을 설정한 뒤 신뢰성 있는 양방향 전송을 제공합니다. 순서 제어, 오류 제어, 흐름 제어, 다중화 기능을 수행합니다.

UDP(User Datagram Protocol)는 연결 설정 없이 데이터를 전송합니다. TCP와 같은 순서 보장과 흐름 제어가 없어 빠른 전송이 필요한 상황에 유리합니다.

## 28-3. ARP

주소 결정 프로토콜(ARP, Address Resolution Protocol)은 같은 네트워크에서 알고 있는 IPv4 주소에 대응하는 MAC 주소를 알아냅니다.

> ARP: IP 주소 → MAC 주소

# 29. CSMA/CD

반송파 감지 다중 접속 및 충돌 탐지(CSMA/CD, Carrier Sense Multiple Access with Collision Detection)는 전통적인 공유형 이더넷(Ethernet)에서 사용하는 매체 접근 제어 방식이며 IEEE 802.3과 관련됩니다.

동작 흐름:

1. 전송 전에 통신 매체가 사용 중인지 확인합니다.
2. 비어 있으면 데이터를 전송합니다.
3. 전송 중 충돌이 발생했는지 감지합니다.
4. 충돌하면 전송을 멈추고 임의 시간 후 다시 시도합니다.

> **주의:** 오늘날의 전이중(Full-Duplex) 스위치 이더넷에서는 송수신 경로가 분리되어 충돌이 발생하지 않으므로 CSMA/CD를 사용하지 않습니다.

# 시험 직전 필수 암기표

| 주제 | 핵심 내용 |
|---|---|
| C 구조체(Structure) | 서로 다른 자료형의 변수를 하나로 묶으며 `struct` 사용 |
| 가비지 컬렉터(Garbage Collector) | 사용하지 않는 객체의 메모리를 자동 회수 |
| 전치 증가(Prefix Increment) | 먼저 증가한 뒤 값 사용 |
| 후치 증가(Postfix Increment) | 값을 먼저 사용한 뒤 증가 |
| 비트 XOR(`^`) | 두 비트가 다르면 1 |
| 논리 연산 | 논리 AND, 논리 OR, 논리 NOT |
| 조건 연산자(Conditional Operator) | `조건 ? 참값 : 거짓값` |
| `%d` | 10진 정수 입출력 |
| `%c` | 문자 입출력 |
| `%s` | 문자열 입출력 |
| switch의 `break` 생략 | 다음 case까지 이어서 실행되는 폴스루 발생 |
| do-while문 | 최소 한 번 실행 |
| C 문자열의 끝 | 널 문자 `\0` |
| 주소 연산자(`&`) | 변수의 주소를 구함 |
| 간접 참조 연산자(`*`) | 포인터가 가리키는 값에 접근 |
| `a[i]` | `*(a + i)`와 같은 요소에 접근 |
| Python `input()` | 문자열로 입력 |
| Python 리스트(List) | 요소 변경 가능 |
| Python 튜플(Tuple) | 요소 변경 불가능 |
| Python 딕셔너리(Dictionary) | 키와 값으로 구성 |
| `range(start, stop)` | `stop`은 포함하지 않음 |
| 슬라이싱(Slicing) | `[start:stop:step]`, `stop` 제외 |
| `malloc()` | 동적 메모리 할당, 실패 시 `NULL` |
| `free()` | 동적 메모리 해제 |
| Java 예외 처리 | `try`, `catch`, `finally` |
| 최초 적합(First Fit) | 첫 번째로 들어갈 수 있는 공간 |
| 최적 적합(Best Fit) | 남는 공간이 가장 작은 곳 |
| 최악 적합(Worst Fit) | 남는 공간이 가장 큰 곳 |
| FIFO | 가장 먼저 들어온 페이지 교체 |
| 스래싱(Thrashing) | 실제 처리보다 페이지 교체 시간이 더 많음 |
| 프로세스(Process) | 실행 중인 프로그램 |
| 스레드(Thread) | 프로세스 내부의 실행 단위 |
| PCB | 운영체제가 프로세스 관리 정보를 저장하는 자료구조 |
| HRN | `(대기시간 + 서비스시간) / 서비스시간`, 큰 값 우선 |
| `fork` | 프로세스 생성 |
| `chmod` | 권한 변경 |
| `chown` | 소유자 변경 |
| `umask` | 생성 기본 권한에서 제거할 권한 비트 설정 |
| IPv4 | 32비트, 10진수와 점 |
| IPv6 | 128비트, 16진수와 콜론 |
| 데이터 링크 계층(Data Link Layer) | 프레임, 오류 제어, 흐름 제어 |
| 네트워크 계층(Network Layer) | 경로 설정과 패킷 전달 |
| 전송 계층(Transport Layer) | 종단 간 데이터 전송 |
| 세션 계층(Session Layer) | 대화와 동기 제어 |
| 리피터(Repeater) | 신호 재생 |
| 스위치(Switch) | MAC 주소 기반 프레임 전달 |
| 라우터(Router) | 네트워크 간 최적 경로 선택 |
| MQTT | 발행-구독 기반 IoT 메시징 |
| TCP | 연결형, 신뢰성, 순서 및 흐름 제어 |
| UDP | 비연결형, 빠른 전송 |
| ARP | IPv4 주소를 MAC 주소로 변환 |
| CSMA/CD | 공유형 이더넷의 충돌 감지 방식 |
