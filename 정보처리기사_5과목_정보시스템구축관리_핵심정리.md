# 정보처리기사 필기 5과목 — 정보시스템 구축 관리 핵심 정리

> 소프트웨어 유지·재공학 → 비용과 일정 관리 → 품질·위험 관리 → 정보시스템 신기술 → 데이터베이스 관리 → 보안과 암호화 → 공격 기법과 보안 시스템

이 문서는 원본 PDF에서 `5과목 정보시스템 구축 관리`가 시작되는 33쪽 오른쪽부터 마지막 41쪽까지를 바탕으로 정리했습니다. 원문에 오해하기 쉬운 설명이 있는 경우에는 시험 흐름을 해치지 않는 범위에서 정확한 표현으로 보완했습니다.

# 1. 소프트웨어 재사용, 재공학, CASE

## 1-1. 소프트웨어 재사용(Software Reuse)

소프트웨어 재사용은 이미 만들어진 명세서, 설계, 코드, 모듈 등을 다른 시스템 개발에 다시 활용하는 것입니다.

- 개발 시간과 비용을 줄일 수 있습니다.
- 검증된 부품을 활용하면 품질 향상에 도움이 됩니다.
- 생산성이 높아지고 문서·설계·코드를 공유할 수 있습니다.

## 1-2. 소프트웨어 재공학(Software Reengineering)

소프트웨어 재공학은 기존 시스템을 분석하고 개선하여 유지보수성과 사용성을 높이는 활동입니다.

| 활동 | 핵심 의미 |
|---|---|
| 분석(Analysis) | 기존 시스템의 동작을 이해하고 재공학 대상을 선정 |
| 재구성(Restructuring) | 기능을 유지하면서 코드나 구조를 더 나은 형태로 개선 |
| 역공학(Reverse Engineering) | 기존 시스템에서 분석·설계 정보를 찾아냄 |
| 이식(Migration) | 다른 운영체제나 하드웨어 환경에서도 사용하도록 변환 |

**핵심:** 역공학은 정보를 추출하는 분석 활동이고, 재공학은 그 분석을 바탕으로 시스템을 개선하는 더 넓은 활동입니다.

## 1-3. 컴퓨터 지원 소프트웨어 공학(CASE, Computer-Aided Software Engineering)

CASE는 소프트웨어 개발 과정을 자동화 도구로 지원하는 것입니다.

- 모듈 재사용성을 높입니다.
- 개발 기간과 비용을 줄이는 데 도움을 줍니다.
- 품질과 유지보수성을 높입니다.

# 2. 소프트웨어 비용 산정

## 2-1. 코드 라인 수(LOC, Lines of Code) 기법

LOC 기법은 소스 코드의 예상 라인 수를 바탕으로 노력, 비용, 기간을 산정하는 방법입니다. 예상 LOC를 구할 때는 낙관 추정치(Optimistic Estimate), 기대 추정치 또는 최빈 추정치(Most Likely Estimate), 비관 추정치(Pessimistic Estimate)를 고려합니다. 이는 PERT의 낙관시간·최빈시간·비관시간과 구분합니다.

```text
노력(인월) = 개발 기간 × 투입 인원
노력(인월) = LOC / 1인당 월평균 생산 코드 라인 수
개발 비용 = 노력(인월) × 1인당 월평균 인건비
개발 기간 = 노력(인월) / 투입 인원
생산성 = LOC / 노력(인월)
```

| 변수 | 의미 | 단위 |
|---|---|---|
| LOC | 예상 코드 라인 수 | 줄 |
| 노력 | 프로젝트에 필요한 총 작업량 | 인월(Person-Month) |
| 개발 기간 | 개발에 걸리는 기간 | 개월 |
| 투입 인원 | 개발에 참여하는 인원 | 명 |
| 1인당 월평균 생산 코드 라인 수 | 한 사람이 한 달에 생산하는 평균 코드량 | 줄/인월 |
| 1인당 월평균 인건비 | 한 사람의 한 달 평균 인건비 | 원/인월 |
| 개발 비용 | 개발에 필요한 총 인건비 | 원 |
| 생산성 | 1인월당 생산한 코드량 | 줄/인월 |

개발 기간과 투입 인원이 주어지면 두 값을 곱해 총 노력을 구합니다. LOC와 생산 코드 라인 수가 주어지면 두 값을 나누어 노력을 구한 뒤, 노력에 인건비를 곱해 비용을 구하거나 노력에서 투입 인원을 나누어 기간을 구합니다. 생산성은 LOC를 노력으로 나누어 구합니다. 예를 들어 예상 LOC가 12,000줄이고 1인당 월평균 생산 코드 라인 수가 1,000줄/인월이면 노력은 `12,000 / 1,000 = 12인월`입니다. 3명이 투입되면 개발 기간은 `12 / 3 = 4개월`이며, 이는 3명이 투입될 때 완료까지 4개월이 필요하다는 뜻입니다.

## 2-2. 건설적 비용 산정 모형(COCOMO, Constructive Cost Model)

COCOMO는 배리 보엠(Barry Boehm)이 제안한 LOC 기반 비용 산정 모형입니다.

| 유형 | 특징 |
|---|---|
| 조직형(Organic Mode) | 비교적 작고 단순하며 경험 있는 팀이 개발하는 프로젝트 |
| 반분리형(Semi-Detached Mode) | 조직형과 내장형의 중간 규모·복잡도 프로젝트 |
| 내장형(Embedded Mode) | 하드웨어, 실시간성, 규제 등 강한 제약을 받는 복잡한 시스템 |

> **주의:** COCOMO 유형은 코드 라인 수만으로 정하지 않습니다. 프로젝트 규모, 복잡성, 개발 환경, 제약조건을 함께 고려합니다.

# 3. 일정 관리 기법

## 3-1. 프로그램 평가 및 검토 기술(PERT)

PERT는 작업별 낙관시간(Optimistic Time), 최빈시간(Most Likely Time), 비관시간(Pessimistic Time)을 이용해 일정의 불확실성을 고려하는 기법입니다.

```text
기대시간 = (낙관시간 + 4 × 최빈시간 + 비관시간) / 6
```

| 변수 | 의미 | 단위 |
|---|---|---|
| 낙관시간 | 작업이 가장 순조로울 때의 예상 시간 | 시간 또는 일 |
| 최빈시간 | 가장 가능성이 높은 예상 시간 | 시간 또는 일 |
| 비관시간 | 작업이 가장 불리할 때의 예상 시간 | 시간 또는 일 |
| 기대시간 | 세 추정 시간을 가중 평균한 예상 소요 시간 | 시간 또는 일 |

세 추정 시간은 모두 같은 단위를 사용해 식에 대입합니다. 예를 들어 낙관시간이 2일, 최빈시간이 5일, 비관시간이 8일이면 `(2 + 4 × 5 + 8) / 6 = 5일`입니다. 이는 해당 작업의 기대 소요 시간이 5일이라는 뜻입니다.

- 작업 간 선후 관계를 파악할 수 있습니다.
- 일정상 여유 시간과 임계 경로를 분석할 수 있습니다.

## 3-2. 임계 경로(Critical Path)

임계 경로는 프로젝트 완료 시간을 결정하는 **가장 긴 경로**입니다. 임계 경로의 작업이 늦어지면 전체 프로젝트도 늦어질 수 있습니다.

## 3-3. 간트 차트(Gantt Chart)

간트 차트는 작업 일정을 수평 막대로 표현합니다. 막대의 길이는 작업 기간을 뜻합니다.

| 기법 | 핵심 특징 |
|---|---|
| PERT | 작업 순서와 시간의 불확실성을 분석 |
| 임계 경로(Critical Path) | 전체 완료 시간을 결정하는 가장 긴 경로 |
| 간트 차트(Gantt Chart) | 작업별 일정과 진행 상태를 막대로 표시 |

**핵심:** 간트 차트는 일정 파악에는 직관적이지만 복잡한 작업 의존 관계를 표현하는 데는 한계가 있습니다.

# 4. 프로젝트, 위험, 성숙도 관리

## 4-1. 소프트웨어 프로젝트 관리(Software Project Management)

정해진 기간과 비용 안에서 요구사항을 만족시키는 시스템을 개발하기 위한 전반적인 활동입니다. 일정, 비용, 인력, 품질, 위험을 함께 관리합니다.

## 4-2. 위험 관리(Risk Management)

프로젝트 진행 중 발생할 수 있는 위험을 미리 파악하고 대책을 세우는 활동입니다.

> 위험 식별(Risk Identification) → 위험 분석 및 평가(Risk Analysis and Assessment) → 위험 관리 계획(Risk Management Planning) → 위험 감시 및 조치(Risk Monitoring and Control)

- 위험 감시(Risk Monitoring)는 위험이 현실화될 징후를 지속해서 확인하는 활동입니다.

## 4-3. 능력 성숙도 통합 모델(CMMI, Capability Maturity Model Integration)

CMMI는 소프트웨어 개발 조직의 프로세스 능력과 성숙도를 평가하는 모델입니다.

| 단계 | 의미 |
|---|---|
| 초기(Initial) | 프로세스가 개인 역량에 크게 의존 |
| 관리(Managed) | 프로젝트 단위의 계획과 관리가 이루어짐 |
| 정의(Defined) | 조직 표준 프로세스가 정의되어 사용됨 |
| 정량적 관리(Quantitatively Managed) | 측정값을 바탕으로 프로세스를 관리 |
| 최적화(Optimizing) | 지속적인 개선을 수행 |

> 초기 → 관리 → 정의 → 정량적 관리 → 최적화
> **초관정정최**

# 5. 네트워크와 컴퓨팅 신기술

## 5-1. 소프트웨어 정의 네트워킹(SDN, Software-Defined Networking)

SDN은 네트워크의 제어 평면(Control Plane)과 데이터 평면(Data Plane)을 분리하고, 제어 기능을 소프트웨어로 중앙 관리하는 구조입니다. 정책 변경과 네트워크 가상화가 쉬워집니다.

## 5-2. 유비쿼터스 컴퓨팅(Ubiquitous Computing)

언제 어디서나 여러 기기를 통해 컴퓨팅 서비스를 사용할 수 있는 환경입니다. 사용자가 컴퓨터의 존재를 강하게 의식하지 않아도 서비스를 이용하는 것이 핵심입니다.

## 5-3. 클라우드 컴퓨팅 서비스 유형

| 유형 | 사용자가 제공받는 범위 |
|---|---|
| 서비스형 인프라(IaaS) | 서버, 스토리지, 네트워크 등 인프라 |
| 서비스형 플랫폼(PaaS) | 애플리케이션 개발·배포·실행 플랫폼 |
| 서비스형 소프트웨어(SaaS) | 완성된 소프트웨어를 서비스 형태로 이용 |

**핵심:** IaaS → PaaS → SaaS로 갈수록 사용자가 직접 관리하는 범위가 줄어듭니다.

## 5-4. 서버 팜(Server Farm)과 웹 크롤링(Web Crawling)

- 서버 팜은 여러 서버를 하나의 시스템처럼 운영해 대용량 처리, 부하 분산, 안정적 서비스를 지원합니다.
- 웹 크롤링은 크롤러(Crawler)가 웹 페이지의 URL을 자동으로 탐색하고 데이터를 수집하는 작업입니다. 웹 스크래핑(Web Scraping)은 수집한 웹 페이지에서 필요한 데이터를 추출하는 작업입니다. Python의 Scrapy 같은 도구가 사용될 수 있습니다.

## 5-5. 메타버스(Metaverse), 메시 네트워크(Mesh Network)

- 메타버스는 현실 세계와 가상 세계가 결합되어 사회·경제·문화 활동이 이뤄지는 3차원 가상 공간을 뜻합니다.
- 메시 네트워크는 다수의 노드를 그물망처럼 연결해 여러 통신 경로를 확보하는 구조입니다. 일부 노드에 문제가 생겨도 다른 경로를 쓸 수 있습니다.

## 5-6. 클라우드 HSM과 파스-타(PaaS-TA)

하드웨어 보안 모듈(HSM, Hardware Security Module)은 암호화 키의 생성, 저장, 연산을 보호하는 장치입니다. 클라우드 HSM은 이를 클라우드 환경에서 제공하는 형태입니다.

> **주의:** HSM은 하드웨어 보호 경계로 키 탈취 위험을 줄이지만, 모든 보안 취약점을 자동으로 제거하지는 않습니다.

파스-타(PaaS-TA)는 애플리케이션 개발·배포·운영을 지원하는 국내 개방형 클라우드 플랫폼입니다.

## 5-7. 근거리 통신, SSO, 무선 보안

- 징(Zing)은 원문에서 짧은 거리의 초고속 근접 무선통신으로 소개합니다. 시험에서는 용어와 핵심 특징 중심으로 봅니다.
- 통합 인증(SSO, Single Sign-On)은 중앙 인증 시스템에서 한 번 인증하면 여러 서비스에 접근할 수 있어 편리합니다. 다만 중앙 인증 정보가 침해되면 피해 범위가 여러 서비스로 커질 수 있습니다.
- Wi-Fi 보호 접속(WPA, Wi-Fi Protected Access)은 무선 LAN의 인증과 암호화를 위한 표준입니다. WEP의 취약점을 보완하기 위해 등장했으며 WPA, WPA2, WPA3는 서로 다른 세대입니다.

# 6. 라우팅, 컨테이너, 플랫폼 기술

## 6-1. RIP와 OSPF(Open Shortest Path First)

| 구분 | RIP | OSPF |
|---|---|---|
| 방식 | 거리 벡터(Distance Vector) | 링크 상태 라우팅(Link-State Routing) |
| 주요 기준 | 홉 수(Hop Count) | 링크 비용(Cost) |
| 최대 홉 수 | 15 | 고정된 15홉 제한 없음 |
| 특징 | 단순하지만 대규모망에 한계 | 수렴이 빠르고 대규모망에 적합 |

라우팅 정보 프로토콜(RIP)은 IGP에 속하고, 개방형 최단 경로 우선(OSPF)은 링크 상태 라우팅(Link-State Routing) 정보와 다익스트라 알고리즘(Dijkstra Algorithm)을 바탕으로 최단 경로를 계산합니다.

## 6-2. 증발품(Vaporware)과 도커(Docker)

- 증발품은 판매·배포 계획은 발표했지만 실제 출시가 지나치게 지연되거나 이뤄지지 않은 제품입니다.
- 도커는 애플리케이션과 라이브러리·설정 등 실행 환경을 이미지(Image)로 함께 패키징하고, 컨테이너 기술로 실행하도록 돕는 오픈소스 플랫폼입니다.

| 구분 | 컨테이너(Container) | 가상머신(Virtual Machine) |
|---|---|---|
| 실행 구조 | 호스트 운영체제 커널 공유 | 별도 게스트 운영체제 사용 |
| 크기 | 상대적으로 작음 | 상대적으로 큼 |
| 시작 속도 | 빠름 | 상대적으로 느림 |

## 6-3. 매시업, 디지털 트윈, BaaS

- 매시업(Mashup)은 여러 웹 서비스와 데이터를 결합해 새 서비스를 만드는 기술입니다.
- 디지털 트윈(Digital Twin)은 현실의 사물, 설비, 공정을 디지털 공간에 가상 모델로 복제하고 실제 객체의 상태를 반영하여 분석·예측에 활용하는 기술입니다.
- 서비스형 블록체인(BaaS, Blockchain as a Service)은 블록체인 앱 개발 환경을 클라우드에서 제공하여 이용자의 인프라 구성·운영 부담을 줄이는 서비스입니다.

# 7. 보안 기반 기술과 빅데이터

## 7-1. TCP 래퍼, DPI, 허니팟, 고가용성

| 기술 | 핵심 의미 |
|---|---|
| TCP 래퍼(TCP Wrapper) | 과거 UNIX 계열 시스템에서 호스트 기반으로 네트워크 서비스 접속을 허용·거부한 보안 도구 |
| 심층 패킷 검사(DPI, Deep Packet Inspection) | 헤더뿐 아니라 페이로드까지 분석해 침입을 탐지하고 악성 트래픽을 식별·관리함. 암호화된 트래픽은 분석이 제한될 수 있음 |
| 허니팟(Honeypot) | 공격자를 유인하고 공격 행위를 관찰하는 격리 시스템 |
| 고가용성(High Availability) | 장애 시 대체 시스템으로 전환하는 장애 조치(Failover)로 중단을 최소화함. 복구 뒤 원래 시스템으로 되돌리는 복귀 조치(Failback)와 구분 |

HACMP는 IBM 계열의 고가용성 클러스터 솔루션 이름으로 알려져 있습니다.

## 7-2. 하둡, 스쿱, 맵리듀스, 데이터 마이닝

- 하둡(Hadoop)은 대용량 데이터를 분산 저장·처리하는 오픈소스 플랫폼입니다.
- 하둡 분산 파일 시스템(HDFS, Hadoop Distributed File System)은 대용량 파일을 블록으로 나누어 여러 노드에 복제·분산 저장하는 계층입니다.
- 스쿱(Sqoop)은 관계형 데이터베이스(RDB)와 Hadoop 사이에서 대용량 데이터를 옮기는 도구입니다.
- 맵리듀스(MapReduce)는 Map 단계에서 데이터를 가공하고 Reduce 단계에서 결과를 모아 집계하는 분산 처리 모델입니다.
- 데이터 마이닝(Data Mining)은 대량 데이터에서 유용한 패턴과 지식을 찾는 기법입니다.

| 데이터 마이닝 유형 | 목적과 간단한 사례 |
|---|---|
| 분류(Classification) | 미리 정한 범주로 나눔: 스팸 메일 여부 판정 |
| 군집화(Clustering) | 유사한 특성끼리 묶음: 고객 집단 구분 |
| 연관 규칙(Association Rule) | 항목 사이의 동시 발생 관계를 찾음: 장바구니 품목 관계 분석 |
| 예측(Prediction) | 기존 데이터로 미래 값을 추정함: 판매량 예측 |

## 7-3. 온라인 분석 처리(OLAP, Online Analytical Processing)

OLAP은 다차원 데이터의 요약 정보를 분석하여 의사결정에 활용하는 방식입니다.

| 연산 | 의미 |
|---|---|
| 롤업(Roll-Up) | 더 높은 수준으로 요약 |
| 드릴다운(Drill-Down) | 더 세부적인 수준으로 이동 |
| 드릴스루(Drill-Through) | 요약 데이터에서 원천 상세 데이터로 이동 |
| 드릴어크로스(Drill-Across) | 관련된 다른 큐브나 사실 테이블로 이동 |
| 피벗(Pivot) | 분석 축의 방향을 변경 |
| 슬라이싱(Slicing) | 한 차원의 특정 값을 선택 |
| 다이싱(Dicing) | 여러 차원의 일부 범위를 선택 |

# 8. 데이터베이스 회복과 병행제어

## 8-1. 회복(Recovery)과 즉각 갱신(Immediate Update)

회복은 장애로 데이터베이스가 손상됐을 때 정상 상태로 복구하는 작업입니다.

즉각 갱신은 트랜잭션이 완료되기 전에도 변경 내용을 실제 데이터베이스에 반영할 수 있는 방식입니다. 장애에 대비해 로그(Log)를 남기며, 회복 때 Undo와 Redo가 모두 필요할 수 있습니다.

| 방식 | 핵심 특징 |
|---|---|
| 즉각 갱신(Immediate Update) | 커밋 전에도 반영 가능하며 Undo·Redo 고려 |
| 지연 갱신(Deferred Update) | 커밋 뒤 반영하며 일반적으로 Redo 중심 |

## 8-2. 병행제어(Concurrency Control)와 로킹

병행제어는 여러 트랜잭션이 동시에 실행될 때 갱신 분실과 같은 상호 간섭을 방지하고 데이터베이스의 일관성을 지키는 활동입니다.

- 데이터베이스 공유와 시스템 활용도를 높입니다.
- 일관성을 유지하고 사용자 응답시간을 줄입니다.

| 로킹 단위(Locking Granularity) | 장점 | 단점 |
|---|---|---|
| 큰 단위(데이터베이스·테이블) | 로크 수가 적어 관리가 쉬움 | 병행성이 낮음 |
| 작은 단위(페이지·행) | 병행성이 높음 | 로크 수와 관리 오버헤드 증가 |

대표적인 로킹 단위는 `데이터베이스 → 테이블 → 페이지 → 행` 순이며, 오른쪽으로 갈수록 단위가 작아집니다.

**핵심:** 로킹 단위가 작을수록 병행성은 높지만 관리 비용도 커집니다.

## 8-3. 교착상태(Deadlock)

교착상태는 서로 자원을 기다리며 더 이상 진행할 수 없는 상태입니다.

> 상호 배제(Mutual Exclusion) → 점유와 대기(Hold and Wait) → 비선점(Non-Preemption) → 환형 대기(Circular Wait)

네 조건이 모두 성립하면 교착상태가 발생할 수 있습니다. 처리 방법에는 예방, 회피, 탐지, 회복이 있습니다.

은행원 알고리즘(Banker's Algorithm)은 자원을 할당한 뒤에도 시스템이 안전 상태(Safe State)를 유지할 수 있을 때만 자원을 할당하는 회피 기법입니다.

# 9. 정보보안의 3대 요소

| 요소 | 의미 |
|---|---|
| 기밀성(Confidentiality) | 인가된 사용자만 정보에 접근하도록 보장 |
| 무결성(Integrity) | 인가되지 않은 변경·훼손을 막음 |
| 가용성(Availability) | 인가된 사용자가 필요할 때 서비스와 정보를 사용 가능 |

> **기무가**

**핵심:** 인가된 사용자만 정보를 수정할 수 있다는 성질은 무결성입니다.

# 10. 웹과 시스템 보안 약점

## 10-1. 세션 하이재킹(Session Hijacking)

쿠키 등에 저장된 세션 식별자(Session ID)를 탈취하거나 추측해 정상 사용자의 인증된 세션을 가장하고 권한을 탈취하는 공격입니다. 시험에서는 비동기화 상태, ACK Storm, 패킷 유실, 예상하지 못한 연결 재설정이 탐지 단서가 될 수 있음을 기억합니다.

## 10-2. 대표 약점

| 약점 | 핵심 내용 | 방어 방향 |
|---|---|---|
| SQL 삽입(SQL Injection) | 입력값으로 비정상 SQL이 실행되어 DB가 노출·변조될 수 있음 | 매개변수화 질의, 입력 검증 |
| 경로 조작(Path Traversal) | 외부 입력으로 파일 경로를 바꿔 서버 자원에 접근 | 허용 목록 검증, 경로 정규화 |
| 크로스사이트 스크립팅(XSS, Cross-Site Scripting) | 악성 스크립트가 사용자의 브라우저에서 실행 | 출력 인코딩, 입력 검증 |
| 스택 가드(Stack Guard) | 카나리값으로 스택 버퍼 오버플로 변조를 탐지 | 값 변조 시 실행 중단 |

| XSS 유형 | 악성 스크립트의 전달·실행 방식 |
|---|---|
| 저장형(Stored) | 서버에 저장된 스크립트가 페이지를 보는 사용자에게 전달되어 실행 |
| 반사형(Reflected) | 요청에 포함된 스크립트가 서버 응답에 바로 반사되어 실행 |
| DOM 기반(DOM-Based) | 브라우저의 클라이언트 측 코드가 DOM을 조작하는 과정에서 실행 |

접근 지정자(Access Modifier)는 객체·멤버의 외부 접근 범위를 정하는 예약어입니다. 다음 표는 Java 기준입니다.

| 접근 지정자 | 일반적인 접근 범위 |
|---|---|
| public | 모든 위치 |
| protected | 같은 패키지 및 상속 관계 |
| default | 같은 패키지 |
| private | 해당 클래스 내부 |

# 11. 암호화와 해시

## 11-1. 대칭키 암호화(Symmetric Key Encryption)

원문에서 개인키 암호화라고 표현한 방식은 보통 대칭키 암호화라고 부릅니다. 같은 비밀키로 암호화와 복호화를 수행하므로 빠르지만 키 배송과 관리가 어렵습니다.

```text
n명이 서로 직접 통신할 때 필요한 대칭키 수 = n(n - 1) / 2
```

| 구분 | 의미 | 대표 알고리즘 |
|---|---|---|
| 블록 암호(Block Cipher) | 일정 크기 블록 단위 암호화 | DES, SEED, AES, ARIA, IDEA |
| 스트림 암호(Stream Cipher) | 비트·바이트 단위의 연속 데이터 암호화 | RC4, LFSR 기반 방식 |

## 11-2. 공개키 암호화(Public Key Encryption)

공개키와 개인키라는 서로 다른 한 쌍의 키를 사용합니다. 공개키는 배포할 수 있지만 개인키는 해당 소유자가 비밀리에 관리해야 합니다.

```text
n명의 사용자가 각자 한 쌍의 키를 사용할 때 전체 키 수 = 2n
```

| 구분 | 알고리즘 |
|---|---|
| 대칭키 암호 | DES, AES, SEED, ARIA, IDEA |
| 공개키 암호 | RSA, ECC, Rabin |

- DES는 64비트 블록과 56비트 유효 키를 사용합니다. 현재 신규 시스템에 권장되지 않습니다.
- AES는 128비트 블록과 128·192·256비트 키를 지원하는 대칭키 블록 암호입니다.
- RSA와 Rabin은 큰 정수의 소인수분해 어려움에 기반합니다.
- ECC는 타원곡선 이산로그 문제에 기반합니다.

## 11-3. 해시(Hash)와 솔트(Salt)

해시는 임의 길이의 입력을 고정 길이의 값으로 바꾸는 일방향 함수입니다. 서로 다른 입력에서 같은 해시값을 찾기 어려워야 하는 충돌 저항성(Collision Resistance)이 필요합니다. 일반적인 암호화처럼 복호화를 전제로 하지 않으며 무결성 검증과 비밀번호 저장에 활용됩니다.

- 대표 해시: SHA 계열, MD4, MD5, N-HASH, SNEFRU
- MD4와 MD5는 충돌 취약점이 있어 보안 목적에 권장되지 않습니다.
- 솔트는 비밀번호에 사용자별로 고유하고 충분히 무작위인 값을 더한 뒤 해시하여 레인보우 테이블 공격을 어렵게 합니다. 일반적으로 비밀값일 필요는 없으며 해시값과 함께 저장할 수 있습니다.

# 12. 서비스 거부와 악성 프로그램

공격 유형은 구분 기준만 학습하며 실행 방법이나 도구 사용법은 다루지 않습니다.

| 공격 | 핵심 방식 |
|---|---|
| 죽음의 핑(Ping of Death) | 비정상적으로 큰 ICMP 패킷으로 장애 유발 |
| 스머핑(Smurfing) | 출발지 IP를 위조한 ICMP 브로드캐스트 증폭 공격 |
| LAND 공격 | 송신·수신 IP를 모두 공격 대상 주소로 설정 |
| 분산 서비스 거부(DDoS) | 여러 공격 지점이 동시에 자원을 고갈시킴 |
| 파밍(Pharming) | DNS·호스트 정보 변조로 정상 주소도 가짜 사이트로 연결 |

> **시험용 명칭:** Trin00, TFN, TFN2K, Stacheldraht는 과거의 대표적인 DDoS 도구명입니다. 사용법은 다루지 않습니다.

| 구분 | 핵심 방식 |
|---|---|
| 피싱(Phishing) | 가짜 메시지나 사이트로 사용자를 속여 링크 접속이나 정보 입력을 유도 |
| 파밍(Pharming) | DNS·호스트 정보 등을 변조해 정상 주소로 접속해도 가짜 사이트로 이동시킴 |

| 구분 | 웜(Worm) | 바이러스(Virus) |
|---|---|---|
| 확산 방식 | 네트워크를 통해 스스로 복제 | 보통 다른 파일이나 프로그램에 기생 |
| 사용자 실행 | 없이도 확산 가능 | 감염 파일 실행이 필요한 경우가 많음 |

랜섬웨어(Ransomware)는 파일이나 시스템을 암호화·잠금하고 복구를 조건으로 금전을 요구하는 악성 프로그램입니다. 백업, 보안 패치, 의심스러운 첨부파일 주의가 중요합니다.

# 13. 로그, 인증, 침입 탐지, SSH

## 13-1. 시스템 로그

| 로그 | 의미 |
|---|---|
| `wtmp` | 로그인·로그아웃, 시스템 시작·종료 이력 |
| `utmp` | 현재 로그인한 사용자 정보 |
| `btmp` | 로그인 실패 이력 |
| `lastlog` | 사용자별 마지막 로그인 정보 |

## 13-2. 인증(Authentication)과 인가(Authorization)

| 구분 | 의미 |
|---|---|
| 인증(Authentication) | 사용자가 누구인지 확인 |
| 인가(Authorization) | 인증된 사용자가 무엇을 할 수 있는지 결정 |

| 인증 요소 | 의미 | 예시 |
|---|---|---|
| 지식 기반(Something You Know) | 사용자가 알고 있는 정보 | 비밀번호, PIN |
| 소유 기반(Something You Have) | 사용자가 가진 물건 | OTP, 스마트카드 |
| 생체 기반(Something You Are) | 신체적 특징 | 지문, 홍채, 얼굴 |
| 행위 기반(Something You Do) | 행동 특성 | 서명, 음성, 키 입력 패턴 |

다중요소 인증(MFA)은 서로 다른 인증 요소를 두 가지 이상 결합하는 방식입니다.

## 13-3. 침입 탐지 시스템(IDS, Intrusion Detection System)

IDS는 시스템의 비정상 사용, 오용, 남용을 탐지하고 알림을 제공합니다.

| 탐지 방식 | 기준 | 특징 |
|---|---|---|
| 오용 탐지(Misuse Detection) | 알려진 공격 패턴 | 알려진 공격 탐지에 강함 |
| 이상 탐지(Anomaly Detection) | 정상 행위 기준에서의 이탈 | 새로운 공격도 탐지 가능하지만 오탐 가능 |

| 시스템 | 역할 |
|---|---|
| 침입 탐지 시스템(IDS) | 이상 행위를 탐지하고 알림 |
| 침입 방지 시스템(IPS, Intrusion Prevention System) | 탐지 후 차단까지 수행 |

## 13-4. 시큐어 셸(SSH, Secure Shell)

SSH는 암호화된 원격 로그인, 원격 명령 실행, 파일 전송을 지원하는 프로토콜입니다. 기본 포트는 `22`이며, 평문 전송을 사용하는 Telnet보다 안전합니다.

| 구분 | SSH | Telnet |
|---|---|---|
| 전송 보호 | 암호화 | 일반적으로 평문 |
| 기본 포트 | 22 | 23 |
| 보안성 | 높음 | 낮음 |

# 시험 직전 필수 암기표

| 주제 | 핵심 내용 |
|---|---|
| 소프트웨어 재사용(Software Reuse) | 개발 시간·비용 단축, 품질·생산성 향상 |
| 분석(Analysis) | 기존 시스템 이해 및 재공학 대상 선정 |
| 재구성(Restructuring) | 코드와 구조 개선 |
| 역공학(Reverse Engineering) | 기존 시스템에서 분석·설계 정보 추출 |
| 이식(Migration) | 다른 운영체제·하드웨어 환경으로 이전 |
| CASE | 소프트웨어 개발 과정 자동화 지원 |
| LOC | 코드 라인 수 기반 비용 산정 |
| 노력(인월) | 개발 기간 × 투입 인원 |
| COCOMO | LOC 기반 비용 산정 모형 |
| 조직형(Organic Mode) | 작고 비교적 단순한 프로젝트 |
| 반분리형(Semi-Detached Mode) | 조직형과 내장형의 중간 |
| 내장형(Embedded Mode) | 강한 제약이 있는 복잡한 시스템 |
| PERT | 낙관·최빈·비관 시간을 이용한 일정 분석 |
| 임계 경로(Critical Path) | 프로젝트 완료 시간을 결정하는 가장 긴 경로 |
| 간트 차트(Gantt Chart) | 작업 일정을 가로 막대로 표현 |
| 위험 관리 순서 | 식별 → 분석·평가 → 계획 → 감시·조치 |
| CMMI 순서 | 초기 → 관리 → 정의 → 정량적 관리 → 최적화 |
| SDN | 제어 평면과 데이터 평면을 분리해 소프트웨어로 제어 |
| IaaS | 인프라 제공 |
| PaaS | 개발·실행 플랫폼 제공 |
| SaaS | 완성된 소프트웨어 제공 |
| SSO | 한 번의 인증으로 여러 서비스 이용 |
| RIP | 거리 벡터, 최대 15홉 |
| OSPF | 링크 상태, 비용 기반 최단 경로 |
| Docker | 컨테이너 기반 애플리케이션 실행 환경 |
| 디지털 트윈(Digital Twin) | 현실의 객체를 디지털 공간에 가상화 |
| Hadoop | 대용량 데이터의 분산 저장·처리 |
| Sqoop | RDB와 Hadoop 사이의 대용량 데이터 전송 |
| MapReduce | Map과 Reduce 단계의 분산 처리 |
| OLAP | 다차원 분석과 의사결정 지원 |
| 즉각 갱신(Immediate Update) | 커밋 전에도 DB에 반영 가능 |
| 큰 로킹 단위 | 관리가 쉽지만 병행성 낮음 |
| 작은 로킹 단위 | 병행성 증가, 관리 오버헤드 증가 |
| 교착상태 조건 | 상호 배제·점유와 대기·비선점·환형 대기 |
| 은행원 알고리즘 | 안전 상태를 유지하는 교착상태 회피 기법 |
| 기밀성(Confidentiality) | 인가된 사용자만 접근 |
| 무결성(Integrity) | 인가되지 않은 변경 방지 |
| 가용성(Availability) | 필요할 때 사용 가능 |
| 세션 하이재킹(Session Hijacking) | 정상 사용자의 세션 탈취 |
| SQL 삽입(SQL Injection) | 악의적인 SQL 실행을 유도하는 입력 약점 |
| XSS | 사용자 브라우저에서 악성 스크립트 실행 |
| 스택 가드(Stack Guard) | 카나리값으로 스택 변조 탐지 |
| 대칭키 암호 | 같은 키로 암호화·복호화, 빠른 처리 |
| 공개키 암호 | 공개키와 개인키 한 쌍 사용 |
| 블록 암호 | 일정 크기의 블록 단위 암호화 |
| 스트림 암호 | 비트·바이트 스트림 단위 암호화 |
| 해시(Hash) | 고정 길이 결과를 만드는 일방향 함수 |
| 솔트(Salt) | 해시 전 무작위 값을 추가 |
| 죽음의 핑(Ping of Death) | 비정상적으로 큰 ICMP 패킷 공격 |
| 스머핑(Smurfing) | ICMP 브로드캐스트 증폭 공격 |
| LAND 공격 | 송신·수신 IP를 공격 대상 주소로 설정 |
| DDoS | 여러 지점에서 동시에 수행하는 서비스 거부 공격 |
| 파밍(Pharming) | 정상 주소로 접속해도 가짜 사이트로 유도 |
| 웜(Worm) | 스스로 복제하며 네트워크로 확산 |
| 랜섬웨어(Ransomware) | 파일 암호화 후 금전 요구 |
| `wtmp` | 로그인·로그아웃 및 시스템 시작·종료 이력 |
| `utmp` | 현재 로그인한 사용자 |
| `btmp` | 로그인 실패 이력 |
| `lastlog` | 마지막 로그인 정보 |
| 인증(Authentication) | 사용자가 누구인지 확인 |
| 인가(Authorization) | 허용 권한 결정 |
| 오용 탐지(Misuse Detection) | 알려진 공격 패턴 기반 탐지 |
| 이상 탐지(Anomaly Detection) | 정상 상태와 다른 행위 탐지 |
| IDS | 침입 탐지 |
| IPS | 침입 탐지와 차단 |
| SSH | 암호화된 원격 접속, 기본 포트 22 |
