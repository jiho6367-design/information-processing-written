# 정보처리기사 필기 2과목 — 소프트웨어 개발 핵심 정리

> 자료구조 → 트리와 그래프 → 수식 표기법 → 정렬·검색·해싱 → 개발·형상 관리 → 소프트웨어 테스트 → 코드 품질 → 인터페이스 구현

## 1. 자료구조의 분류

자료구조(Data Structure)는 데이터를 저장하고 처리하기 위한 구성 방식입니다. 자료 사이의 순서와 연결 관계에 따라 선형 구조와 비선형 구조로 나눕니다.

| 구분 | 자료구조 |
|---|---|
| 선형 구조(Linear Structure) | 배열(Array), 선형 리스트(Linear List), 연결 리스트(Linked List), 스택(Stack), 큐(Queue), 데크(Deque) |
| 비선형 구조(Non-Linear Structure) | 트리(Tree), 그래프(Graph) |

## 2. 스택(Stack)과 큐(Queue)

스택은 한쪽 끝에서만 삽입과 삭제가 이루어지는 자료구조입니다. 가장 나중에 넣은 자료가 가장 먼저 나오는 **후입선출(LIFO, Last In First Out)** 방식입니다.

- 삽입: 푸시(Push)
- 삭제: 팝(Pop)
- 활용: 함수 호출·복귀 주소 저장, 인터럽트 처리, 괄호 검사, 수식 변환·계산, 깊이 우선 탐색

큐는 먼저 넣은 자료가 먼저 나오는 **선입선출(FIFO, First In First Out)** 방식입니다. 삽입 위치는 리어(Rear), 삭제 위치는 프런트(Front)입니다.

| 구분 | 스택(Stack) | 큐(Queue) |
|---|---|---|
| 처리 방식 | 후입선출(LIFO) | 선입선출(FIFO) |
| 삽입·삭제 위치 | 같은 한쪽 끝 | 서로 다른 양쪽 끝 |
| 대표 연산 | Push, Pop | Enqueue, Dequeue |

**핵심:** 스택은 `나중에 넣은 것부터`, 큐는 `먼저 넣은 것부터` 처리합니다.

## 3. 그래프(Graph)

그래프는 정점(Vertex)과 간선(Edge)으로 자료의 연결 관계를 표현합니다. 방향이 있는 그래프는 방향 그래프(Directed Graph), 방향이 없는 그래프는 무방향 그래프(Undirected Graph)입니다.

| 용어 | 의미 |
|---|---|
| 차수(Degree) | 한 정점에 연결된 간선의 수입니다. 방향 그래프에서는 진입 차수와 진출 차수로 나눕니다. |
| 경로(Path) | 간선을 따라 정점들을 차례로 연결한 순서입니다. |
| 사이클(Cycle) | 출발 정점과 도착 정점이 같은 경로입니다. |

예를 들어 정점 A, B, C가 `A-B`, `B-C`, `C-A`로 연결되면 A의 차수는 2이고, `A → B → C`는 경로, `A → B → C → A`는 사이클입니다.

자기 자신을 향하는 간선을 허용하지 않을 때 정점이 `n`개인 그래프의 최대 간선 수는 다음과 같습니다.

```text
무방향 그래프 최대 간선 수 = n(n - 1) / 2
방향 그래프 최대 간선 수 = n(n - 1)
```

## 4. 트리(Tree)

트리는 노드(Node)와 가지(Branch)로 계층 관계를 표현하는 비선형 자료구조입니다.

| 용어 | 의미 |
|---|---|
| 루트 노드(Root Node) | 트리의 최상위 노드 |
| 부모 노드(Parent Node) | 하위 노드를 가지는 노드 |
| 자식 노드(Child Node) | 상위 부모 노드에 연결된 노드 |
| 형제 노드(Sibling Node) | 같은 부모를 가진 노드 |
| 단말 노드(Leaf Node) | 자식이 없는 노드 |
| 차수(Degree) | 한 노드에서 뻗어 나온 자식 또는 가지의 수 |
| 레벨(Level) | 루트부터의 계층 위치 |
| 깊이(Depth) | 루트에서 해당 노드까지의 경로 길이 |

## 5. 이진 트리 순회(Binary Tree Traversal)

이진 트리는 각 노드가 최대 두 개의 자식을 가질 수 있는 트리입니다. 순회는 노드를 방문하는 순서를 뜻합니다.

| 순회 | 방문 순서 | 암기 |
|---|---|---|
| 전위 순회(Preorder Traversal) | Root → Left → Right | 루왼오 |
| 중위 순회(Inorder Traversal) | Left → Root → Right | 왼루오 |
| 후위 순회(Postorder Traversal) | Left → Right → Root | 왼오루 |

원본의 이진 트리 예제는 루트 A의 왼쪽에 B, 오른쪽에 C가 있고, B 아래에는 D와 E, D 아래에는 H와 I, C 아래에는 F와 G가 있는 구조입니다. 이 트리를 실제 순서대로 따라가면 다음과 같습니다.

| 순회 | 방문 결과 | 따라가는 핵심 |
|---|---|---|
| 전위 순회(Preorder Traversal) | A → B → D → H → I → E → C → F → G | 루트를 먼저 방문한 뒤 왼쪽, 오른쪽을 방문 |
| 중위 순회(Inorder Traversal) | H → D → I → B → E → A → F → C → G | 왼쪽을 모두 방문한 뒤 루트, 오른쪽 방문 |
| 후위 순회(Postorder Traversal) | H → I → D → E → B → F → G → C → A | 왼쪽과 오른쪽을 모두 방문한 뒤 루트 방문 |

## 6. 수식 표기법(Expression Notation)

| 표기법 | 연산자 위치 | 예시 |
|---|---|---|
| 중위 표기법(Infix Notation) | 피연산자 사이 | `A + B` |
| 전위 표기법(Prefix Notation) | 피연산자 앞 | `+ A B` |
| 후위 표기법(Postfix Notation) | 피연산자 뒤 | `A B +` |

중위식을 전위·후위식으로 바꿀 때는 연산자 우선순위와 괄호를 먼저 고려합니다. 변환하거나 계산할 때 연산자와 피연산자 두 개를 하나의 작은 식 또는 값으로 묶어 스택에 다시 넣는 것이 공통 원리입니다.

```text
중위식: X = A / B * (C + D) + E
후위식: X A B / C D + * E + =
전위식: = X + * / A B + C D E
```

### 6-1. 중위 → 후위 변환

왼쪽부터 읽으며 피연산자는 바로 출력하고 연산자는 스택에 넣습니다. 연산자 우선순위와 결합 방향에 따라 스택 위 연산자를 먼저 계산해야 할 때 꺼내 출력하며, 닫는 괄호를 만나면 여는 괄호 전까지의 연산자를 꺼냅니다. 입력을 모두 읽은 뒤 스택의 연산자를 차례로 출력합니다.

### 6-2. 중위 → 전위 변환

연산 우선순위에 따라 식을 괄호로 묶고, 각 연산자를 해당 피연산자 두 개의 앞쪽으로 옮긴 뒤 괄호를 제거합니다. 예를 들어 `(A / B)`는 `/ A B`가 됩니다.

### 6-3. 후위 → 중위 변환

왼쪽부터 읽어 피연산자를 스택에 넣습니다. 연산자를 만나면 **먼저 꺼낸 값을 오른쪽 피연산자**, 다음에 꺼낸 값을 왼쪽 피연산자로 두고 `(왼쪽 연산자 오른쪽)`을 만들어 다시 넣습니다.

### 6-4. 전위 → 중위 변환

오른쪽부터 읽어 피연산자를 스택에 넣습니다. 연산자를 만나면 **먼저 꺼낸 값을 왼쪽 피연산자**, 다음에 꺼낸 값을 오른쪽 피연산자로 두고 `(왼쪽 연산자 오른쪽)`을 만들어 다시 넣습니다.

### 6-5. 후위식 계산

왼쪽부터 읽어 수를 스택에 넣고, 연산자를 만나면 먼저 꺼낸 값을 오른쪽, 다음에 꺼낸 값을 왼쪽에 놓아 계산합니다. `8 3 - 5 /`는 `(8 - 3) / 5`이므로 결과는 `1`입니다.

### 6-6. 전위식 계산

오른쪽부터 읽어 수를 스택에 넣고, 연산자를 만나면 먼저 꺼낸 값을 왼쪽, 다음에 꺼낸 값을 오른쪽에 놓아 계산합니다. `/ - 8 3 5`는 `(8 - 3) / 5`이므로 결과는 `1`입니다.

## 7. 정렬 알고리즘(Sorting)

| 정렬 | 평균 시간 복잡도 | 최악 시간 복잡도 | 핵심 |
|---|---:|---:|---|
| 삽입 정렬(Insertion Sort) | O(n²) | O(n²) | 정렬된 앞부분의 알맞은 위치에 값을 삽입 |
| 선택 정렬(Selection Sort) | O(n²) | O(n²) | 최솟값 또는 최댓값을 찾아 앞쪽과 교환 |
| 버블 정렬(Bubble Sort) | O(n²) | O(n²) | 인접한 두 값을 비교·교환 |
| 퀵 정렬(Quick Sort) | O(n log n) | O(n²) | 피벗(Pivot)을 기준으로 자료를 분할 |
| 힙 정렬(Heap Sort) | O(n log n) | O(n log n) | 완전 이진 트리 형태의 힙(Heap) 사용 |
| 합병 정렬(Merge Sort) | O(n log n) | O(n log n) | 자료를 나눈 뒤 정렬된 부분을 병합 |

예를 들어 `8, 5, 6, 2, 4`를 오름차순으로 정렬할 때:

- 삽입 정렬은 각 새 값을 왼쪽의 정렬된 부분에 끼워 넣습니다.
- 선택 정렬은 매 회전에서 가장 작은 값인 `2`를 찾아 앞자리와 바꿉니다.
- 버블 정렬은 인접한 값을 비교하여 큰 값을 오른쪽으로 계속 보냅니다.

| 정렬 | 첫 회전 또는 첫 핵심 단계 | 중간 배열 |
|---|---|---|
| 삽입 정렬(Insertion Sort) | 두 번째 값 5를 정렬된 부분의 8과 비교해 앞에 삽입 | `5, 8, 6, 2, 4` |
| 선택 정렬(Selection Sort) | 전체에서 최솟값 2를 찾아 첫 값 8과 교환 | `2, 5, 6, 8, 4` |
| 버블 정렬(Bubble Sort) | 왼쪽부터 인접 값을 비교·교환해 최댓값 8을 끝으로 이동 | `5, 6, 2, 4, 8` |
| 퀵 정렬(Quick Sort) | 가운데 값 6을 피벗으로 정해 작은 값과 큰 값으로 첫 분할 | `5, 2, 4 / 6 / 8` |
| 힙 정렬(Heap Sort) | 최대 힙의 루트 8을 끝으로 옮기고 앞부분을 다시 힙으로 구성 | `6, 5, 4, 2 / 8` |
| 합병 정렬(Merge Sort) | 인접한 한 원소 부분 배열끼리 정렬하며 첫 병합 | `5, 8 / 2, 6 / 4` |

안정 정렬(Stable Sort)은 같은 값의 원래 순서를 유지하는 정렬이고, 제자리 정렬(In-Place Sort)은 별도의 큰 배열 없이 원래 배열 안에서 정렬하는 방식입니다.

| 정렬 | 안정 정렬 여부 | 제자리 정렬 여부 |
|---|---|---|
| 삽입 정렬(Insertion Sort) | 안정 | 예 |
| 선택 정렬(Selection Sort) | 일반적으로 불안정 | 예 |
| 버블 정렬(Bubble Sort) | 안정 | 예 |
| 퀵 정렬(Quick Sort) | 일반적으로 불안정 | 일반적인 분할 구현에서는 예 |
| 힙 정렬(Heap Sort) | 불안정 | 예 |
| 합병 정렬(Merge Sort) | 안정 | 일반적인 배열 구현에서는 아니오 |

원본 예제인 8, 5, 6, 2, 4를 오름차순으로 정렬하면 다음과 같습니다.

| 회전 | 삽입 정렬(Insertion Sort) | 선택 정렬(Selection Sort) | 버블 정렬(Bubble Sort) |
|---|---|---|---|
| 초기 | 8, 5, 6, 2, 4 | 8, 5, 6, 2, 4 | 8, 5, 6, 2, 4 |
| 1회전 | 5, 8, 6, 2, 4 | 2, 5, 6, 8, 4 | 5, 6, 2, 4, 8 |
| 2회전 | 5, 6, 8, 2, 4 | 2, 4, 6, 8, 5 | 5, 2, 4, 6, 8 |
| 3회전 | 2, 5, 6, 8, 4 | 2, 4, 5, 8, 6 | 2, 4, 5, 6, 8 |
| 4회전 | 2, 4, 5, 6, 8 | 2, 4, 5, 6, 8 | 2, 4, 5, 6, 8 |

## 8. 이진 검색(Binary Search)

이진 검색은 **정렬된 자료에서만** 사용할 수 있습니다. 가운데 값과 찾을 값을 비교하여 검색 범위를 절반씩 줄입니다.

```text
중간 위치 = (첫 위치 + 마지막 위치) / 2
시간 복잡도 = O(log n)
```

## 9. 해싱(Hashing)

해싱은 키(Key)를 해시 함수(Hash Function)에 넣어 해시 테이블(Hash Table)의 저장 위치인 홈 주소(Home Address)를 찾는 방법입니다.

| 방법 | 홈 주소를 구하는 원리 |
|---|---|
| 제산법(Division Method) | 키를 테이블 크기보다 작은 소수로 나눈 나머지를 사용 |
| 제곱법(Mid-Square Method) | 키를 제곱한 뒤 가운데 부분을 사용 |
| 폴딩법(Folding Method) | 키를 여러 부분으로 나누어 더하거나 XOR한 값을 사용 |
| 숫자 분석법(Digit Analysis Method) | 키 값에서 분포가 좋은 자릿수를 선택해 사용 |

서로 다른 키가 같은 홈 주소를 가지는 현상은 충돌(Collision)입니다. 동의어(Synonym)는 동일한 홈 주소를 갖는 서로 다른 키를 뜻합니다. 체이닝(Chaining)은 충돌한 자료를 연결 리스트(Linked List)로 연결하여 저장합니다.

## 10. 데이터베이스 스키마 3계층(Schema Architecture)

| 스키마 | 관점 |
|---|---|
| 외부 스키마(External Schema) | 개별 사용자와 응용 프로그램 관점의 논리 구조 |
| 개념 스키마(Conceptual Schema) | 데이터베이스 전체의 논리적 구조와 제약 조건 |
| 내부 스키마(Internal Schema) | 실제 저장장치 관점의 물리적 저장 구조 |

> 외부는 사용자, 개념은 전체, 내부는 저장장치

## 11. 빌드 자동화 도구(Build Automation Tool)

| 도구 | 역할 |
|---|---|
| Ant | 빌드 절차를 자동화하는 도구 |
| Maven | 의존성 관리와 표준화된 프로젝트 빌드를 지원 |
| Gradle | Groovy 또는 Kotlin 기반의 유연한 빌드 도구 |
| Jenkins | 지속적 통합(Continuous Integration)과 자동화를 지원하는 서버 |

**핵심:** Ant·Maven·Gradle은 빌드 도구이고, Jenkins는 빌드·테스트·배포를 자동화할 수 있는 서버입니다.

## 12. 소프트웨어 패키징(Software Packaging)

소프트웨어 패키징은 모듈별 실행 파일을 묶어 배포 가능한 설치 파일로 만드는 작업입니다. 개발자보다 사용자를 중심으로 설치 환경, 설치 절차, 삭제 방법을 고려합니다.

## 13. 디지털 저작권 관리(DRM)

디지털 저작권 관리(DRM, Digital Rights Management)는 디지털 콘텐츠의 사용 권한을 보호하고 관리하는 기술입니다.

| 구성 요소 | 역할 |
|---|---|
| 클리어링 하우스(Clearing House) | 사용 권한과 라이선스 발급·결제 등을 관리 |
| 콘텐츠 제공자(Content Provider) | 콘텐츠를 제공 |
| 패키저(Packager) | 콘텐츠를 메타데이터와 함께 배포 가능한 형태로 묶고 암호화 |
| 콘텐츠 분배자(Content Distributor) | 암호화된 콘텐츠를 사용자에게 전달 |
| DRM 컨트롤러(DRM Controller) | 콘텐츠 이용 권한을 통제 |

DRM의 기술 요소에는 콘텐츠 암호화·키 관리, 식별 체계, 라이선스 관리, 정책 관리, 크랙 방지 기술이 있습니다.

## 14. 설치 매뉴얼(Installation Manual)

설치 매뉴얼은 사용자가 설치와 삭제를 스스로 할 수 있도록 작성합니다. 소프트웨어 개요, 설치 파일, 설치 환경, 설치 순서, 삭제 방법, 오류 해결 방법을 포함합니다.

## 15. 형상 관리와 버전 관리

소프트웨어 형상 관리(SCM, Software Configuration Management)는 개발 과정에서 변경되는 소스 코드와 문서를 체계적으로 관리하는 활동입니다.

| 활동 또는 기능 | 의미 |
|---|---|
| 체크아웃(Check-Out) | 저장소에서 수정할 파일을 가져옴 |
| 체크인(Check-In) | 변경한 파일을 저장소에 반영 |
| 커밋(Commit) | 변경 내용을 저장소의 새 이력으로 확정 |
| 병합(Merge) | 서로 다른 변경 내용을 합침 |
| 충돌(Conflict) | 같은 부분을 서로 다르게 수정해 자동 병합이 어려운 상태 |
| 차이 비교(Diff) | 버전 사이의 변경 내용을 비교 |

> **주의:** 중앙 집중형과 분산형 버전 관리 도구에 따라 체크인(Check-In)과 커밋(Commit)의 용례와 작업 흐름이 다를 수 있습니다. 시험에서는 둘 다 변경 내용을 저장소에 반영하는 작업이라는 범위에서 구분합니다.

대표 도구는 Git, CVS, Subversion(SVN), RCS, BitKeeper입니다.

| 활동 | 의미 |
|---|---|
| 형상 항목(Configuration Item) | 관리 대상이 되는 소스 코드, 문서, 실행 파일 등의 항목 |
| 버전 관리(Version Control) | 변경 이력을 보존하고 필요한 버전을 관리하는 활동 |
| 변경 관리(Change Control) | 변경 요청을 검토하고 승인, 반영, 추적하는 활동 |
| 형상 감사(Configuration Audit) | 형상 항목과 변경 절차가 기준에 맞는지 확인하는 활동 |
| 형상 기록(Status Accounting) | 형상 항목의 상태와 변경 이력을 기록하고 보고하는 활동 |

## 16. 소프트웨어 테스트(Software Testing)의 기본 원칙

| 원칙 | 의미 |
|---|---|
| 파레토 법칙(Pareto Principle) | 오류의 약 80%가 전체 모듈의 약 20%에서 발견될 수 있음 |
| 결함 집중(Defect Clustering) | 결함은 일부 모듈에 집중되는 경향이 있음 |
| 살충제 패러독스(Pesticide Paradox) | 같은 테스트를 반복하면 새 결함을 찾는 효과가 줄어듦 |
| 오류-부재의 궤변(Absence-of-Errors Fallacy) | 오류가 적어도 사용자 요구를 만족하지 못하면 좋은 시스템이 아님 |

완벽한 테스트는 현실적으로 어렵기 때문에, 테스트는 가능한 한 이른 단계부터 시작하고 위험이 큰 부분에 집중합니다.

## 17. 화이트박스와 블랙박스 테스트

| 구분 | 화이트박스 테스트(White-Box Testing) | 블랙박스 테스트(Black-Box Testing) |
|---|---|---|
| 기준 | 내부 코드와 제어 흐름 | 요구사항과 입출력 기능 |
| 코드 지식 | 필요 | 필수 아님 |
| 대표 기법 | 기초 경로·조건·루프·데이터 흐름 검사 | 동치 분할·경계값 분석·원인-효과 그래프·오류 예측 |

화이트박스 테스트는 프로그램 내부의 논리적 경로를 검사합니다. 블랙박스 테스트는 내부 구현보다 입력에 대한 출력 결과가 요구사항에 맞는지 검사합니다.

| 구분 | 세부 기법 | 핵심 |
|---|---|---|
| 화이트박스 테스트(White-Box Testing) | 기초 경로 검사(Basis Path Testing) | 독립적인 실행 경로를 기준으로 테스트합니다. |
|  | 조건 검사(Condition Testing) | 조건식의 참과 거짓 조합을 검사합니다. |
|  | 루프 검사(Loop Testing) | 반복문의 초기값, 종료 조건, 반복 횟수를 검사합니다. |
|  | 데이터 흐름 검사(Data Flow Testing) | 변수의 정의와 사용 관계를 검사합니다. |
| 블랙박스 테스트(Black-Box Testing) | 동치 분할 검사(Equivalence Partitioning) | 같은 결과가 기대되는 입력 집합을 나누어 대표값을 검사합니다. |
|  | 경계값 분석(Boundary Value Analysis) | 입력 범위의 경계와 경계 주변 값을 검사합니다. |
|  | 원인-효과 그래프 검사(Cause-Effect Graphing) | 입력 조건과 결과의 논리 관계를 그래프로 검사합니다. |
|  | 오류 예측(Error Guessing) | 경험상 오류가 날 만한 경우를 추정해 검사합니다. |
|  | 비교 검사(Comparison Testing) | 같은 기능을 수행하는 여러 구현의 결과를 비교합니다. |

## 18. V-모델(V-Model)과 테스트 단계

| 개발 단계 | 대응 테스트 |
|---|---|
| 요구사항 분석 | 인수 테스트(Acceptance Test) |
| 시스템 설계 | 시스템 테스트(System Test) |
| 아키텍처 설계 | 통합 테스트(Integration Test) |
| 모듈 설계·구현 | 단위 테스트(Unit Test) |

단위 테스트는 모듈 또는 컴포넌트 단위로 실시합니다. 알고리즘 오류, 잘못된 수식, 반복문의 종료 조건 오류 등을 찾는 데 적합합니다.

### 18-1. 인수 테스트(Acceptance Test)

| 테스트 | 수행 환경 |
|---|---|
| 알파 테스트(Alpha Test) | 개발자 환경에서 사용자와 개발자가 함께 수행 |
| 베타 테스트(Beta Test) | 실제 사용자 환경에서 제한된 사용자가 수행 |

## 19. 통합 테스트(Integration Testing)

| 방식 | 통합 방향 | 보조 도구 |
|---|---|---|
| 하향식 통합(Top-Down Integration) | 상위 모듈에서 하위 모듈로 | 테스트 스텁(Test Stub) |
| 상향식 통합(Bottom-Up Integration) | 하위 모듈에서 상위 모듈로 | 테스트 드라이버(Test Driver) |
| 빅뱅 통합(Big-Bang Integration) | 모든 모듈을 한 번에 결합 | 별도 단계적 통합 없음 |

하향식 통합은 깊이 우선(Depth-First)으로 한 경로를 먼저 완성하거나, 너비 우선(Breadth-First)으로 같은 수준의 모듈을 먼저 통합할 수 있습니다.

## 20. 테스트 드라이버와 스텁

**핵심:** 테스트 드라이버(Test Driver)는 호출하는 상위 모듈 대역이고, 테스트 스텁(Test Stub)은 호출되는 하위 모듈 대역입니다.

## 21. 테스트 오라클(Test Oracle)

테스트 오라클은 테스트 결과가 올바른지 판단하는 기준입니다.

| 종류 | 의미 |
|---|---|
| 참 오라클(True Oracle) | 모든 입력에 대해 정확한 결과를 제시 |
| 샘플링 오라클(Sampling Oracle) | 일부 입력에 대해서만 정확한 결과를 제시 |
| 휴리스틱 오라클(Heuristic Oracle) | 경험적 규칙이나 추정으로 결과를 판단 |
| 일관성 검사 오라클(Consistent Oracle) | 여러 실행 결과 사이의 일관성을 검사 |

핵심요약 PDF의 `추정 오라클`은 일반적으로 휴리스틱 오라클(Heuristic Oracle)로도 표현됩니다.

## 22. 시간 복잡도(Time Complexity)

시간 복잡도는 입력 크기 `n`이 커질 때 알고리즘의 실행 시간이 증가하는 정도를 나타냅니다.

> O(1) → O(log n) → O(n) → O(n log n) → O(n²) → O(2ⁿ) → O(n!)

| 복잡도 | 예시 |
|---|---|
| O(1) | 스택의 삽입(Push)·삭제(Pop) |
| O(log n) | 이진 검색(Binary Search) |
| O(n log n) | 힙 정렬(Heap Sort), 합병 정렬(Merge Sort) |
| O(n²) | 삽입·선택·버블 정렬 |

## 23. 순환 복잡도(Cyclomatic Complexity)

맥케이브 순환 복잡도(McCabe Cyclomatic Complexity)는 제어 흐름 그래프(Control Flow Graph)에서 독립적인 실행 경로의 수를 나타냅니다.

```text
V(G) = E - N + 2

E: 간선 수
N: 노드 수
```

조건 분기 수를 이용할 때는 다음처럼 간단히 계산할 수 있습니다.

```text
순환 복잡도 = 조건 분기 수 + 1
```

평면으로 그린 제어 흐름 그래프에서는 외부 영역을 포함한 영역 수로도 계산합니다.

```text
V(G) = 영역 수 = 내부 영역 수 + 외부 영역 수
```

핵심요약 PDF 예제는 내부 영역 3개와 외부 영역 1개이므로 순환 복잡도는 `3 + 1 = 4`입니다.

## 24. 클린 코드(Clean Code)와 정적 분석(Static Analysis)

클린 코드는 읽기 쉽고, 단순하며, 중복과 불필요한 의존성이 적은 코드입니다. 외계인 코드(Alien Code)는 오래되어 문서나 담당자가 없고 이해·유지보수가 어려운 코드를 말합니다.

추상화(Abstraction)는 한 단계의 코드가 같은 추상화 수준을 유지하도록 작성하는 원칙입니다. 예를 들어 상위 함수에는 업무 흐름만 두고 세부 처리는 하위 함수로 분리합니다.

정적 분석은 프로그램을 실행하지 않고 소스 코드의 결함 가능성, 규칙 위반, 복잡도 등을 검사하는 방법입니다. PMD, Checkstyle, Cppcheck 등이 대표 도구입니다.

## 25. EAI

| 유형 | 의미 |
|---|---|
| 지점 간 연결(Point-to-Point) | 애플리케이션을 1:1로 직접 연결 |
| 허브 앤드 스포크(Hub and Spoke) | 중앙 허브를 통해 데이터를 교환 |
| 메시지 버스(Message Bus) | 공통 미들웨어 버스를 통해 연결 |
| 하이브리드(Hybrid) | 허브 앤드 스포크와 메시지 버스를 결합 |

## 26. JSON과 AJAX

JSON(JavaScript Object Notation)은 속성-값(Attribute-Value) 쌍으로 데이터를 표현하는 텍스트 기반 교환 형식입니다.

AJAX(Asynchronous JavaScript and XML)는 웹 페이지 전체를 다시 불러오지 않고 서버와 비동기적으로 데이터를 주고받는 기술입니다. 이름에 XML이 포함되지만 JSON도 널리 사용합니다.

## 27. 인터페이스 보안과 검증 도구

### IPSec(IP Security)

IPSec은 네트워크 계층(Network Layer)에서 IP 패킷의 인증, 무결성, 기밀성을 제공하는 보안 프로토콜 묶음입니다.

| 구성 | 주요 기능 |
|---|---|
| 인증 헤더(AH, Authentication Header) | 인증과 무결성 제공, 암호화는 제공하지 않음 |
| 캡슐화 보안 페이로드(ESP, Encapsulating Security Payload) | 암호화와 선택적인 인증·무결성 제공 |

IPSec에는 전송 모드(Transport Mode)와 터널 모드(Tunnel Mode)가 있습니다. 전송 모드는 원래 IP 헤더를 유지하고, 터널 모드는 원래 패킷 전체를 새 IP 패킷 안에 캡슐화합니다.

### 트립와이어(Tripwire)

트립와이어는 파일과 시스템 설정의 기준 정보(Baseline)를 저장한 뒤 현재 상태와 비교하여 비인가 변경을 탐지하는 무결성 검사 도구입니다.

### 인터페이스 구현 검증 도구

| 도구 | 주요 용도 |
|---|---|
| xUnit | 여러 언어에 적용되는 단위 테스트 프레임워크 계열 |
| STAF | 서비스 호출과 컴포넌트 재사용 등을 지원하는 테스트 자동화 프레임워크 |
| FitNesse | 웹 기반 테스트 케이스 작성·실행·결과 확인 도구 |
| NTAF | FitNesse와 STAF의 장점을 결합한 테스트 자동화 프레임워크 |
| Watir | Ruby 기반 웹 애플리케이션 테스트 프레임워크 |

## 시험 직전 필수 암기표

| 주제 | 핵심 내용 |
|---|---|
| 선형 자료구조(Linear Structure) | 배열·리스트·스택·큐·데크 |
| 비선형 자료구조(Non-Linear Structure) | 트리·그래프 |
| 스택(Stack) | 후입선출(LIFO) |
| 큐(Queue) | 선입선출(FIFO) |
| 무방향 그래프 최대 간선 | `n(n-1)/2` |
| 방향 그래프 최대 간선 | `n(n-1)` |
| 전위 순회(Preorder Traversal) | Root → Left → Right |
| 중위 순회(Inorder Traversal) | Left → Root → Right |
| 후위 순회(Postorder Traversal) | Left → Right → Root |
| 중위·전위·후위 표기 | 연산자가 사이·앞·뒤에 위치 |
| 삽입 정렬(Insertion Sort) | 정렬된 부분에 값을 삽입 |
| 선택 정렬(Selection Sort) | 최솟값을 찾아 앞쪽과 교환 |
| 버블 정렬(Bubble Sort) | 인접한 값을 비교·교환 |
| 퀵 정렬(Quick Sort) | 피벗을 기준으로 분할 |
| 힙 정렬(Heap Sort) | 완전 이진 트리 이용 |
| 이진 검색(Binary Search) | 정렬된 자료에서 범위를 절반씩 축소 |
| 체이닝(Chaining) | 충돌 자료를 연결 리스트에 저장 |
| 외부·개념·내부 스키마 | 사용자·전체 논리 구조·물리 저장 구조 |
| Ant·Maven·Gradle | 빌드 도구 |
| Jenkins | 자동화·지속적 통합 서버 |
| DRM | 디지털 콘텐츠의 사용 권한 관리 |
| 체크아웃(Check-Out) | 저장소에서 작업 파일을 가져옴 |
| 체크인·커밋(Check-In·Commit) | 변경 내용을 저장소에 반영 |
| 형상 관리(SCM) | 변경 사항과 버전을 체계적으로 관리 |
| 파레토 법칙(Pareto Principle) | 오류의 80%가 모듈 20%에 집중 |
| 화이트박스 테스트 | 내부 논리와 경로 검사 |
| 블랙박스 테스트 | 요구사항과 입출력 기능 검사 |
| 단위 테스트(Unit Test) | 모듈·컴포넌트 단위 |
| 알파·베타 테스트 | 개발자 환경·실제 사용자 환경 |
| 테스트 드라이버(Test Driver) | 상위 모듈 대역, 상향식 통합에 사용 |
| 테스트 스텁(Test Stub) | 하위 모듈 대역, 하향식 통합에 사용 |
| 순환 복잡도 | 독립적인 실행 경로 수 |
| 좋은 코드 | 가독성·단순성·낮은 의존성·낮은 중복성 |
| Point-to-Point | 애플리케이션 1:1 연결 |
| Hub and Spoke | 중앙 허브 방식 |
| Message Bus | 공통 미들웨어 버스 방식 |
| EAI 하이브리드(Hybrid) | Hub and Spoke와 Message Bus 결합 |
| JSON | 속성-값 쌍의 텍스트 데이터 형식 |
| AJAX | 비동기 웹 통신 |
| Tripwire | 파일과 설정의 무결성 검사 |
| AH | 인증·무결성 제공 |
| ESP | 암호화와 선택적 인증·무결성 제공 |
